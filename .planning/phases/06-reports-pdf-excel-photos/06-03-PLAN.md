---
phase: "06-reports-pdf-excel-photos"
plan: "06-03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - apps/api/src/routes/relatorios.ts
  - apps/api/src/app.ts
autonomous: true
requirements: [REPORTS-01, REPORTS-04, REPORTS-05, REPORTS-07]

must_haves:
  truths:
    - "POST /api/relatorios retorna 202 imediatamente com o relatorioId sem bloquear"
    - "GET /api/relatorios lista relatórios do usuário atual com status e paginação"
    - "GET /api/relatorios/:id/download retorna presigned URL se PRONTO e não expirado"
    - "Rota de download retorna 404 se status != PRONTO e 410 se expiresAt < now"
    - "POST valida todos os filtros opcionais antes de enfileirar"
    - "Todos os endpoints exigem autenticação JWT (requireRole)"
  artifacts:
    - path: "apps/api/src/routes/relatorios.ts"
      provides: "Router Express com POST, GET list, GET download"
      exports: ["default (Router)"]
    - path: "apps/api/src/app.ts"
      provides: "Router montado em /api/relatorios"
      contains: "relatoriosRouter"
  key_links:
    - from: "apps/api/src/routes/relatorios.ts"
      to: "apps/api/src/jobs/queue.ts"
      via: "reportQueue.add()"
      pattern: "reportQueue"
    - from: "apps/api/src/routes/relatorios.ts"
      to: "apps/api/src/services/report-generator.ts"
      via: "getReportPresignedUrl()"
      pattern: "getReportPresignedUrl"
---

<objective>
Criar os endpoints REST para solicitar, listar e baixar relatórios. O POST enfileira o
job no BullMQ e retorna 202 imediatamente — zero spinner bloqueante no UI.

Purpose: Conectar o frontend ao worker assíncrono. Operador clica "Gerar Relatório",
o endpoint responde em < 50ms com o ID, e o Socket.IO notifica quando pronto.

Output: `relatorios.ts` com três rotas montadas em /api/relatorios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@apps/api/src/routes/eventos.ts
@apps/api/src/jobs/queue.ts
@apps/api/src/services/report-generator.ts
@.planning/phases/06-reports-pdf-excel-photos/06-01-SUMMARY.md
@.planning/phases/06-reports-pdf-excel-photos/06-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos dos planos anteriores que este plano consome -->

De apps/api/src/jobs/queue.ts (plan 06-01):
```typescript
export const reportQueue: Queue  // fila 'report-jobs'
```

De apps/api/src/jobs/report-worker.ts (plan 06-01/02):
```typescript
export type ReportJobPayload = {
  relatorioId: string;
  empresaId: string;
  formato: 'PDF' | 'XLSX';
  filtros: { dataInicio?, dataFim?, obraId?, cameraId?, classificacao?, placa? };
  criadoPor: string;
}
```

De apps/api/src/services/report-generator.ts (plan 06-02):
```typescript
export async function getReportPresignedUrl(key: string): Promise<string>
// TTL: 3600s, assina com GARAGE_SERVER_URL
```

Middleware disponível (padrão do projeto):
```typescript
// req.user = { sub: string, empresaId: string, role: Role }
// req.tenantClient = PrismaClient com empresaId injetado
import { requireRole } from '../middleware/rbac';
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar apps/api/src/routes/relatorios.ts com as 3 rotas</name>
  <files>
    apps/api/src/routes/relatorios.ts
  </files>
  <behavior>
    - POST /api/relatorios: retorna 202 + { relatorioId } sem await na geração
    - POST com formato inválido: retorna 400 + { error: 'formato deve ser PDF ou XLSX' }
    - GET /api/relatorios/:id/download com status PRONTO e não expirado: retorna { downloadUrl }
    - GET /api/relatorios/:id/download com status != PRONTO: retorna 404 + { error: 'Relatório não está pronto' }
    - GET /api/relatorios/:id/download com expiresAt < now: retorna 410 + { error: 'Link expirado' }
    - GET /api/relatorios/:id/download para relatorio de outro tenant: retorna 404
    - GET /api/relatorios: retorna { items: Relatorio[], nextCursor } com cursor pagination
  </behavior>
  <action>
Criar `apps/api/src/routes/relatorios.ts`:

```typescript
import { Router, type Router as RouterType } from 'express';
import { requireRole } from '../middleware/rbac';
import { reportQueue } from '../jobs/queue';
import { getReportPresignedUrl } from '../services/report-generator';
import type { ReportJobPayload } from '../jobs/report-worker';

const router: RouterType = Router();

// ============================================================
// POST /api/relatorios
// Enfileira geração assíncrona e retorna 202 imediatamente.
// REPORTS-01: não bloqueia — BullMQ processa em background.
// REPORTS-04: filtros opcionais validados aqui.
// ============================================================
router.post(
  '/',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR'),
  async (req, res) => {
    const { formato, filtros = {} } = req.body as {
      formato: unknown;
      filtros?: {
        dataInicio?: string;
        dataFim?: string;
        obraId?: string;
        cameraId?: string;
        classificacao?: string;
        placa?: string;
      };
    };

    // Validação de formato
    if (formato !== 'PDF' && formato !== 'XLSX') {
      res.status(400).json({ error: 'formato deve ser PDF ou XLSX' });
      return;
    }

    // Validação de classificação (enum Prisma)
    const validClassificacoes = ['LIBERADO', 'VISITANTE', 'ATENCAO', 'SUSPEITO', 'CRITICO'];
    if (filtros.classificacao && !validClassificacoes.includes(filtros.classificacao)) {
      res.status(400).json({ error: 'classificacao inválida' });
      return;
    }

    // Validar datas (ISO string)
    if (filtros.dataInicio && Number.isNaN(new Date(filtros.dataInicio).getTime())) {
      res.status(400).json({ error: 'dataInicio inválida' });
      return;
    }
    if (filtros.dataFim && Number.isNaN(new Date(filtros.dataFim).getTime())) {
      res.status(400).json({ error: 'dataFim inválida' });
      return;
    }

    const empresaId = req.user!.empresaId;
    const criadoPor = req.user!.sub;

    // Criar registro Relatorio em PENDENTE antes de enfileirar
    const relatorio = await req.tenantClient!.relatorio.create({
      data: {
        empresaId,
        formato,
        filtros: filtros as object,
        criadoPor,
        status: 'PENDENTE',
      },
    });

    // Enfileirar job — REPORTS-01: 202 imediato
    const payload: ReportJobPayload = {
      relatorioId: relatorio.id,
      empresaId,
      formato,
      filtros,
      criadoPor,
    };
    await reportQueue.add(`relatorio-${relatorio.id}`, payload, {
      jobId: relatorio.id, // idempotência: mesmo ID não enfileira duplicado
    });

    res.status(202).json({ relatorioId: relatorio.id });
  },
);

// ============================================================
// GET /api/relatorios
// Lista relatórios do usuário atual com cursor pagination.
// Operadores veem apenas os próprios; Admins veem da empresa toda.
// ============================================================
router.get(
  '/',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR'),
  async (req, res) => {
    const rawLimit = Number(req.query.limit ?? 20);
    const limit = Number.isFinite(rawLimit) ? Math.min(Math.max(rawLimit, 1), 50) : 20;
    const cursor = typeof req.query.cursor === 'string' ? req.query.cursor.trim() : undefined;
    const role = req.user!.role;
    const userId = req.user!.sub;

    // Operador vê apenas os próprios relatórios; Admin vê da empresa toda
    const criadoPorFilter = role === 'OPERADOR' ? { criadoPor: userId } : {};

    const relatorios = await req.tenantClient!.relatorio.findMany({
      where: criadoPorFilter,
      take: limit + 1,
      ...(cursor ? { cursor: { id: cursor }, skip: 1 } : {}),
      orderBy: { criadoEm: 'desc' },
      select: {
        id: true,
        formato: true,
        status: true,
        filtros: true,
        expiresAt: true,
        erroMsg: true,
        criadoEm: true,
        // garageKey não exposto diretamente — apenas via /download
      },
    });

    const hasMore = relatorios.length > limit;
    const page = hasMore ? relatorios.slice(0, limit) : relatorios;

    res.json({
      items: page,
      nextCursor: hasMore ? page[page.length - 1]?.id ?? null : null,
    });
  },
);

// ============================================================
// GET /api/relatorios/:id/download
// Gera presigned URL se relatório PRONTO e não expirado.
// REPORTS-07: link expira em 1 hora (gerenciado pelo expiresAt no banco).
// ============================================================
router.get(
  '/:id/download',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR'),
  async (req, res) => {
    const { id } = req.params;

    const relatorio = await req.tenantClient!.relatorio.findUnique({
      where: { id },
      select: { id: true, status: true, garageKey: true, expiresAt: true, empresaId: true },
    });

    // 404 se não existe ou pertence a outro tenant (tenantClient já filtra por empresaId)
    if (!relatorio) {
      res.status(404).json({ error: 'Relatório não encontrado' });
      return;
    }

    if (relatorio.status !== 'PRONTO') {
      res.status(404).json({ error: 'Relatório não está pronto' });
      return;
    }

    if (!relatorio.garageKey || !relatorio.expiresAt) {
      res.status(500).json({ error: 'Estado interno inválido do relatório' });
      return;
    }

    // REPORTS-07: verificar expiração
    if (new Date() > relatorio.expiresAt) {
      res.status(410).json({ error: 'Link expirado — solicite um novo relatório' });
      return;
    }

    // Gerar nova presigned URL (TTL 3600s)
    const downloadUrl = await getReportPresignedUrl(relatorio.garageKey);

    res.json({
      downloadUrl,
      expiresAt: relatorio.expiresAt.toISOString(),
    });
  },
);

export default router;
```
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    TypeScript compila. Router exportado como default. Três rotas implementadas:
    POST retorna 202, GET list com cursor pagination, GET download com 404/410 guards.
  </done>
</task>

<task type="auto">
  <name>Task 2: Montar router em app.ts e validar integração</name>
  <files>
    apps/api/src/app.ts
  </files>
  <action>
Localizar `apps/api/src/app.ts` (ou o arquivo equivalente que monta os routers Express —
pode ser `src/index.ts` ou `src/server.ts`). Adicionar:

```typescript
import relatoriosRouter from './routes/relatorios';

// Montar após os outros routers existentes
app.use('/api/relatorios', relatoriosRouter);
```

A linha de montagem deve seguir o mesmo padrão dos outros routers (`eventos`, `obras`,
`cameras`, etc.) já registrados no arquivo.

Após montar, executar o build completo para confirmar integração:
```bash
cd apps/api && pnpm build
```

Se `pnpm build` não existir no package.json de apps/api, usar `pnpm tsc --noEmit`.

Verificar também que `startReportWorker()` está sendo chamado no bootstrap (este passo
foi feito no plan 06-01, mas confirmar que não foi perdido na reescrita do worker em
plan 06-02). Se não estiver, adicionar a chamada junto com os outros workers.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    `app.ts` monta `/api/relatorios`. TypeScript compila. `startReportWorker()` chamado
    no bootstrap. Nenhum import circular introduzido.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → POST /api/relatorios | filtros vêm do corpo da request — validados antes de persistir |
| request → BullMQ | jobId = relatorioId garante idempotência — mesmo relatorio não gera dois jobs |
| GET download → Garage | presigned URL gerada server-side; chave garageKey nunca exposta ao cliente diretamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | GET /api/relatorios/:id/download | mitigate | tenantClient garante que apenas relatórios do proprio tenant são acessíveis; verificação de status e expiry antes de emitir URL |
| T-06-10 | Denial of Service | POST sem rate limiting — flood de jobs | mitigate | BullMQ deduplica via jobId=relatorioId; implementar rate limit de 10 req/min por usuário na rota POST (pode ser adicionado com `express-rate-limit` já usado no projeto) |
| T-06-11 | Tampering | filtros.classificacao injetado sem validação de enum | mitigate | Validação explícita do enum antes de persistir — array fixo de valores válidos |
| T-06-12 | Spoofing | criadoPor extraído do req.body em vez do JWT | mitigate | criadoPor = req.user!.sub — nunca do body; empresaId = req.user!.empresaId — nunca do body |
</threat_model>

<verification>
- POST /api/relatorios retorna 202 + { relatorioId } sem esperar geração
- POST com formato='DOC' retorna 400
- POST com classificacao='INVALIDO' retorna 400
- GET /api/relatorios/:id/download com status=PENDENTE retorna 404
- GET /api/relatorios/:id/download com expiresAt no passado retorna 410
- TypeScript compila sem erros em apps/api
- Router montado em /api/relatorios em app.ts
</verification>

<success_criteria>
- Três rotas REST funcionais com validação de entrada
- criadoPor e empresaId sempre extraídos do JWT — nunca do body
- Isolamento de tenant garantido pelo tenantClient em todas as queries
- Idempotência de jobs via jobId=relatorioId no BullMQ
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-reports-pdf-excel-photos/06-03-SUMMARY.md`
com: endpoints criados (method + path + response), campos validados no POST,
comportamento de autorização por role (OPERADOR vs ADMIN), e o arquivo onde o router
foi montado.
</output>
