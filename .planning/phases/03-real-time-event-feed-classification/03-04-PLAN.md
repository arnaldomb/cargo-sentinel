---
phase: 03
plan: 04
title: Dashboard Operacional + Classificação Inline
status: planned
created: 2026-06-21
depends_on:
  - 03-03-PLAN.md
requirements:
  - PLACA-05
  - PLACA-06
  - REALTIME-04
  - REALTIME-06
---

# 03-04 — Dashboard web em tempo real e classificação inline

## Objective

Construir a interface operacional da Fase 3: um dashboard autenticado com feed vivo, linhas coloridas por classificação, status de câmeras e reclassificação inline com confirmação para níveis 4 e 5.

## Scope

- layout autenticado da aplicação
- sidebar com status de câmeras
- feed com hidratação inicial via REST e updates via Socket.IO
- badge colorida por classificação
- popover inline de classificação
- confirmação para `SUSPEITO` e `CRITICO`
- pausa de auto-scroll quando o usuário desce a lista

## Out Of Scope

- alerta full-screen cross-site
- perfil detalhado da placa
- relatórios

## Context

Hoje a home autenticada é apenas um placeholder. A Fase 3 transforma `/` no painel principal do produto. O frontend deve nascer simples, rápido e operacional; a maior parte da inteligência vem do backend e das regras de tenant já prontas.

## UI Sections

### App shell

- top bar com nome do produto e ação de logout
- sidebar esquerda com câmeras e status
- coluna principal com feed ao vivo

### Feed row

- thumbnail
- placa
- obra
- câmera
- horário
- direção
- badge de classificação

### Interaction model

- click na badge abre seletor inline
- selecionar `SUSPEITO` ou `CRITICO` abre confirmação
- sucesso atualiza a linha localmente e depois reconcilia com o socket

## Tasks

### Task 1: Criar app shell autenticado

- adicionar layout e componentes em `apps/web/src/app`
- mover `/` para um dashboard real
- manter `/login` isolado no grupo auth

### Task 2: Criar clientes REST e Socket.IO

- criar camada de fetch para `/api/eventos/feed` e `/api/cameras/status`
- criar provider simples de Socket.IO no web
- assinar `feed:evento-novo`, `feed:placa-classificada` e `feed:camera-status`

### Task 3: Criar componentes do feed

- `EventFeed`
- `EventRow`
- `ClassificationBadge`
- `ClassificationPopover`
- mapear cor por enum: verde → vermelho

### Task 4: Implementar auto-scroll controlado

- manter feed fixado no topo quando o operador está no topo
- ao detectar scroll manual para baixo, pausar auto-scroll
- exibir affordance para voltar aos novos eventos

### Task 5: Confirmar níveis críticos

- abrir confirmação antes de enviar `SUSPEITO` ou `CRITICO`
- manter mensagem explícita de impacto operacional

### Task 6: Cobrir com testes

- teste de render do dashboard autenticado
- teste de atualização do feed ao receber evento socket
- teste do popover inline
- teste da confirmação obrigatória para níveis 4 e 5

## Verification

- `pnpm --filter @cargo-sentinel/web test -- --run`
- `pnpm --filter @cargo-sentinel/web build`
- validação manual em duas janelas autenticadas

## Acceptance Criteria

- o operador vê eventos novos no topo sem reload
- linhas ficam coloridas conforme classificação
- o feed não "puxa" a tela quando o operador está navegando histórico recente
- classificação pode ser alterada sem sair da página
- níveis 4 e 5 exigem confirmação explícita

## Done

O produto passa a ter um painel operacional utilizável no dia a dia, conectado ao backend em tempo real e capaz de reclassificar veículos diretamente do feed.
