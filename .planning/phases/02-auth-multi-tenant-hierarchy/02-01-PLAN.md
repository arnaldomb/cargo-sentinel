---
phase: 02-auth-multi-tenant-hierarchy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - packages/database/src/seed.ts
  - packages/database/package.json
autonomous: true
requirements: [AUTH-03, TENANT-01, TENANT-02, TENANT-03, TENANT-04]
must_haves:
  truths:
    - "A tabela User existe no Postgres com colunas email, passwordHash, nome, role, empresaId"
    - "O enum Role com SUPER_ADMIN, ADMIN_EMPRESA, OPERADOR existe no banco"
    - "Existe um usuário SUPER_ADMIN com empresaId NULL após rodar o seed"
    - "Existe uma Empresa de demonstração com um usuário ADMIN_EMPRESA e um OPERADOR"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "Modelo User + enum Role + relação Empresa.users"
      contains: "model User"
    - path: "packages/database/src/seed.ts"
      provides: "Seed de Super Admin + empresa demo com admin e operador"
      min_lines: 30
  key_links:
    - from: "packages/database/prisma/schema.prisma User.empresaId"
      to: "Empresa.id"
      via: "relation opcional (String?)"
      pattern: "empresa\\s+Empresa\\?\\s+@relation"
---

<objective>
Adicionar o modelo de autenticação ao schema Prisma (modelo `User` + enum `Role`), aplicar ao Postgres via `prisma db push`, e popular o banco com um Super Admin e uma empresa de demonstração (admin + operador) para destravar o restante da Phase 2.

Purpose: Toda a autenticação (Plan 02) e o middleware de tenant (Plan 03) dependem da existência da tabela `User` com `passwordHash` e `role`. O seed garante credenciais utilizáveis para verificação manual de login e isolamento multi-tenant.
Output: schema.prisma atualizado, banco migrado, seed.ts executável que cria 1 SUPER_ADMIN + 1 empresa demo com 1 ADMIN_EMPRESA + 1 OPERADOR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-multi-tenant-hierarchy/02-RESEARCH.md
@packages/database/prisma/schema.prisma
@packages/database/src/index.ts

<interfaces>
<!-- Estado atual do schema (Phase 1). NÃO recriar estes modelos — apenas adicionar User/Role e a relação users em Empresa. -->

Enum existente em schema.prisma:
```prisma
enum EmpresaStatus { ATIVO  SUSPENSO }
```

Modelo Empresa existente (precisa receber o campo `users User[]`):
```prisma
model Empresa {
  id        String        @id @default(cuid())
  nome      String
  cnpj      String        @unique
  status    EmpresaStatus @default(ATIVO)
  createdAt DateTime      @default(now())
  obras     Obra[]
  cameras   Camera[]
  eventos   Evento[]
}
```

Prisma client export (packages/database/src/index.ts):
```typescript
export const prisma; // PrismaClient com @prisma/adapter-pg (Pool de process.env.DATABASE_URL)
export { createTenantClient } from './tenant'; // (prisma, empresaId: string)
```

DECISÃO LOCKED (D2): NÃO criar tabela RefreshToken. A estratégia de refresh é JWT com maxAge 7 dias + updateAge 5 min (configurado no Plan 02). Ignorar o Pattern 1 do RESEARCH que sugere RefreshToken — ele foi escrito antes da decisão do usuário.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar enum Role + modelo User ao schema Prisma</name>
  <read_first>
    - packages/database/prisma/schema.prisma (estado atual — ver os enums e o modelo Empresa existentes)
    - .planning/phases/02-auth-multi-tenant-hierarchy/02-RESEARCH.md (Pattern 1, mas SEM RefreshToken — ver nota LOCKED D2)
  </read_first>
  <action>
Editar `packages/database/prisma/schema.prisma`:

1. Adicionar o enum `Role` (após o enum `EmpresaStatus`):
```prisma
enum Role {
  SUPER_ADMIN
  ADMIN_EMPRESA
  OPERADOR
}
```

2. Adicionar o modelo `User` (após o enum `Role`):
```prisma
model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  nome         String
  role         Role     @default(OPERADOR)
  empresaId    String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  empresa      Empresa? @relation(fields: [empresaId], references: [id])

  @@index([empresaId])
  @@index([email])
}
```

3. Adicionar ao modelo `Empresa` existente a linha de relação (junto às outras relações `obras`, `cameras`, `eventos`):
```prisma
  users     User[]
```

NÃO criar `RefreshToken` (decisão LOCKED D2 — refresh é via JWT maxAge 7d/updateAge 5min, configurado no Plan 02).
NÃO alterar `datasource db` nem adicionar `url` (Prisma v7 usa adapter via prisma.config.ts — ver index.ts).
NÃO alterar Obra, Camera ou Evento — TENANT-02/03/04 já estão satisfeitos pelo schema da Phase 1 (verificar que `empresaId` denormalizado existe em Camera e Evento).
  </action>
  <verify>
    <automated>cd packages/database && npx prisma validate --schema prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "enum Role" packages/database/prisma/schema.prisma` retorna 0
    - `grep -q "SUPER_ADMIN" packages/database/prisma/schema.prisma` retorna 0
    - `grep -q "ADMIN_EMPRESA" packages/database/prisma/schema.prisma` retorna 0
    - `grep -q "model User" packages/database/prisma/schema.prisma` retorna 0
    - `grep -q "passwordHash String" packages/database/prisma/schema.prisma` retorna 0
    - `grep -q "empresa      Empresa? @relation" packages/database/prisma/schema.prisma` retorna 0
    - `grep -q "users     User\[\]" packages/database/prisma/schema.prisma` retorna 0
    - `grep -c "model RefreshToken" packages/database/prisma/schema.prisma` retorna 0 (NÃO deve existir)
    - `npx prisma validate` imprime "is valid"
  </acceptance_criteria>
  <done>schema.prisma contém enum Role (3 valores), modelo User com passwordHash/role/empresaId opcional, relação Empresa.users; prisma validate passa; nenhum modelo RefreshToken presente.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar schema ao Postgres via prisma db push</name>
  <files>packages/database/prisma/schema.prisma, packages/database/node_modules/.prisma/client/</files>
  <read_first>
    - packages/database/prisma/schema.prisma (após Task 1)
    - packages/database/src/index.ts (confirmar que DATABASE_URL é a fonte de conexão)
  </read_first>
  <action>
Esta tarefa é BLOQUEANTE — deve rodar APÓS Task 1 e ANTES da verificação da fase.

Aplicar o schema ao Postgres. Prisma v7 usa `prisma.config.ts` para o CLI e NÃO lê `url` do schema — `DATABASE_URL` deve ser passada como variável de ambiente.

Comando (non-TTY, a partir do diretório do projeto):
```bash
cd packages/database && DATABASE_URL=postgresql://sentinel:sentinel@localhost:5432/cargo_sentinel npx prisma db push --accept-data-loss
```

Se o Postgres rodar apenas em Docker, usar:
```bash
docker exec cargo-sentinel-api-1 sh -c "cd /app/packages/database && DATABASE_URL=postgresql://sentinel:sentinel@postgres:5432/cargo_sentinel npx prisma db push --accept-data-loss"
```

Depois do push, regenerar o Prisma Client para que `Role` e `User` fiquem disponíveis no TypeScript:
```bash
cd packages/database && DATABASE_URL=postgresql://sentinel:sentinel@localhost:5432/cargo_sentinel npx prisma generate
```
  </action>
  <verify>
    <automated>cd packages/database && DATABASE_URL=postgresql://sentinel:sentinel@localhost:5432/cargo_sentinel npx prisma db push --accept-data-loss</automated>
  </verify>
  <acceptance_criteria>
    - O comando `prisma db push` termina com "Your database is now in sync with your Prisma schema" ou "already in sync"
    - `prisma generate` termina com "Generated Prisma Client"
    - Após generate, `grep -rq "SUPER_ADMIN" packages/database/node_modules/.prisma/client/index.d.ts` retorna 0 (enum Role gerado no client)
  </acceptance_criteria>
  <done>Tabela User criada no Postgres, enum Role aplicado, Prisma Client regenerado com tipos User e Role disponíveis para import.</done>
</task>

<task type="auto">
  <name>Task 3: Seed de Super Admin + empresa demo (admin + operador)</name>
  <read_first>
    - packages/database/src/index.ts (import de prisma)
    - packages/database/package.json (scripts existentes)
  </read_first>
  <action>
Criar `packages/database/src/seed.ts` que popula credenciais de teste com senhas hasheadas via bcryptjs (cost 12).

1. Instalar bcryptjs no workspace database:
```bash
pnpm --filter @cargo-sentinel/database add bcryptjs && pnpm --filter @cargo-sentinel/database add -D @types/bcryptjs tsx
```

2. Criar `packages/database/src/seed.ts`:
```typescript
import bcryptjs from 'bcryptjs';
import { prisma } from './index';

async function main() {
  const hash = (pwd: string) => bcryptjs.hashSync(pwd, 12);

  // SUPER_ADMIN — empresaId NULL explícito (AUTH-02)
  await prisma.user.upsert({
    where: { email: 'superadmin@cargosentinel.com' },
    update: {},
    create: {
      email: 'superadmin@cargosentinel.com',
      passwordHash: hash('SuperAdmin123!'),
      nome: 'Super Admin',
      role: 'SUPER_ADMIN',
      empresaId: null,
    },
  });

  // Empresa demo (tenant)
  const empresa = await prisma.empresa.upsert({
    where: { cnpj: '00000000000191' },
    update: {},
    create: { nome: 'Construtora Demo', cnpj: '00000000000191', status: 'ATIVO' },
  });

  // ADMIN_EMPRESA do tenant demo
  await prisma.user.upsert({
    where: { email: 'admin@demo.com' },
    update: {},
    create: {
      email: 'admin@demo.com',
      passwordHash: hash('Admin123!'),
      nome: 'Admin Demo',
      role: 'ADMIN_EMPRESA',
      empresaId: empresa.id,
    },
  });

  // OPERADOR do tenant demo
  await prisma.user.upsert({
    where: { email: 'operador@demo.com' },
    update: {},
    create: {
      email: 'operador@demo.com',
      passwordHash: hash('Operador123!'),
      nome: 'Operador Demo',
      role: 'OPERADOR',
      empresaId: empresa.id,
    },
  });

  console.log('Seed concluído: superadmin@cargosentinel.com, admin@demo.com, operador@demo.com');
}

main()
  .then(() => process.exit(0))
  .catch((e) => { console.error(e); process.exit(1); });
```

3. Adicionar script ao `packages/database/package.json` em "scripts":
```json
"seed": "tsx src/seed.ts"
```

4. Executar o seed:
```bash
cd packages/database && DATABASE_URL=postgresql://sentinel:sentinel@localhost:5432/cargo_sentinel pnpm seed
```
  </action>
  <verify>
    <automated>cd packages/database && DATABASE_URL=postgresql://sentinel:sentinel@localhost:5432/cargo_sentinel pnpm seed</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "role: 'SUPER_ADMIN'" packages/database/src/seed.ts` retorna 0
    - `grep -q "empresaId: null" packages/database/src/seed.ts` retorna 0
    - `grep -q "bcryptjs.hashSync" packages/database/src/seed.ts` retorna 0
    - `grep -q '"seed": "tsx src/seed.ts"' packages/database/package.json` retorna 0
    - A execução do seed imprime "Seed concluído: superadmin@cargosentinel.com, admin@demo.com, operador@demo.com"
    - `grep -q "bcryptjs" packages/database/package.json` retorna 0
  </acceptance_criteria>
  <done>seed.ts cria 1 SUPER_ADMIN (empresaId null), 1 empresa demo, 1 ADMIN_EMPRESA e 1 OPERADOR com senhas bcrypt cost 12; script `pnpm seed` funcional; execução bem-sucedida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed → banco | Senhas em texto plano no seed só existem em ambiente de dev; produção troca via Super Admin panel (Phase 7) |
| User.passwordHash → respostas API | passwordHash nunca pode vazar para o cliente (mitigado nos Plans 02/04 via select explícito) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | User.passwordHash | mitigate | bcryptjs cost 12 no seed; nunca usar select-all em rotas (enforced Plan 02/04) |
| T-02-02 | Tampering | User.role / empresaId | mitigate | role e empresaId só definidos server-side (seed/Super Admin panel); cliente nunca escreve role |
| T-02-03 | Spoofing | Credenciais de seed | accept | senhas demo conhecidas só em dev; produção provisiona via Phase 7 (rationale: ambiente de desenvolvimento, sem PII real) |
</threat_model>

<verification>
- `npx prisma validate` passa
- `prisma db push` deixa o banco em sync
- `pnpm seed` cria os 4 usuários sem erro
- Query manual: `SELECT email, role, "empresaId" FROM "User"` retorna 4 linhas com o SUPER_ADMIN tendo empresaId NULL
</verification>

<success_criteria>
- Modelo User + enum Role aplicados ao Postgres (AUTH-03)
- Empresa.users relation presente (TENANT-01)
- Obra/Camera/Evento com empresaId denormalizado confirmados (TENANT-02/03/04)
- Seed executável cria SUPER_ADMIN (empresaId null) + tenant demo com admin e operador
- Nenhuma tabela RefreshToken criada (decisão D2)
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-multi-tenant-hierarchy/02-01-SUMMARY.md`
</output>
