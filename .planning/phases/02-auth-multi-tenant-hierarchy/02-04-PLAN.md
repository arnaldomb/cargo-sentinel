---
phase: 02-auth-multi-tenant-hierarchy
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - apps/api/src/middleware/pipeline.ts
  - apps/api/src/routes/obras.ts
  - apps/api/src/routes/cameras.ts
  - apps/api/src/routes/obras.test.ts
  - apps/api/src/routes/cameras.test.ts
  - apps/api/src/index.ts
autonomous: true
requirements: [TENANT-05, TENANT-06]
must_haves:
  truths:
    - "ADMIN_EMPRESA pode criar, editar e desativar Obras da sua empresa via API"
    - "ADMIN_EMPRESA pode adicionar, editar e remover Câmeras de uma Obra da sua empresa via API"
    - "OPERADOR pode listar Obras e Câmeras da sua empresa, mas não criar, editar ou remover"
    - "Nenhuma rota retorna Obras ou Câmeras de outra empresa — isolamento de tenant garantido por req.tenantClient"
    - "obraId e cameraId de params são sempre verificados contra o tenant antes de qualquer mutação"
  artifacts:
    - path: "apps/api/src/middleware/pipeline.ts"
      provides: "protectedPipeline [authMiddleware, tenantMiddleware] — módulo neutro sem dependências circulares"
      contains: "protectedPipeline"
      min_lines: 8
    - path: "apps/api/src/routes/obras.ts"
      provides: "CRUD de Obra scoped ao tenant via req.tenantClient"
      contains: "protectedPipeline"
      min_lines: 80
    - path: "apps/api/src/routes/cameras.ts"
      provides: "CRUD de Camera scoped à obra e ao tenant via req.tenantClient"
      contains: "protectedPipeline"
      min_lines: 80
    - path: "apps/api/src/routes/obras.test.ts"
      provides: "Testes de isolamento de tenant + RBAC para obras"
      contains: "tenantClient"
    - path: "apps/api/src/routes/cameras.test.ts"
      provides: "Testes de isolamento de tenant + RBAC para cameras"
      contains: "tenantClient"
  key_links:
    - from: "apps/api/src/routes/obras.ts"
      to: "req.tenantClient.obra"
      via: "tenantMiddleware (Plan 03)"
      pattern: "req\\.tenantClient\\.obra\\."
    - from: "apps/api/src/routes/cameras.ts"
      to: "req.tenantClient.camera"
      via: "tenantMiddleware (Plan 03)"
      pattern: "req\\.tenantClient\\.camera\\."
    - from: "apps/api/src/index.ts"
      to: "obrasRouter + camerasRouter"
      via: "app.use('/api/obras', ...protectedPipeline, obrasRouter)"
      pattern: "app\\.use\\('/api/obras'"
---

<objective>
Implementar o CRUD de Obras e Câmeras no Express (TENANT-05 + TENANT-06): rotas protegidas por `protectedPipeline` (extraído para `middleware/pipeline.ts` — sem dependência circular) com RBAC por role e isolamento de tenant garantido exclusivamente via `req.tenantClient` (nunca trust em params crus).

Purpose: Fecha o contrato de hierarquia multi-tenant da Phase 2 — Admin Empresa pode gerenciar Obras e Câmeras da sua empresa, Operador pode apenas consultar, e nenhum tenant vê dados de outro.
Output: `pipeline.ts` (módulo neutro), `obras.ts`, `cameras.ts`, seus testes, e registro no `index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth-multi-tenant-hierarchy/02-RESEARCH.md
@apps/api/src/index.ts
@apps/api/src/routes/lpr.ts
@packages/database/prisma/schema.prisma

<interfaces>
<!-- Contratos que este plano consome — extraídos dos planos anteriores. -->

De apps/api/src/middleware/auth.ts (Plan 03):
```typescript
export async function authMiddleware(req: Request, res: Response, next: NextFunction): Promise<void>
// descriptografa JWE, popula req.user, retorna 401 sem token
```

De apps/api/src/middleware/tenant.ts (Plan 03):
```typescript
export function tenantMiddleware(req: Request, res: Response, next: NextFunction): void
// injeta req.tenantClient; SUPER_ADMIN recebe prisma raw; usuário normal recebe createTenantClient(prisma, empresaId)
```

De apps/api/src/middleware/rbac.ts (Plan 03):
```typescript
export function requireRole(...roles: 'SUPER_ADMIN' | 'ADMIN_EMPRESA' | 'OPERADOR'): RequestHandler
// retorna 403 se req.user.role não está na lista
```

De apps/api/src/types/express.d.ts (Plan 03):
```typescript
interface Request {
  user?: { id: string; role: string; empresaId: string | null };
  tenantClient?: ReturnType<typeof createTenantClient>;
}
```

Schema Prisma relevante (packages/database/prisma/schema.prisma):
```prisma
model Obra {
  id        String   @id @default(cuid())
  nome      String
  endereco  String?
  ativo     Boolean  @default(true)   // campo de soft-delete (a adicionar — ver ação abaixo)
  empresaId String
  createdAt DateTime @default(now())
  empresa   Empresa  @relation(fields: [empresaId], references: [id])
  cameras   Camera[]
}

model Camera {
  id        String  @id @default(cuid())
  codigoLpr String  @unique
  ip        String?
  obraId    String
  empresaId String
  ativo     Boolean @default(true)   // campo de soft-delete (a adicionar — ver ação abaixo)
  createdAt DateTime @default(now())
}
```

ATENÇÃO: O schema atual (Phase 1) NÃO tem campo `ativo` em Obra nem Camera.
A Task 1 deste plano deve adicionar `ativo Boolean @default(true)` em ambas as entidades
e rodar `prisma db push --schema packages/database/prisma/schema.prisma`.
O campo não quebra a Phase 1 (nullable-equivalent via default true, sem migration destrutiva).

Padrão de rota (apps/api/src/routes/lpr.ts):
```typescript
import { Router, type Router as RouterType } from 'express';
const router: RouterType = Router();
// ... router.post/get/put/delete
export default router;
```

createTenantClient injeta WHERE empresaId automaticamente em TODA query do tenantClient.
Portanto: `req.tenantClient.obra.findFirstOrThrow({ where: { id: obraId } })` já garante
que a obra pertence ao tenant — NÃO é necessário re-checar empresaId explicitamente.
Se o registro não pertencer ao tenant, Prisma lança P2025 (Not Found) → capturar e retornar 404.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar pipeline.ts + campo `ativo` no schema + router de Obras com CRUD completo</name>
  <files>apps/api/src/middleware/pipeline.ts, packages/database/prisma/schema.prisma, apps/api/src/routes/obras.ts</files>
  <read_first>
    - apps/api/src/middleware/auth.ts (assinatura de authMiddleware — Plan 03)
    - apps/api/src/middleware/tenant.ts (assinatura de tenantMiddleware — Plan 03)
    - apps/api/src/middleware/rbac.ts (assinatura de requireRole — Plan 03)
    - packages/database/prisma/schema.prisma (estado atual — confirmar que ativo não existe)
    - apps/api/src/routes/lpr.ts (padrão de router: imports, Router(), export default)
    - apps/api/src/index.ts (pós Plan 03 — confirmar que NÃO exporta mais protectedPipeline; só importa do pipeline.ts)
  </read_first>
  <action>
1. Criar `apps/api/src/middleware/pipeline.ts` — módulo dedicado sem dependências circulares:

```typescript
import { authMiddleware } from './auth';
import { tenantMiddleware } from './tenant';

/**
 * Pipeline de proteção reutilizável.
 * Ordem obrigatória: auth → tenant (Pitfall 5 do RESEARCH.md).
 * Importar DESTE módulo — não de '../index' — para evitar dependência circular.
 */
export const protectedPipeline = [authMiddleware, tenantMiddleware] as const;
```

IMPORTANTE: obras.ts, cameras.ts e qualquer rota futura devem importar protectedPipeline
de `'../middleware/pipeline'`, NUNCA de `'../index'`. O index.ts importa os routers, então
um router importando de index criaria ciclo index→obras→index.

2. Editar `packages/database/prisma/schema.prisma`: adicionar `ativo Boolean @default(true)` em `Obra` (após `empresaId`) e em `Camera` (após `empresaId`). Rodar:
```bash
pnpm --filter @cargo-sentinel/database exec prisma db push
pnpm --filter @cargo-sentinel/database exec prisma generate
```
Verificar saída: sem erros, Prisma client regenerado.

3. Criar `apps/api/src/routes/obras.ts` (importa protectedPipeline de '../middleware/pipeline', NÃO de '../index'):

```typescript
import { Router, type Router as RouterType } from 'express';
import { requireRole } from '../middleware/rbac';

const router: RouterType = Router();

// Todas as rotas já passam por protectedPipeline (montado no index.ts via spread)
// req.tenantClient injeta WHERE empresaId automaticamente em toda query

/**
 * GET /api/obras
 * Lista obras ativas da empresa do usuário autenticado.
 * TENANT-06: OPERADOR+ pode listar.
 */
router.get(
  '/',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR'),
  async (req, res) => {
    const obras = await req.tenantClient!.obra.findMany({
      where: { ativo: true },
      orderBy: { createdAt: 'desc' },
      select: { id: true, nome: true, endereco: true, ativo: true, createdAt: true },
    });
    res.json(obras);
  },
);

/**
 * POST /api/obras
 * Cria nova obra para a empresa. Injeta empresaId do token (nunca do body).
 * TENANT-05: ADMIN_EMPRESA+ pode criar.
 */
router.post(
  '/',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA'),
  async (req, res) => {
    const { nome, endereco } = req.body as { nome?: string; endereco?: string };
    if (!nome || typeof nome !== 'string' || nome.trim().length < 2) {
      return res.status(400).json({ error: 'Campo nome é obrigatório (mínimo 2 caracteres)' });
    }
    // empresaId vem do req.tenantClient (injetado pelo token) — nunca do body
    // Para SUPER_ADMIN, tenantClient é prisma raw (sem filtro) — empresaId deve ser explícito
    const empresaId = req.user!.empresaId;
    if (!empresaId) {
      return res.status(400).json({ error: 'SUPER_ADMIN deve informar empresaId ao criar obra' });
    }
    const obra = await req.tenantClient!.obra.create({
      data: { nome: nome.trim(), endereco: endereco?.trim(), empresaId },
    });
    res.status(201).json(obra);
  },
);

/**
 * PUT /api/obras/:id
 * Atualiza nome ou endereço de uma obra. Verifica pertencimento ao tenant via findFirstOrThrow.
 * TENANT-05: ADMIN_EMPRESA+ pode editar.
 */
router.put(
  '/:id',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA'),
  async (req, res) => {
    const { id } = req.params;
    const { nome, endereco } = req.body as { nome?: string; endereco?: string };
    if (!nome || typeof nome !== 'string' || nome.trim().length < 2) {
      return res.status(400).json({ error: 'Campo nome é obrigatório (mínimo 2 caracteres)' });
    }
    try {
      // findFirstOrThrow verifica pertencimento ao tenant (tenantClient injeta WHERE empresaId)
      await req.tenantClient!.obra.findFirstOrThrow({ where: { id } });
      const updated = await req.tenantClient!.obra.update({
        where: { id },
        data: { nome: nome.trim(), endereco: endereco?.trim() },
      });
      res.json(updated);
    } catch (err: unknown) {
      const code = (err as { code?: string })?.code;
      if (code === 'P2025') return res.status(404).json({ error: 'Obra não encontrada' });
      throw err;
    }
  },
);

/**
 * DELETE /api/obras/:id
 * Soft-delete: marca ativo=false. Nunca apaga fisicamente.
 * TENANT-05: ADMIN_EMPRESA+ pode desativar.
 */
router.delete(
  '/:id',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA'),
  async (req, res) => {
    const { id } = req.params;
    try {
      await req.tenantClient!.obra.findFirstOrThrow({ where: { id } });
      await req.tenantClient!.obra.update({ where: { id }, data: { ativo: false } });
      res.status(204).send();
    } catch (err: unknown) {
      const code = (err as { code?: string })?.code;
      if (code === 'P2025') return res.status(404).json({ error: 'Obra não encontrada' });
      throw err;
    }
  },
);

export default router;
```
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | grep -i "error TS" | head -5; grep -q "protectedPipeline" src/middleware/pipeline.ts && grep -q "requireRole" src/routes/obras.ts && grep -q "findFirstOrThrow" src/routes/obras.ts && echo "OBRAS_ROUTER_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "protectedPipeline" apps/api/src/middleware/pipeline.ts` retorna 0 (módulo dedicado existe)
    - `grep -cq "\.\./index" apps/api/src/routes/obras.ts` retorna 0 (NÃO importa de index — sem ciclo)
    - `grep -q "ativo Boolean" packages/database/prisma/schema.prisma` retorna 0 (campo adicionado em Obra e Camera)
    - `grep -q "requireRole" apps/api/src/routes/obras.ts` retorna 0
    - `grep -q "req.tenantClient!.obra.findFirstOrThrow" apps/api/src/routes/obras.ts` retorna 0 (verificação de pertencimento)
    - `grep -q "empresaId" apps/api/src/routes/obras.ts` retorna 0 mas NÃO deve conter `req.body.empresaId` (empresaId nunca vem do body)
    - `grep -q "P2025" apps/api/src/routes/obras.ts` retorna 0 (tratamento de not-found)
    - `grep -q "ativo: false" apps/api/src/routes/obras.ts` retorna 0 (soft-delete)
    - `npx tsc --noEmit` sem erros no workspace api
  </acceptance_criteria>
  <done>pipeline.ts criado com protectedPipeline [authMiddleware, tenantMiddleware] sem dependência circular; schema atualizado com campo ativo em Obra e Camera; prisma db push executado; obras.ts com GET/POST/PUT/DELETE implementados com isolamento de tenant via tenantClient e soft-delete no DELETE; TypeScript sem erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Router de Câmeras + testes de obras e câmeras + registro no index.ts</name>
  <files>apps/api/src/routes/cameras.ts, apps/api/src/routes/obras.test.ts, apps/api/src/routes/cameras.test.ts, apps/api/src/index.ts</files>
  <read_first>
    - apps/api/src/middleware/pipeline.ts (Task 1 — protectedPipeline para importar nos testes via mock)
    - apps/api/src/routes/obras.ts (Task 1 — padrão a seguir)
    - apps/api/src/index.ts (onde registrar os novos routers)
    - apps/api/src/middleware/auth.test.ts (padrão de mock: vi.mock, req/res fake, next spy)
    - packages/database/prisma/schema.prisma (Task 1 — schema atualizado com ativo)
  </read_first>
  <behavior>
    Testes de obras (obras.test.ts):
    - Test 1: GET /api/obras com OPERADOR retorna 200 e chama tenantClient.obra.findMany
    - Test 2: POST /api/obras com OPERADOR retorna 403 (requireRole rejeita)
    - Test 3: POST /api/obras com ADMIN_EMPRESA e body { nome: 'Obra X' } retorna 201 e chama tenantClient.obra.create com empresaId do req.user
    - Test 4: POST /api/obras com body vazio retorna 400 (validação de nome)
    - Test 5: PUT /api/obras/:id com ADMIN_EMPRESA quando findFirstOrThrow lança P2025 retorna 404
    - Test 6: DELETE /api/obras/:id com ADMIN_EMPRESA chama update com { ativo: false } e retorna 204

    Testes de cameras (cameras.test.ts):
    - Test 7: GET /api/obras/:obraId/cameras com OPERADOR retorna 200 e chama tenantClient.camera.findMany com where.obraId
    - Test 8: POST /api/obras/:obraId/cameras com OPERADOR retorna 403
    - Test 9: POST /api/obras/:obraId/cameras com ADMIN_EMPRESA e body { codigoLpr: 'LPR-0001', ip: '192.168.1.10' } retorna 201 e chama tenantClient.camera.create com obraId e empresaId do token
    - Test 10: POST /api/obras/:obraId/cameras com codigoLpr duplicado (erro P2002 Prisma) retorna 409
    - Test 11: DELETE /api/obras/:obraId/cameras/:id com ADMIN_EMPRESA chama update com { ativo: false } e retorna 204
  </behavior>
  <action>
1. Criar `apps/api/src/routes/cameras.ts` (importa requireRole de '../middleware/rbac'; NÃO importa protectedPipeline — ele é aplicado pelo index.ts via spread):

```typescript
import { Router, type Router as RouterType } from 'express';
import { requireRole } from '../middleware/rbac';

const router: RouterType = Router({ mergeParams: true }); // herda :obraId do parent router

/**
 * GET /api/obras/:obraId/cameras
 * Lista câmeras ativas da obra, verificando pertencimento ao tenant via obra.
 * TENANT-06: OPERADOR+ pode listar.
 */
router.get(
  '/',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR'),
  async (req, res) => {
    const { obraId } = req.params;
    try {
      // Verifica que a obra pertence ao tenant (tenantClient injeta WHERE empresaId)
      await req.tenantClient!.obra.findFirstOrThrow({ where: { id: obraId } });
    } catch (err: unknown) {
      const code = (err as { code?: string })?.code;
      if (code === 'P2025') return res.status(404).json({ error: 'Obra não encontrada' });
      throw err;
    }
    const cameras = await req.tenantClient!.camera.findMany({
      where: { obraId, ativo: true },
      orderBy: { createdAt: 'desc' },
      select: { id: true, codigoLpr: true, ip: true, ativo: true, createdAt: true },
    });
    res.json(cameras);
  },
);

/**
 * POST /api/obras/:obraId/cameras
 * Cria câmera e associa à obra. empresaId denormalizado a partir do req.user (nunca do body).
 * TENANT-05: ADMIN_EMPRESA+ pode criar.
 */
router.post(
  '/',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA'),
  async (req, res) => {
    const { obraId } = req.params;
    const { codigoLpr, ip } = req.body as { codigoLpr?: string; ip?: string };
    if (!codigoLpr || typeof codigoLpr !== 'string' || codigoLpr.trim().length < 3) {
      return res.status(400).json({ error: 'Campo codigoLpr é obrigatório (mínimo 3 caracteres)' });
    }
    const empresaId = req.user!.empresaId;
    if (!empresaId) {
      return res.status(400).json({ error: 'SUPER_ADMIN deve informar empresaId ao criar câmera' });
    }
    try {
      // Verifica pertencimento da obra ao tenant antes de criar câmera
      await req.tenantClient!.obra.findFirstOrThrow({ where: { id: obraId } });
      const camera = await req.tenantClient!.camera.create({
        data: {
          codigoLpr: codigoLpr.trim(),
          ip: ip?.trim(),
          obraId,
          empresaId, // denormalizado (TENANT-04)
        },
      });
      res.status(201).json(camera);
    } catch (err: unknown) {
      const code = (err as { code?: string })?.code;
      if (code === 'P2025') return res.status(404).json({ error: 'Obra não encontrada' });
      if (code === 'P2002') return res.status(409).json({ error: 'codigoLpr já existe' });
      throw err;
    }
  },
);

/**
 * PUT /api/obras/:obraId/cameras/:id
 * Atualiza câmera. Verifica pertencimento de obra e câmera ao tenant.
 * TENANT-05: ADMIN_EMPRESA+ pode editar.
 */
router.put(
  '/:id',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA'),
  async (req, res) => {
    const { obraId, id } = req.params;
    const { codigoLpr, ip } = req.body as { codigoLpr?: string; ip?: string };
    if (!codigoLpr || typeof codigoLpr !== 'string' || codigoLpr.trim().length < 3) {
      return res.status(400).json({ error: 'Campo codigoLpr é obrigatório (mínimo 3 caracteres)' });
    }
    try {
      // Verifica obra e câmera no mesmo tenant (tenantClient injeta WHERE empresaId)
      await req.tenantClient!.obra.findFirstOrThrow({ where: { id: obraId } });
      await req.tenantClient!.camera.findFirstOrThrow({ where: { id, obraId } });
      const updated = await req.tenantClient!.camera.update({
        where: { id },
        data: { codigoLpr: codigoLpr.trim(), ip: ip?.trim() },
      });
      res.json(updated);
    } catch (err: unknown) {
      const code = (err as { code?: string })?.code;
      if (code === 'P2025') return res.status(404).json({ error: 'Câmera ou obra não encontrada' });
      if (code === 'P2002') return res.status(409).json({ error: 'codigoLpr já existe' });
      throw err;
    }
  },
);

/**
 * DELETE /api/obras/:obraId/cameras/:id
 * Soft-delete: marca ativo=false. Verifica pertencimento ao tenant.
 * TENANT-05: ADMIN_EMPRESA+ pode desativar.
 */
router.delete(
  '/:id',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA'),
  async (req, res) => {
    const { obraId, id } = req.params;
    try {
      await req.tenantClient!.obra.findFirstOrThrow({ where: { id: obraId } });
      await req.tenantClient!.camera.findFirstOrThrow({ where: { id, obraId } });
      await req.tenantClient!.camera.update({ where: { id }, data: { ativo: false } });
      res.status(204).send();
    } catch (err: unknown) {
      const code = (err as { code?: string })?.code;
      if (code === 'P2025') return res.status(404).json({ error: 'Câmera ou obra não encontrada' });
      throw err;
    }
  },
);

export default router;
```

2. Criar `apps/api/src/routes/obras.test.ts` cobrindo Tests 1-6.
   - Usar `vi.mock` para `../middleware/pipeline` (mockar protectedPipeline como array vazio para isolar lógica de rota)
   - Mockar `../middleware/rbac` retornando next() ou res.status(403) conforme o role simulado
   - Mockar `req.tenantClient` como objeto com métodos `obra.findMany`, `obra.findFirstOrThrow`, `obra.create`, `obra.update` (vi.fn())
   - Mockar `req.user` com os campos necessários
   - Para Test 5: fazer `obra.findFirstOrThrow` lançar `Object.assign(new Error(), { code: 'P2025' })`
   - Para Test 6: assertar que `obra.update` foi chamado com `data: { ativo: false }`

3. Criar `apps/api/src/routes/cameras.test.ts` cobrindo Tests 7-11 com o mesmo padrão de mock.
   - Para Test 10: fazer `camera.create` lançar `Object.assign(new Error(), { code: 'P2002' })`
   - Para Test 9: assertar que `camera.create` foi chamado com `obraId` e `empresaId` de `req.user` (nunca do body)

4. Editar `apps/api/src/index.ts` para importar protectedPipeline de './middleware/pipeline' e registrar os novos routers:
```typescript
import obrasRouter from './routes/obras';
import camerasRouter from './routes/cameras';
import { protectedPipeline } from './middleware/pipeline';

// Após a linha do lprRouter:
// Rotas protegidas de Obras e Câmeras (TENANT-05, TENANT-06)
app.use('/api/obras', ...protectedPipeline, obrasRouter);
// camerasRouter é sub-rota de obras (mergeParams: true para herdar :obraId)
app.use('/api/obras/:obraId/cameras', ...protectedPipeline, camerasRouter);
```

IMPORTANTE: index.ts deve importar protectedPipeline de './middleware/pipeline', não
re-exportá-lo. Se o Plan 03 adicionou `export const protectedPipeline` no index.ts,
remover essa exportação e substituir pelo import de pipeline.ts.

ATENÇÃO: `...protectedPipeline` usa spread no `app.use` — Express aceita múltiplos handlers em array. Verificar que a sintaxe está correta no TypeScript (`RequestHandler[]` spread).
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/routes/obras.test.ts src/routes/cameras.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "mergeParams: true" apps/api/src/routes/cameras.ts` retorna 0 (herança de :obraId)
    - `grep -q "P2002" apps/api/src/routes/cameras.ts` retorna 0 (tratamento de codigoLpr duplicado → 409)
    - `grep -q "req.tenantClient!.obra.findFirstOrThrow" apps/api/src/routes/cameras.ts` retorna 0 (verifica obra antes de criar/editar câmera)
    - `grep -q "empresaId" apps/api/src/routes/cameras.ts` retorna 0 e NÃO contém `req.body.empresaId` (empresaId nunca vem do body — TENANT-04 denormalizado do token)
    - `grep -cq "\.\./index" apps/api/src/routes/cameras.ts` retorna 0 (NÃO importa de index — sem ciclo)
    - `grep -cq "\.\./index" apps/api/src/routes/obras.ts` retorna 0 (NÃO importa de index — sem ciclo)
    - `grep -q "from './middleware/pipeline'" apps/api/src/index.ts` retorna 0 (index importa de pipeline.ts)
    - `grep -q "app.use('/api/obras'" apps/api/src/index.ts` retorna 0
    - `grep -q "app.use('/api/obras/:obraId/cameras'" apps/api/src/index.ts` retorna 0
    - `grep -q "...protectedPipeline" apps/api/src/index.ts` retorna 0 (spread do pipeline)
    - `npx vitest run src/routes/obras.test.ts src/routes/cameras.test.ts` passa com 11+ testes verdes
    - `npx tsc --noEmit` sem erros no workspace api
    - `npx vitest run src/routes/lpr.test.ts` continua passando (Phase 1 não quebrada)
  </acceptance_criteria>
  <done>cameras.ts com GET/POST/PUT/DELETE implementados (mergeParams, verifica obra antes de câmera, empresaId denormalizado do token, P2002→409, P2025→404, soft-delete); 11 testes passando; routers registrados no index.ts com protectedPipeline importado de pipeline.ts (sem ciclo); tsc sem erros; LPR tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → /api/obras | req.user.empresaId vem do token JWE (Plan 03), nunca do body da requisição |
| params (:id, :obraId) → Prisma | IDs de params são não-confiáveis até verificados via tenantClient.findFirstOrThrow |
| req.tenantClient → Prisma | tenantClient injeta WHERE empresaId em toda query — isolamento automático por tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Elevation of Privilege | IDOR em obras/:id | mitigate | findFirstOrThrow via tenantClient verifica pertencimento antes de qualquer mutação; P2025 → 404 (não revela existência) |
| T-02-21 | Elevation of Privilege | IDOR em obras/:obraId/cameras/:id | mitigate | verifica obra E câmera separadamente via tenantClient antes de mutar; dupla barreira de tenant |
| T-02-22 | Tampering | empresaId injetado via body na criação | mitigate | empresaId lido SOMENTE de req.user (token descriptografado); campo empresaId do body é ignorado |
| T-02-23 | Elevation of Privilege | OPERADOR invocando rota de criação/edição | mitigate | requireRole('ADMIN_EMPRESA', 'SUPER_ADMIN') em POST/PUT/DELETE → 403 antes de qualquer acesso a DB |
| T-02-24 | Information Disclosure | erro P2025 revelando existência de registro de outro tenant | mitigate | P2025 retorna 404 genérico sem detalhar a quem o registro pertence |
| T-02-25 | Denial of Service | criação de câmeras com codigoLpr duplicado | mitigate | P2002 capturado → 409 Conflict; codigoLpr tem @@unique no schema |
</threat_model>

<verification>
- `vitest run src/routes/obras.test.ts src/routes/cameras.test.ts` passa (11+ testes verdes)
- `tsc --noEmit` sem erros no workspace api
- Testes da Phase 1 (lpr.test.ts) continuam verdes
- `grep -cq "../index" apps/api/src/routes/obras.ts` retorna 0 (sem ciclo confirmado)
- `grep -cq "../index" apps/api/src/routes/cameras.ts` retorna 0 (sem ciclo confirmado)
- Verificação manual integrada (com Plan 03 ativo):
  1. Obter token ADMIN_EMPRESA via POST /api/auth/login (Plan 02)
  2. POST /api/obras com `{ nome: "Obra Teste" }` → deve retornar 201 com id
  3. GET /api/obras → deve retornar somente obras da empresa do token
  4. POST /api/obras/:obraId/cameras com `{ codigoLpr: "LPR-0001", ip: "192.168.1.10" }` → 201
  5. GET /api/obras/:obraId/cameras → deve retornar câmera criada
  6. DELETE /api/obras/:obraId/cameras/:id → 204; GET confirma ativo=false
  7. Usar token de empresa diferente e tentar GET /api/obras/:id de outra empresa → 404
</verification>

<success_criteria>
- ADMIN_EMPRESA pode criar, editar e desativar Obras via API (TENANT-05)
- ADMIN_EMPRESA pode adicionar, editar e remover Câmeras de uma Obra via API (TENANT-05)
- OPERADOR pode listar Obras e Câmeras, é rejeitado com 403 em operações de escrita (TENANT-06)
- Nenhuma rota retorna dados de outro tenant (isolamento via tenantClient)
- empresaId nunca vem do body — sempre do token (TENANT-04 honrado)
- Soft-delete em vez de DELETE físico (ativo=false)
- 11+ testes cobrindo RBAC, isolamento de tenant e tratamento de erros Prisma
- Sem dependência circular: routers importam de middleware/pipeline.ts, não de index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-multi-tenant-hierarchy/02-04-SUMMARY.md`
</output>
