---
id: "03.5-03"
name: "Component Polish — Badges, Botões e Formulários"
phase: "3.5"
plan: 3
type: execute
wave: 2
autonomous: true
objective: "Corrigir cores dos classification badges para spec UI-04, padronizar botões primários/secundários com tokens ggtech e ajustar página de login para identidade visual consistente"
requirements: [UI-02, UI-04]
depends_on: ["03.5-01"]
files_modified:
  - apps/web/src/lib/dashboard.ts
  - apps/web/src/components/critical-confirm-dialog.tsx
  - apps/web/src/app/(auth)/login/page.tsx

must_haves:
  truths:
    - "Badge LIBERADO usa bg-green-600 (#16a34a) — correto, manter"
    - "Badge VISITANTE usa bg-gray-500 (#6b7280) — era bg-blue-600, corrigir"
    - "Badge ATENCAO usa bg-yellow-600 (#ca8a04) — era bg-yellow-500 (muito claro), corrigir"
    - "Badge SUSPEITO usa bg-orange-600 (#ea580c) — era bg-orange-500 (#f97316), corrigir"
    - "Badge CRITICO usa bg-red-700 (#b91c1c) — correto, manter"
    - "Cor de border-left do event row segue os mesmos hex exatos da spec"
    - "Botão de login usa bg-ggtech-blue (classe Tailwind) em vez de style inline"
    - "Inputs de login usam focus:ring-ggtech-blue em vez de focus:ring-blue-500 genérico"
    - "CriticalConfirmDialog: botão confirmar Suspeito usa bg-orange-600; botão Crítico usa bg-red-700"
    - "pnpm test --filter=@cargo-sentinel/web passa (testes existentes de badge e popover)"
  artifacts:
    - path: "apps/web/src/lib/dashboard.ts"
      provides: "getClassificationTailwindClasses e getClassificationColor com cores UI-04 corretas"
    - path: "apps/web/src/app/(auth)/login/page.tsx"
      provides: "Página de login com classes Tailwind ggtech (sem style inline)"
    - path: "apps/web/src/components/critical-confirm-dialog.tsx"
      provides: "Dialog com cores alinhadas ao novo mapeamento de classificação"
  key_links:
    - from: "apps/web/src/lib/dashboard.ts"
      to: "apps/web/src/components/classification-badge.tsx"
      via: "getClassificationTailwindClasses — badge consome classes retornadas"
    - from: "apps/web/src/lib/dashboard.ts"
      to: "apps/web/src/components/dashboard-client.tsx"
      via: "getClassificationColor — border-left dos event rows"
    - from: "apps/web/src/lib/dashboard.ts"
      to: "apps/web/src/components/classification-popover.tsx"
      via: "getClassificationTailwindClasses — cores dos botões no popover"
---

<objective>
Corrigir o mapeamento de cores de classificação para atender UI-04, padronizar botões e inputs da tela de login com tokens Tailwind ggtech, e ajustar o CriticalConfirmDialog para consistência. Nenhuma lógica de negócio muda — apenas o mapeamento visual.

Purpose: UI-04 especifica cores exatas para cada nível de classificação. Hoje VISITANTE usa blue (inconsistente com spec gray) e ATENCAO/SUSPEITO usam tons ligeiramente errados. UI-02 exige botões padronizados — a página de login ainda usa `style` inline.

Output: dashboard.ts com cores corrigidas, login/page.tsx sem inline styles, dialog com cores alinhadas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.5-ui-design-polish-identidade-visual-ggtech/03.5-01-SUMMARY.md

<interfaces>
<!-- Estado atual das funções que serão modificadas -->

De apps/web/src/lib/dashboard.ts — getClassificationTailwindClasses (ATUAL):
```ts
case 'LIBERADO':  return { bg: 'bg-green-600', text: 'text-white', border: 'border-green-600' };
case 'VISITANTE': return { bg: 'bg-blue-600',  text: 'text-white', border: 'border-blue-600' };  // ← ERRADO: spec=gray
case 'ATENCAO':   return { bg: 'bg-yellow-500', text: 'text-white', border: 'border-yellow-500' }; // ← ERRADO: spec=#ca8a04
case 'SUSPEITO':  return { bg: 'bg-orange-500', text: 'text-white', border: 'border-orange-500' }; // ← ERRADO: spec=#ea580c
case 'CRITICO':   return { bg: 'bg-red-700',   text: 'text-white', border: 'border-red-700' };   // ← CORRETO
```

De apps/web/src/lib/dashboard.ts — getClassificationColor (ATUAL):
```ts
case 'LIBERADO':  return '#16a34a'; // correto
case 'VISITANTE': return '#2563eb'; // ← ERRADO: spec=gray #6b7280
case 'ATENCAO':   return '#eab308'; // ← ERRADO: spec=#ca8a04
case 'SUSPEITO':  return '#f97316'; // ← ERRADO: spec=#ea580c
case 'CRITICO':   return '#b91c1c'; // correto
```

Spec UI-04 (do REQUIREMENTS.md):
- Liberado = #16a34a (green-600 ✓)
- Visitante = #6b7280 (gray-500)
- Atenção   = #ca8a04 (yellow-600)
- Suspeito  = #ea580c (orange-600)
- Crítico   = #b91c1c (red-700 ✓)

De apps/web/src/components/critical-confirm-dialog.tsx:
```tsx
// isCritico usa bg-red-700 (correto)
// Suspeito usa bg-orange-500 — deve ser bg-orange-600 (ea580c = orange-600 no Tailwind v3)
const isCritico = classificacao === 'CRITICO';
<span className={isCritico ? 'bg-red-700' : 'bg-orange-500'} />
<button className={isCritico ? 'bg-red-700' : 'bg-orange-500'} />
```

De apps/web/src/app/(auth)/login/page.tsx:
```tsx
// H1 usa style inline: style={{ color: '#003366' }}
// Button usa style inline: style={{ backgroundColor: '#0056b3' }}
// Inputs usam focus:ring-blue-500 genérico
```

Testes existentes que devem continuar passando:
- apps/web/src/components/classification-badge.test.tsx
- apps/web/src/components/classification-popover.test.tsx
- apps/web/src/components/critical-confirm-dialog.test.tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Corrigir mapeamento de cores em dashboard.ts</name>
  <files>apps/web/src/lib/dashboard.ts, apps/web/src/lib/dashboard.test.ts</files>
  <behavior>
    - getClassificationTailwindClasses('VISITANTE') → { bg: 'bg-gray-500', text: 'text-white', border: 'border-gray-500' }
    - getClassificationTailwindClasses('ATENCAO') → { bg: 'bg-yellow-600', text: 'text-white', border: 'border-yellow-600' }
    - getClassificationTailwindClasses('SUSPEITO') → { bg: 'bg-orange-600', text: 'text-white', border: 'border-orange-600' }
    - getClassificationTailwindClasses('LIBERADO') → { bg: 'bg-green-600', ... } (sem mudança — apenas verificar)
    - getClassificationTailwindClasses('CRITICO') → { bg: 'bg-red-700', ... } (sem mudança — apenas verificar)
    - getClassificationColor('VISITANTE') → '#6b7280'
    - getClassificationColor('ATENCAO') → '#ca8a04'
    - getClassificationColor('SUSPEITO') → '#ea580c'
    - getClassificationColor('LIBERADO') → '#16a34a' (sem mudança)
    - getClassificationColor('CRITICO') → '#b91c1c' (sem mudança)
  </behavior>
  <action>
**Passo RED:** Primeiro, verificar se dashboard.test.ts já tem testes para as cores. Se sim, esses testes devem falhar com os valores atuais errados. Se não tiver testes de cor, adicionar no dashboard.test.ts:

```ts
describe('getClassificationTailwindClasses', () => {
  it('VISITANTE retorna gray-500 conforme spec UI-04', () => {
    expect(getClassificationTailwindClasses('VISITANTE').bg).toBe('bg-gray-500');
  });
  it('ATENCAO retorna yellow-600 conforme spec UI-04', () => {
    expect(getClassificationTailwindClasses('ATENCAO').bg).toBe('bg-yellow-600');
  });
  it('SUSPEITO retorna orange-600 conforme spec UI-04', () => {
    expect(getClassificationTailwindClasses('SUSPEITO').bg).toBe('bg-orange-600');
  });
});

describe('getClassificationColor', () => {
  it('VISITANTE retorna #6b7280 conforme spec UI-04', () => {
    expect(getClassificationColor('VISITANTE')).toBe('#6b7280');
  });
  it('ATENCAO retorna #ca8a04 conforme spec UI-04', () => {
    expect(getClassificationColor('ATENCAO')).toBe('#ca8a04');
  });
  it('SUSPEITO retorna #ea580c conforme spec UI-04', () => {
    expect(getClassificationColor('SUSPEITO')).toBe('#ea580c');
  });
});
```

**Passo GREEN:** Atualizar `getClassificationTailwindClasses` em dashboard.ts:
```ts
case 'VISITANTE': return { bg: 'bg-gray-500',   text: 'text-white', border: 'border-gray-500' };
case 'ATENCAO':   return { bg: 'bg-yellow-600',  text: 'text-white', border: 'border-yellow-600' };
case 'SUSPEITO':  return { bg: 'bg-orange-600',  text: 'text-white', border: 'border-orange-600' };
```

Atualizar `getClassificationColor` em dashboard.ts:
```ts
case 'VISITANTE': return '#6b7280';
case 'ATENCAO':   return '#ca8a04';
case 'SUSPEITO':  return '#ea580c';
```

Manter LIBERADO e CRITICO inalterados.

Nota sobre yellow-600: No Tailwind v3, `yellow-600` = `#ca8a04` (exatamente o hex da spec). Confirmar rodando `pnpm test` após a mudança.
  </action>
  <verify>
    <automated>cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel && pnpm test --filter=@cargo-sentinel/web 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm test passa; getClassificationColor('VISITANTE')='#6b7280', ('ATENCAO')='#ca8a04', ('SUSPEITO')='#ea580c'; getClassificationTailwindClasses mapeado corretamente.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Atualizar CriticalConfirmDialog para orange-600</name>
  <files>apps/web/src/components/critical-confirm-dialog.tsx</files>
  <action>
Substituir as ocorrências de `bg-orange-500` por `bg-orange-600` no CriticalConfirmDialog para alinhar com o novo mapeamento de SUSPEITO.

Localizar todas as strings `bg-orange-500` no arquivo e substituir por `bg-orange-600`. Há duas ocorrências:
1. O ícone `!` circular: `isCritico ? 'bg-red-700' : 'bg-orange-500'` → `isCritico ? 'bg-red-700' : 'bg-orange-600'`
2. O botão confirmar: `isCritico ? 'bg-red-700' : 'bg-orange-500'` → `isCritico ? 'bg-red-700' : 'bg-orange-600'`

Também substituir `border-orange-200 bg-orange-50 text-orange-800` pelo bloco de warning:
- Manter `border-orange-200 bg-orange-50 text-orange-800` — esse é o background do aviso informativo, não o botão. Manter como está (tom mais claro intencional para fundo de alerta).

Verificar depois com `pnpm test --filter=@cargo-sentinel/web` — o teste `critical-confirm-dialog.test.tsx` deve continuar passando (ele não testa classes CSS, apenas comportamento).
  </action>
  <verify>
    <automated>cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel && grep -n "bg-orange-500" apps/web/src/components/critical-confirm-dialog.tsx</automated>
  </verify>
  <done>Nenhuma ocorrência de bg-orange-500 em critical-confirm-dialog.tsx; substituídas por bg-orange-600; testes passam.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Padronizar página de login com tokens Tailwind</name>
  <files>apps/web/src/app/(auth)/login/page.tsx</files>
  <action>
Substituir todos os `style` inline da página de login por classes Tailwind com tokens ggtech.

**Mudanças:**

1. H1 `style={{ color: '#003366' }}` → remover o style, adicionar classe `text-ggtech-darkblue`
2. Botão submit `style={{ backgroundColor: '#0056b3' }}` → remover o style, adicionar classe `bg-ggtech-blue hover:bg-ggtech-blue/90`
3. Inputs `focus:ring-blue-500` → `focus:ring-ggtech-blue focus:border-ggtech-blue`

Resultado final do formulário de login:
```tsx
export default function LoginPage() {
  const [state, formAction, isPending] = useActionState(loginAction, initialState);

  return (
    <div className="flex min-h-screen items-center justify-center bg-gray-50">
      <div className="w-full max-w-md rounded-lg bg-white p-8 shadow-md">
        <h1 className="mb-6 text-center font-heading text-2xl font-semibold text-ggtech-darkblue">
          Cargo Sentinel
        </h1>
        <form action={formAction} className="space-y-4">
          <div>
            <label htmlFor="email" className="mb-1 block text-sm font-medium text-gray-700">
              E-mail
            </label>
            <input
              id="email"
              name="email"
              type="email"
              required
              autoComplete="email"
              className="w-full rounded-md border border-gray-300 px-3 py-2 focus:border-ggtech-blue focus:outline-none focus:ring-2 focus:ring-ggtech-blue"
              placeholder="seu@email.com"
            />
          </div>
          <div>
            <label htmlFor="password" className="mb-1 block text-sm font-medium text-gray-700">
              Senha
            </label>
            <input
              id="password"
              name="password"
              type="password"
              required
              autoComplete="current-password"
              className="w-full rounded-md border border-gray-300 px-3 py-2 focus:border-ggtech-blue focus:outline-none focus:ring-2 focus:ring-ggtech-blue"
              placeholder="••••••••"
            />
          </div>
          {state?.error && (
            <p className="text-center text-sm text-red-600">{state.error}</p>
          )}
          <button
            type="submit"
            disabled={isPending}
            className="w-full rounded-md bg-ggtech-blue px-4 py-2 font-medium text-white transition-opacity hover:opacity-90 disabled:opacity-60"
          >
            {isPending ? 'Entrando...' : 'Entrar'}
          </button>
        </form>
      </div>
    </div>
  );
}
```

Adicionar `'use client'` no topo se não estiver presente (já está).
Adicionar `font-heading` no H1 para usar Open Sans (título de seção).
Usar classe `font-sans` no formulário implicitamente (herda do body).

Verificar: nenhum `style=` deve restar no arquivo após a mudança.
  </action>
  <verify>
    <automated>cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel && grep -n "style=" apps/web/src/app/(auth)/login/page.tsx && grep -n "ggtech-blue" apps/web/src/app/(auth)/login/page.tsx</automated>
  </verify>
  <done>Nenhum atributo style= em login/page.tsx; h1 usa text-ggtech-darkblue e font-heading; button usa bg-ggtech-blue; inputs usam focus:ring-ggtech-blue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Login form | Email e senha enviados via Server Action — sem mudança neste plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.5-05 | Tampering | CSS class injection | accept | Classes Tailwind são strings estáticas compiladas em build time; nenhuma classe é construída dinamicamente a partir de input do usuário neste plano |
| T-3.5-06 | Spoofing | Login form styling | accept | Mudança é cosmética; autenticação permanece via Server Action sem alteração |
</threat_model>

<verification>
1. Abrir dashboard em browser: badges de VISITANTE devem ser cinza (não azul), ATENCAO amarelo-escuro, SUSPEITO laranja-escuro
2. `pnpm test --filter=@cargo-sentinel/web` passa sem falhas
3. Abrir página de login: título azul ggtech, botão azul ggtech, sem style inline inspecionado no DOM
4. Inspecionar CriticalConfirmDialog com placa SUSPEITO: ícone e botão laranja-escuro (orange-600, não orange-500)
5. Border-left dos event rows reflete as novas cores: VISITANTE=cinza, ATENCAO=amarelo-escuro, SUSPEITO=laranja-escuro
</verification>

<success_criteria>
- Todos os 5 níveis de classificação com cores corretas conforme UI-04
- Nenhum `style=` inline restante em login/page.tsx
- CriticalConfirmDialog usando orange-600 para SUSPEITO
- Testes existentes passando (pnpm test)
- Requisitos UI-02 e UI-04 endereçados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03.5-ui-design-polish-identidade-visual-ggtech/03.5-03-SUMMARY.md` com:
- Mudanças de cor realizadas (de/para, com hex)
- Estado final dos testes
- Arquivos modificados
</output>
