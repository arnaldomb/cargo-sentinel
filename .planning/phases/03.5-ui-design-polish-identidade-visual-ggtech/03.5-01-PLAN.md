---
id: "03.5-01"
name: "Design Tokens + Typography"
phase: "3.5"
plan: 1
type: execute
wave: 1
autonomous: true
objective: "Configurar next/font/google para Roboto + Open Sans e injetar variáveis CSS no layout raiz, eliminando FOUT e requisições externas de fonte em runtime"
requirements: [UI-02, UI-03]
depends_on: []
files_modified:
  - apps/web/src/app/layout.tsx
  - apps/web/tailwind.config.ts
  - apps/web/src/app/globals.css

must_haves:
  truths:
    - "Roboto carregada via next/font/google (variável CSS --font-roboto aplicada no <html>)"
    - "Open Sans carregada via next/font/google (variável CSS --font-open-sans aplicada no <html>)"
    - "Tailwind fontFamily usa as variáveis CSS, não strings literais"
    - "globals.css não contém font-family com string literal — apenas referência a var(--font-*)"
    - "Cores ggtech no tailwind.config: ggtech-darkblue=#003366, ggtech-blue=#0056b3, ggtech-lightblue=#007bff estão presentes (já existem; manter)"
    - "pnpm build --filter=@cargo-sentinel/web passa sem erro"
  artifacts:
    - path: "apps/web/src/app/layout.tsx"
      provides: "Root layout com next/font, variáveis CSS e classes Tailwind de fonte"
    - path: "apps/web/tailwind.config.ts"
      provides: "fontFamily mapeado para variáveis CSS var(--font-roboto) e var(--font-open-sans)"
    - path: "apps/web/src/app/globals.css"
      provides: "Base CSS sem font-family hardcoded — herda das variáveis CSS do layout"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/tailwind.config.ts"
      via: "className com variáveis CSS (${roboto.variable} ${openSans.variable})"
    - from: "apps/web/tailwind.config.ts"
      to: "apps/web/src/components/*.tsx"
      via: "classes Tailwind font-sans e font-heading"
---

<objective>
Configurar tipografia ggtech via next/font/google no root layout, injetando as variáveis CSS que o Tailwind consome. Eliminar FOUT e requisições externas de fonte.

Purpose: UI-03 exige que Roboto (corpo) e Open Sans (títulos) sejam carregadas em build time, sem `<link>` no `<head>` e sem FOUT. Hoje o globals.css usa `font-family: Roboto` como string literal — carregamento externo em runtime.

Output: layout.tsx com next/font, tailwind.config.ts com fontFamily via variáveis CSS, globals.css limpo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Estado atual dos arquivos que serão modificados -->

apps/web/src/app/layout.tsx (atual — sem next/font):
```tsx
import type { Metadata } from 'next';
import './globals.css';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="pt-BR">
      <body className="bg-gray-50 text-slate-900">{children}</body>
    </html>
  );
}
```

apps/web/tailwind.config.ts (atual — fontFamily com strings literais):
```ts
fontFamily: {
  sans: ['Roboto', 'sans-serif'],
  heading: ['Open Sans', 'sans-serif'],
},
```

apps/web/src/app/globals.css (atual — font-family com string):
```css
@layer base {
  html { font-family: Roboto, system-ui, sans-serif; }
  h1, h2, h3, h4, h5, h6 { font-family: 'Open Sans', system-ui, sans-serif; }
}
```

Componentes usam: font-sans (body), font-heading (títulos h*)
Não há dependência de @next/font — next/font é built-in no Next.js 15.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar next/font/google ao layout raiz</name>
  <files>apps/web/src/app/layout.tsx</files>
  <action>
Substituir o conteúdo de `apps/web/src/app/layout.tsx` pela versão com next/font/google.

Passos exatos:

1. Importar `Roboto` e `Open_Sans` de `next/font/google`.
2. Instanciar cada fonte com `subsets: ['latin']`, `display: 'swap'`, e `variable` (CSS custom property name):
   - `roboto.variable` → `'--font-roboto'`
   - `openSans.variable` → `'--font-open-sans'`
3. Para Roboto, incluir `weight: ['400', '500', '700']` (os pesos usados nos componentes).
4. Para Open Sans, incluir `weight: ['600', '700']` (headings usam font-semibold e font-bold).
5. No `<html>`, adicionar `className={`${roboto.variable} ${openSans.variable}`}`.
6. No `<body>`, manter `className="bg-gray-50 text-slate-900 font-sans"`.

Resultado esperado:
```tsx
import type { Metadata } from 'next';
import { Roboto, Open_Sans } from 'next/font/google';
import './globals.css';

const roboto = Roboto({
  subsets: ['latin'],
  weight: ['400', '500', '700'],
  display: 'swap',
  variable: '--font-roboto',
});

const openSans = Open_Sans({
  subsets: ['latin'],
  weight: ['600', '700'],
  display: 'swap',
  variable: '--font-open-sans',
});

export const metadata: Metadata = {
  title: 'Cargo Sentinel',
  description: 'Plataforma de inteligência de perímetro logístico',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="pt-BR" className={`${roboto.variable} ${openSans.variable}`}>
      <body className="bg-gray-50 text-slate-900 font-sans">{children}</body>
    </html>
  );
}
```

NÃO adicionar `<link>` no `<head>`. NÃO usar `@import` em CSS. O next/font já injeta o `<style>` otimizado.
  </action>
  <verify>
    <automated>cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel && grep -n "next/font/google" apps/web/src/app/layout.tsx && grep -n "variable.*--font" apps/web/src/app/layout.tsx</automated>
  </verify>
  <done>layout.tsx importa Roboto e Open_Sans de next/font/google; html element tem className com ambas as variáveis CSS; sem tag link no head.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Atualizar tailwind.config.ts e globals.css para consumir variáveis CSS</name>
  <files>apps/web/tailwind.config.ts, apps/web/src/app/globals.css</files>
  <action>
**tailwind.config.ts:**

Substituir as entradas de `fontFamily` para referenciar as variáveis CSS injetadas pelo next/font:

```ts
fontFamily: {
  sans: ['var(--font-roboto)', 'system-ui', 'sans-serif'],
  heading: ['var(--font-open-sans)', 'system-ui', 'sans-serif'],
},
```

Manter as cores ggtech existentes (ggtech-darkblue, ggtech-blue, ggtech-lightblue) — NÃO remover.
Manter `content` paths inalterados.

**globals.css:**

Remover as regras `@layer base` que definiam `font-family` via string literal, já que agora o Tailwind e o next/font gerenciam isso via variáveis CSS. O arquivo final deve ser:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

Isso é suficiente porque:
- `font-sans` no `<body>` (via layout.tsx) aplica `var(--font-roboto)` automaticamente
- `font-heading` nos `h1-h6` (via Tailwind base ou classes nos componentes) aplica `var(--font-open-sans)`

Se quiser manter regras de base para headings de forma global (sem precisar colocar font-heading em cada componente), adicionar:

```css
@layer base {
  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-open-sans), system-ui, sans-serif;
  }
}
```

Verificar os componentes existentes: `dashboard-client.tsx` já usa `font-heading` nas classes Tailwind, então a regra @layer base é opcional mas consistente para h* nativos sem classe.
  </action>
  <verify>
    <automated>cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel && grep -n "var(--font-roboto)" apps/web/tailwind.config.ts && grep -n "ggtech-darkblue" apps/web/tailwind.config.ts</automated>
  </verify>
  <done>tailwind.config.ts tem fontFamily.sans = ['var(--font-roboto)', ...] e fontFamily.heading = ['var(--font-open-sans)', ...]; cores ggtech preservadas; globals.css sem strings literais de fonte.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Verificar build sem erros de fonte</name>
  <files></files>
  <action>
Executar o build do workspace web para confirmar que next/font não gera erro de runtime ou build:

```bash
cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel
pnpm build --filter=@cargo-sentinel/web
```

Se o build falhar com erro de `Open_Sans` (underline no nome), verificar: next/font/google usa underscore em nomes compostos (`Open_Sans`, não `OpenSans`). Isso é específico da API do next/font — usar o nome exato conforme documentação.

Se o build passar, o plano está completo. Se falhar por outro motivo, inspecionar o erro e corrigir antes de marcar como done.

Nota: Em desenvolvimento (`pnpm dev`), next/font baixa as fontes uma vez e as serve localmente. Em produção (`pnpm build`), emite CSS estático com `@font-face` — zero requisições externas em runtime.
  </action>
  <verify>
    <automated>cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel && pnpm build --filter=@cargo-sentinel/web 2>&1 | tail -5</automated>
  </verify>
  <done>pnpm build --filter=@cargo-sentinel/web conclui sem erro; next/font está ativo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → Browser | Fontes de terceiros (Google Fonts) — eliminado neste plano via next/font |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.5-01 | Information Disclosure | Google Fonts CDN | mitigate | next/font/google baixa e serve fontes localmente — nenhuma requisição a fonts.googleapis.com em runtime; resolve rastreamento de IP de usuários pelo Google |
| T-3.5-02 | Denial of Service | Font loading | accept | next/font usa `display: 'swap'` — fallback imediato sem bloquear render; risco mínimo |
</threat_model>

<verification>
1. Inspecionar `apps/web/src/app/layout.tsx` — deve conter `from 'next/font/google'` e `className={...variable...}`
2. Inspecionar `apps/web/tailwind.config.ts` — fontFamily deve referenciar `var(--font-roboto)` e `var(--font-open-sans)`
3. `pnpm build --filter=@cargo-sentinel/web` passa
4. No browser (dev server), inspecionar Network tab — nenhuma requisição para `fonts.googleapis.com` ou `fonts.gstatic.com`
5. No browser, inspecionar `<html>` element — deve ter classes com `--font-roboto` e `--font-open-sans` no style computado
</verification>

<success_criteria>
- Roboto e Open Sans carregadas via next/font/google (build-time, sem CDN externo em runtime)
- tailwind.config.ts usa var(--font-roboto) e var(--font-open-sans) em fontFamily
- globals.css sem strings literais de fonte
- pnpm build passa sem erro
- Requisitos UI-02 (botões padronizados — tokens de cor disponíveis) e UI-03 (tipografia via next/font) endereçados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03.5-ui-design-polish-identidade-visual-ggtech/03.5-01-SUMMARY.md` com:
- O que foi feito (fontes configuradas, arquivos modificados)
- Variáveis CSS criadas (--font-roboto, --font-open-sans)
- Decisões tomadas (pesos de fonte escolhidos, por quê globals.css foi simplificado)
- Estado final dos arquivos-chave
</output>
