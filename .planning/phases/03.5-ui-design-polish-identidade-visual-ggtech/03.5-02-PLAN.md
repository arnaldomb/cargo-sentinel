---
id: "03.5-02"
name: "Sidebar Responsiva + Layout Wrapper"
phase: "3.5"
plan: 2
type: execute
wave: 2
autonomous: true
objective: "Extrair a sidebar para componente próprio com colapso mobile (hamburger em ≤768px) e wrapper de layout que garante ausência de overflow horizontal em qualquer breakpoint"
requirements: [UI-01, UI-05]
depends_on: ["03.5-01"]
files_modified:
  - apps/web/src/components/sidebar.tsx
  - apps/web/src/components/dashboard-layout.tsx
  - apps/web/src/components/dashboard-client.tsx
  - apps/web/src/app/page.tsx

must_haves:
  truths:
    - "Em viewport 1280px+: sidebar fixa visível (largura 288px), fundo #003366, texto branco"
    - "Em viewport ≤768px: sidebar oculta por padrão; botão hamburger (Menu icon) no header abre/fecha sidebar como drawer"
    - "Item de nav ativo destacado com bg-ggtech-blue (#0056b3)"
    - "Feed de eventos permanece scrollável em 375px sem overflow horizontal"
    - "Header top-bar preservado com logo e botão Sair"
    - "Ausência de overflow-x em todos os breakpoints (375px, 768px, 1280px)"
  artifacts:
    - path: "apps/web/src/components/sidebar.tsx"
      provides: "Componente Sidebar com props: cameras, userName, userRole, isOpen, onClose"
      exports: ["Sidebar"]
    - path: "apps/web/src/components/dashboard-layout.tsx"
      provides: "Wrapper de layout com toggle state para sidebar mobile"
      exports: ["DashboardLayout"]
    - path: "apps/web/src/components/dashboard-client.tsx"
      provides: "DashboardClient refatorado — sidebar extraída, layout limpo"
  key_links:
    - from: "apps/web/src/components/dashboard-client.tsx"
      to: "apps/web/src/components/sidebar.tsx"
      via: "import { Sidebar } — passa cameras, userName, userRole como props"
    - from: "apps/web/src/components/dashboard-layout.tsx"
      to: "apps/web/src/components/sidebar.tsx"
      via: "controla isOpen state e passa onClose handler"
---

<objective>
Extrair o bloco `<aside>` do DashboardClient para um componente Sidebar dedicado, criar um DashboardLayout que gerencia o toggle mobile, e garantir layout responsivo sem overflow horizontal de 375px a 1280px+.

Purpose: UI-01 exige sidebar fixa com colapso em viewport ≤768px. UI-05 exige layout funcional nos três breakpoints sem overflow. A sidebar atual está inline no DashboardClient, sem suporte a colapso.

Output: Sidebar.tsx (componente isolado), DashboardLayout.tsx (wrapper com toggle state), DashboardClient.tsx refatorado para consumir os dois.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.5-ui-design-polish-identidade-visual-ggtech/03.5-01-SUMMARY.md

<interfaces>
<!-- Interfaces relevantes extraídas dos arquivos atuais -->

De apps/web/src/lib/dashboard.ts:
```ts
export type CameraStatusItem = {
  id: string;
  codigoLpr: string;
  ip: string | null;
  obra: { id: string; nome: string };
  ultimoEventoEm: string | null;
  status: 'online' | 'offline';
};
```

De apps/web/src/components/dashboard-client.tsx (bloco aside atual):
```tsx
<aside className="flex w-72 flex-shrink-0 flex-col overflow-y-auto bg-ggtech-darkblue p-4">
  <div className="mb-4">
    <h2 className="font-heading text-base font-semibold text-white">Câmeras LPR</h2>
    <p className="mt-1 text-xs text-blue-300">{userName} · {userRole}</p>
  </div>
  <div className="flex flex-col gap-3">
    {cameras.map((camera) => (
      <div key={camera.id} className="rounded-lg bg-white/10 p-3 text-white backdrop-blur-sm">
        ...
      </div>
    ))}
  </div>
</aside>
```

De apps/web/src/app/page.tsx (header atual):
```tsx
<header className="flex items-center justify-between bg-ggtech-darkblue px-6 py-3 shadow-md">
  <div>
    <h1 className="font-heading text-xl font-bold text-white">Cargo Sentinel</h1>
    <p className="text-xs text-blue-200">Inteligência de perímetro logístico em tempo real</p>
  </div>
  <form action={logoutAction}>
    <button type="submit" className="rounded-lg bg-ggtech-blue px-4 py-2 text-sm font-medium text-white ...">
      Sair
    </button>
  </form>
</header>
```

Lucide React está disponível (listado em dependencies via packages/ui ou instalado — verificar; se não instalado, instalar em apps/web).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar componente Sidebar</name>
  <files>apps/web/src/components/sidebar.tsx</files>
  <action>
Criar `apps/web/src/components/sidebar.tsx` como Client Component.

Props da Sidebar:
```ts
type SidebarProps = {
  cameras: CameraStatusItem[];
  userName: string;
  userRole: string;
  isOpen: boolean;        // controla visibilidade em mobile
  onClose: () => void;    // fecha o drawer mobile
};
```

**Comportamento desktop (lg+, ≥1024px):**
- `<aside>` sempre visível, posição estática no flex layout
- Largura fixa: `w-72` (288px)
- Fundo: `bg-ggtech-darkblue` (#003366)
- Texto branco, overflow-y auto

**Comportamento mobile (<lg, <1024px):**
- Sidebar como drawer: `fixed inset-y-0 left-0 z-40`
- Quando `isOpen=false`: `translate-x-[-100%]` (oculto fora da tela)
- Quando `isOpen=true`: `translate-x-0` (visível sobre o conteúdo)
- Overlay escuro atrás (`fixed inset-0 bg-black/50 z-30`) clicável para fechar
- Transição suave: `transition-transform duration-300 ease-in-out`

**Implementação com Tailwind:**
```tsx
'use client';

import { X } from 'lucide-react';
import type { CameraStatusItem } from '@/lib/dashboard';

type SidebarProps = {
  cameras: CameraStatusItem[];
  userName: string;
  userRole: string;
  isOpen: boolean;
  onClose: () => void;
};

export function Sidebar({ cameras, userName, userRole, isOpen, onClose }: SidebarProps) {
  return (
    <>
      {/* Mobile overlay */}
      {isOpen && (
        <div
          className="fixed inset-0 z-30 bg-black/50 lg:hidden"
          onClick={onClose}
          aria-hidden="true"
        />
      )}

      {/* Sidebar panel */}
      <aside
        className={[
          'flex flex-col overflow-y-auto bg-ggtech-darkblue p-4',
          // Desktop: always visible, static
          'lg:relative lg:w-72 lg:flex-shrink-0 lg:translate-x-0',
          // Mobile: drawer fixed
          'fixed inset-y-0 left-0 z-40 w-72',
          'transition-transform duration-300 ease-in-out',
          isOpen ? 'translate-x-0' : '-translate-x-full lg:translate-x-0',
        ].join(' ')}
        aria-label="Navegação lateral"
      >
        {/* Close button (mobile only) */}
        <div className="mb-4 flex items-center justify-between lg:block">
          <div>
            <h2 className="font-heading text-base font-semibold text-white">Câmeras LPR</h2>
            <p className="mt-1 text-xs text-blue-300">{userName} · {userRole}</p>
          </div>
          <button
            onClick={onClose}
            className="flex h-8 w-8 items-center justify-center rounded-lg text-white hover:bg-white/10 lg:hidden"
            aria-label="Fechar menu"
          >
            <X size={18} />
          </button>
        </div>

        <div className="flex flex-col gap-3">
          {cameras.length === 0 && (
            <p className="text-xs text-blue-300">Nenhuma câmera registrada.</p>
          )}
          {cameras.map((camera) => (
            <div
              key={camera.id}
              className="rounded-lg bg-white/10 p-3 text-white backdrop-blur-sm"
            >
              <div className="flex items-center justify-between gap-2">
                <strong className="text-sm font-semibold">{camera.codigoLpr}</strong>
                <span
                  className={`rounded-full px-2 py-0.5 text-xs font-medium text-white ${
                    camera.status === 'online' ? 'bg-green-600' : 'bg-slate-500'
                  }`}
                >
                  {camera.status}
                </span>
              </div>
              <div className="mt-1 text-xs text-blue-200">{camera.obra.nome}</div>
              <div className="mt-1 text-xs text-blue-300">
                Último:{' '}
                {camera.ultimoEventoEm
                  ? new Date(camera.ultimoEventoEm).toLocaleString('pt-BR')
                  : 'nunca'}
              </div>
            </div>
          ))}
        </div>
      </aside>
    </>
  );
}
```

Se `lucide-react` não estiver instalado em apps/web, instalar:
```bash
pnpm add lucide-react --filter=@cargo-sentinel/web
```
  </action>
  <verify>
    <automated>cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel && grep -n "export function Sidebar" apps/web/src/components/sidebar.tsx && grep -n "translate-x" apps/web/src/components/sidebar.tsx</automated>
  </verify>
  <done>sidebar.tsx existe com export Sidebar; implementa drawer mobile via translate-x com transição; overlay com z-30; botão X visível apenas em mobile (lg:hidden).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Refatorar DashboardClient para usar Sidebar + hamburger</name>
  <files>apps/web/src/components/dashboard-client.tsx</files>
  <action>
Refatorar `dashboard-client.tsx` para:

1. Importar `Sidebar` de `./sidebar`
2. Importar `Menu` de `lucide-react`
3. Adicionar estado: `const [sidebarOpen, setSidebarOpen] = useState(false)`
4. Remover o bloco `<aside>` inline (extraído para Sidebar)
5. Adicionar botão hamburger no header mobile

**Estrutura do return refatorado:**

```tsx
return (
  <>
    {/* Layout principal */}
    <div className="flex flex-1 overflow-hidden">
      {/* Sidebar com câmeras */}
      <Sidebar
        cameras={cameras}
        userName={userName}
        userRole={userRole}
        isOpen={sidebarOpen}
        onClose={() => setSidebarOpen(false)}
      />

      {/* Main — feed de eventos */}
      <main className="flex flex-1 flex-col overflow-hidden p-4">
        <div className="mb-3 flex items-center justify-between">
          <div className="flex items-center gap-3">
            {/* Hamburger — visível apenas em mobile */}
            <button
              onClick={() => setSidebarOpen(true)}
              className="flex h-9 w-9 items-center justify-center rounded-lg text-slate-600 hover:bg-slate-100 lg:hidden"
              aria-label="Abrir menu de câmeras"
              data-testid="hamburger-btn"
            >
              <Menu size={20} />
            </button>
            <div>
              <h2 className="font-heading text-lg font-bold text-slate-900">Feed Operacional</h2>
              <p className="text-sm text-slate-500">Eventos ao vivo com classificação inline.</p>
            </div>
          </div>

          {pendingCount > 0 && (
            <button
              onClick={scrollToTopAndResume}
              className="rounded-full bg-ggtech-blue px-4 py-2 text-sm font-medium text-white transition-opacity hover:opacity-90"
              data-testid="pending-events-btn"
            >
              {pendingCount} novos eventos ↑
            </button>
          )}
        </div>

        {/* resto igual — loading, error, feed list */}
        ...
      </main>
    </div>

    {/* Critical confirmation dialog */}
    ...
  </>
);
```

Manter TODA a lógica de negócio existente (Socket.IO, submitClassification, handleClassificationSelect, scroll pinning, etc.) — apenas a apresentação da sidebar muda.

O `<div className="flex h-screen flex-col bg-gray-50">` no page.tsx permanece no page.tsx — não mover para cá.

Adicionar `Menu` aos imports de lucide-react (ou criar se não houver import ainda).
  </action>
  <verify>
    <automated>cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel && grep -n "sidebarOpen" apps/web/src/components/dashboard-client.tsx && grep -n "import.*Sidebar" apps/web/src/components/dashboard-client.tsx</automated>
  </verify>
  <done>dashboard-client.tsx importa Sidebar e usa sidebarOpen state; bloco aside inline removido; botão hamburger com data-testid="hamburger-btn" presente; toda lógica de negócio preservada.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Garantir layout page.tsx sem overflow horizontal</name>
  <files>apps/web/src/app/page.tsx</files>
  <action>
Revisar `apps/web/src/app/page.tsx` para garantir que o layout container principal previne overflow horizontal em 375px.

Mudanças necessárias:
1. O div raiz deve ter `overflow-hidden` para conter o sidebar drawer: `className="flex h-screen flex-col bg-gray-50 overflow-hidden"`
2. O header precisa de `flex-shrink-0` para não comprimir quando o conteúdo interno for grande
3. O DashboardClient ocupa `flex-1 overflow-hidden` implicitamente (já existe pois é filho de flex-col)

Resultado final do page.tsx:
```tsx
import { redirect } from 'next/navigation';
import { DashboardClient } from '@/components/dashboard-client';
import { auth } from '../../auth';
import { logoutAction } from './(auth)/login/actions';

export default async function Home() {
  const session = await auth();

  if (!session?.user) {
    redirect('/login');
  }

  return (
    <div className="flex h-screen flex-col overflow-hidden bg-gray-50">
      {/* Top bar */}
      <header className="flex flex-shrink-0 items-center justify-between bg-ggtech-darkblue px-6 py-3 shadow-md">
        <div>
          <h1 className="font-heading text-xl font-bold text-white">Cargo Sentinel</h1>
          <p className="text-xs text-blue-200">
            Inteligência de perímetro logístico em tempo real
          </p>
        </div>

        <form action={logoutAction}>
          <button
            type="submit"
            className="rounded-lg bg-ggtech-blue px-4 py-2 text-sm font-medium text-white transition-opacity hover:opacity-90 active:opacity-75"
          >
            Sair
          </button>
        </form>
      </header>

      {/* Body: sidebar + main */}
      <div className="flex flex-1 overflow-hidden">
        <DashboardClient
          userName={session.user.name ?? session.user.email ?? 'Operador'}
          userRole={session.user.role}
        />
      </div>
    </div>
  );
}
```

Nota: O DashboardClient retorna um Fragment (`<>`) com o layout flex. O wrapper `div.flex.flex-1.overflow-hidden` garante que o par sidebar+main fique contido na altura restante da tela.

Verificar que o DashboardClient não retorna um div externo com `flex flex-1 overflow-hidden` — se retornar, remover o wrapper extra para evitar aninhamento duplo. Inspecionar o arquivo atual antes de alterar.
  </action>
  <verify>
    <automated>cd D:\GGTRONIC\Desenvolvimeto\cargo-sentinel && grep -n "overflow-hidden" apps/web/src/app/page.tsx && grep -n "flex-shrink-0" apps/web/src/app/page.tsx</automated>
  </verify>
  <done>page.tsx tem overflow-hidden no div raiz; header tem flex-shrink-0; wrapper flex-1 envolve DashboardClient; sem overflow-x em 375px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client state → DOM | sidebarOpen state controla visibilidade via className — sem acesso a dados sensíveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.5-03 | Tampering | Sidebar drawer z-index | accept | z-index apenas cosmético; sem dados sensíveis expostos por posicionamento de layout |
| T-3.5-04 | Information Disclosure | Câmeras visíveis na sidebar | accept | Dados de câmera já visíveis no feed; sidebar não expõe nada adicional; isolamento de tenant é feito na API |
</threat_model>

<verification>
1. Em viewport 1280px: sidebar visível ao lado do feed, fundo #003366
2. Em viewport 375px: sidebar oculta; botão hamburger (Menu icon) visível no header do feed; clicar abre drawer com overlay escuro; clicar no overlay fecha
3. Em viewport 375px: sem scroll horizontal na página (inspecionar via DevTools → Responsive)
4. Botão X dentro do drawer funciona (fecha sidebar)
5. `pnpm build --filter=@cargo-sentinel/web` passa sem erro
</verification>

<success_criteria>
- Sidebar como componente isolado em sidebar.tsx
- Comportamento drawer funcional em mobile (<lg) com transition suave
- Overlay com z-30 fecha ao clicar fora
- Botão hamburger visível apenas em mobile (lg:hidden no inverso)
- Sem overflow horizontal em 375px, 768px e 1280px+
- Requisitos UI-01 e UI-05 endereçados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03.5-ui-design-polish-identidade-visual-ggtech/03.5-02-SUMMARY.md` com:
- Arquivos criados/modificados
- Estrutura do drawer mobile (classes Tailwind usadas)
- Decisões: breakpoint lg (1024px) escolhido para colapso
- Qualquer ajuste feito em page.tsx
</output>
