---
id: "04-02"
name: "Cross-Site Detection + BullMQ Alert Queue"
phase: "4"
plan: 2
type: execute
wave: 2
autonomous: true
objective: "Após upsert da Placa no worker.ts, detectar condição cross-site (classificacao SUSPEITO/CRITICO + obraId diferente de obraClassificacaoId) e enfileirar jobs de alerta; criar alert-worker.ts que processa jobs com concorrência 1"
requirements: [INTEL-01, INTEL-02, ALERTS-02, ALERTS-03, ALERTS-04, ALERTS-05]
files_modified:
  - apps/api/src/jobs/worker.ts
  - apps/api/src/jobs/alert-worker.ts
  - apps/api/src/jobs/alert-worker.test.ts
  - apps/api/src/jobs/queue.ts
  - apps/api/src/index.ts
depends_on: ["04-01"]

must_haves:
  truths:
    - "Quando placa SUSPEITO/CRITICO é detectada em obra diferente da obraClassificacaoId, job alert:cross-site é enfileirado no BullMQ"
    - "alert-worker.ts processa alert:cross-site (emit Socket.IO) e alert:whatsapp (enviar mensagem) com concorrência 1"
    - "obraClassificacaoId em Placa é atualizado quando classificação muda para SUSPEITO/CRITICO"
    - "Deduplicação via Redis key alert:dedup:{empresaId}:{placa} com TTL 5min SUSPEITO / 15min CRITICO"
    - "Worker de alertas não lança se Evolution API está offline — registra erro e continua"
  artifacts:
    - path: "apps/api/src/jobs/worker.ts"
      provides: "Lógica de detecção cross-site após upsert de Placa"
      contains: "alert:cross-site"
    - path: "apps/api/src/jobs/alert-worker.ts"
      provides: "BullMQ Worker processando jobs de alerta"
      exports: ["alertWorker", "processAlertJob"]
    - path: "apps/api/src/jobs/queue.ts"
      provides: "Queue de alertas registrada"
      contains: "alert-jobs"
  key_links:
    - from: "apps/api/src/jobs/worker.ts"
      to: "apps/api/src/jobs/queue.ts"
      via: "alertQueue.add('alert:cross-site', payload)"
    - from: "apps/api/src/jobs/alert-worker.ts"
      to: "apps/api/src/services/whatsapp.ts"
      via: "sendAlertaWhatsApp(telefone, mensagem)"
    - from: "apps/api/src/jobs/alert-worker.ts"
      to: "apps/api/src/realtime/server.ts"
      via: "emitAlertaCrossSite(empresaId, payload)"
---

<objective>
Este plano implementa o núcleo da inteligência cross-site: a lógica de detecção no worker de LPR e a fila de processamento assíncrono de alertas.

Purpose: A detecção cross-site exige comparar `obraId` do evento atual com `obraClassificacaoId` da Placa — se diferentes e classificação for SUSPEITO(4) ou CRITICO(5), o sistema dispara. O processamento assíncrono via BullMQ (concorrência 1) garante que a WhatsApp não seja sobrecarregada e que o webhook LPR retorne 200 imediatamente (ALERTS-03). A deduplicação via Redis TTL evita spam de mensagens repetidas (ALERTS-04).

Output: worker.ts modificado, alert-worker.ts criado, queue.ts atualizado, index.ts registra o novo worker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/api/src/jobs/worker.ts
@apps/api/src/jobs/queue.ts
@apps/api/src/realtime/server.ts
@apps/api/src/services/redis.ts
@apps/api/src/index.ts

<interfaces>
<!-- Tipos e contratos que o executor precisa conhecer -->

De apps/api/src/jobs/worker.ts (estado atual após Plan 04-01):
O worker já tem: camera lookup, placa upsert, evento upsert, emitEventoNovo, emitCameraStatus.
A lógica cross-site deve ser inserida APÓS o upsert da Placa e ANTES do upsert do Evento.

De apps/api/src/services/whatsapp.ts (criado em Plan 04-01):
```typescript
export async function sendAlertaWhatsApp(telefone: string, mensagem: string): Promise<WhatsAppSendResult>
```

De apps/api/src/realtime/server.ts (a ser estendido em Plan 04-03):
```typescript
// Esta função será criada em Plan 04-03 — o alert-worker.ts vai importá-la
export function emitAlertaCrossSite(empresaId: string, payload: CrossSiteAlertPayload): void
```

Novo tipo de job a criar:
```typescript
// apps/api/src/jobs/alert-worker.ts
export type CrossSiteAlertPayload = {
  empresaId: string;
  placaNumero: string;
  classificacao: 'SUSPEITO' | 'CRITICO';
  obraDetectadaId: string;
  obraDetectadaNome: string;
  obraClassificacaoId: string;
  obraClassificacaoNome: string;
  eventoId: string;
  timestamp: string;
};

export type WhatsAppAlertPayload = {
  empresaId: string;
  obraId: string;
  placaNumero: string;
  classificacao: 'SUSPEITO' | 'CRITICO';
  obraDetectadaNome: string;
  obraClassificacaoNome: string;
  timestamp: string;
};
```

De packages/database/prisma/schema.prisma (após Plan 04-01):
```prisma
model Placa {
  obraClassificacaoId String?
  obraClassificacao   Obra? @relation("PlacaClassificacaoObra", ...)
}

model ConfiguracaoAlerta {
  id        String
  obraId    String
  empresaId String
  telefone  String
  ativo     Boolean
}
```

De apps/api/src/services/redis.ts:
```typescript
export function createRedisConnection(): IORedis
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar queue de alertas e alert-worker.ts</name>
  <files>apps/api/src/jobs/queue.ts, apps/api/src/jobs/alert-worker.ts, apps/api/src/jobs/alert-worker.test.ts</files>
  <behavior>
    - queue.ts já exporta lprQueue — adicionar alertQueue com nome 'alert-jobs'
    - alert-worker.ts processa dois tipos de job: 'alert:cross-site' e 'alert:whatsapp'
    - concurrency: 1 para ambos os tipos (ALERTS-03: nunca paralelo, preserva ordem de entrega)
    - 'alert:cross-site' chama emitAlertaCrossSite (importado de realtime/server) — não falha se socket não inicializado
    - 'alert:whatsapp' verifica dedup Redis, busca ConfiguracaoAlerta ativos da obra, envia para cada número
    - Dedup: SET alert:dedup:{empresaId}:{placa} com NX+EX. Se retorna null = já enviou, skip.
    - TTL de dedup: SUSPEITO = 300s (5min), CRITICO = 900s (15min) — ALERTS-04
    - processAlertJob é exportada e testável sem instanciar o Worker
    - Testes mockam prisma e redis, não fazem conexões reais
  </behavior>
  <action>
1. Abrir apps/api/src/jobs/queue.ts e adicionar a alertQueue:

```typescript
import { Queue } from 'bullmq';
import { createRedisConnection } from '../services/redis';

// Existing lprQueue (keep as is)
export const lprQueue = new Queue('lpr-events', {
  connection: createRedisConnection(),
});

// New alert queue — separate from lpr queue (ALERTS-03: concurrency 1 enforced in worker)
export const alertQueue = new Queue('alert-jobs', {
  connection: createRedisConnection(),
});
```

(Se queue.ts já tiver lprQueue declarada de outra forma, preservar e apenas adicionar alertQueue)

2. Criar apps/api/src/jobs/alert-worker.ts:

```typescript
import { Worker } from 'bullmq';
import { prisma } from '@cargo-sentinel/database';
import { createRedisConnection } from '../services/redis';
import { sendAlertaWhatsApp } from '../services/whatsapp';

export type CrossSiteAlertPayload = {
  empresaId: string;
  placaNumero: string;
  classificacao: 'SUSPEITO' | 'CRITICO';
  obraDetectadaId: string;
  obraDetectadaNome: string;
  obraClassificacaoId: string;
  obraClassificacaoNome: string;
  eventoId: string;
  timestamp: string;
};

export type WhatsAppAlertPayload = {
  empresaId: string;
  obraId: string;
  placaNumero: string;
  classificacao: 'SUSPEITO' | 'CRITICO';
  obraDetectadaNome: string;
  obraClassificacaoNome: string;
  timestamp: string;
};

export type AlertJobData =
  | { type: 'alert:cross-site'; payload: CrossSiteAlertPayload }
  | { type: 'alert:whatsapp'; payload: WhatsAppAlertPayload };

/** TTL de dedup em segundos por nível de classificação (ALERTS-04) */
const DEDUP_TTL: Record<'SUSPEITO' | 'CRITICO', number> = {
  SUSPEITO: 300,  // 5 minutos
  CRITICO: 900,   // 15 minutos
};

/**
 * Verifica deduplicação via Redis NX+EX.
 * Retorna true se devemos enviar (primeira ocorrência na janela).
 * Retorna false se já enviamos (chave existe — janela ativa).
 */
export async function checkAndSetDedup(
  redis: import('ioredis').default,
  empresaId: string,
  placa: string,
  classificacao: 'SUSPEITO' | 'CRITICO',
): Promise<boolean> {
  const key = `alert:dedup:${empresaId}:${placa}`;
  const ttl = DEDUP_TTL[classificacao];
  // SET key value NX EX ttl — retorna "OK" se setou, null se já existia
  const result = await redis.set(key, '1', 'NX', 'EX', ttl);
  return result === 'OK';
}

/**
 * Formata mensagem WhatsApp para alert cross-site.
 * INTEL-03: deve conter placa, classificação, obra detectada, obra original.
 */
export function formatWhatsAppMessage(payload: WhatsAppAlertPayload): string {
  const nivel = payload.classificacao === 'CRITICO' ? 'CRITICO' : 'SUSPEITO';
  return (
    `⚠️ ALERTA ${nivel} — Cargo Sentinel\n\n` +
    `Placa: ${payload.placaNumero}\n` +
    `Nível: ${nivel}\n` +
    `Detectada em: ${payload.obraDetectadaNome}\n` +
    `Classificada originalmente em: ${payload.obraClassificacaoNome}\n` +
    `Horário: ${new Date(payload.timestamp).toLocaleString('pt-BR')}`
  );
}

/**
 * Processa um job de alerta.
 * Exportada para facilitar testes sem instanciar o Worker BullMQ.
 *
 * ALERTS-03: Nunca chamado diretamente do webhook — sempre via fila.
 */
export async function processAlertJob(
  data: AlertJobData,
  deps: {
    emitCrossSite: (empresaId: string, payload: CrossSiteAlertPayload) => void;
    redis: import('ioredis').default;
  },
): Promise<void> {
  if (data.type === 'alert:cross-site') {
    // Emite via Socket.IO para todos operadores da empresa (INTEL-04)
    try {
      deps.emitCrossSite(data.payload.empresaId, data.payload);
    } catch (err) {
      // Socket.IO pode não estar inicializado em testes — não falha o job
      console.warn('[alert-worker] emitCrossSite falhou (socket não iniciado?):', err);
    }
    return;
  }

  if (data.type === 'alert:whatsapp') {
    const { payload } = data;

    // ALERTS-04 + ALERTS-05: dedup Redis antes de buscar números
    const shouldSend = await checkAndSetDedup(
      deps.redis,
      payload.empresaId,
      payload.placaNumero,
      payload.classificacao,
    );

    if (!shouldSend) {
      console.log(
        `[alert-worker] dedup: pulando WhatsApp para ${payload.placaNumero} (janela ativa)`,
      );
      return;
    }

    // Busca números configurados para a obra (ALERTS-06)
    const configuracoes = await prisma.configuracaoAlerta.findMany({
      where: {
        obraId: payload.obraId,
        empresaId: payload.empresaId,
        ativo: true,
      },
      select: { telefone: true },
    });

    if (configuracoes.length === 0) {
      console.log(
        `[alert-worker] nenhum número configurado para obra ${payload.obraId}`,
      );
      return;
    }

    const mensagem = formatWhatsAppMessage(payload);

    // ALERTS-03: concorrência 1 — enviados sequencialmente por ordem do worker
    for (const config of configuracoes) {
      const result = await sendAlertaWhatsApp(config.telefone, mensagem);
      if (!result.success) {
        console.error(
          `[alert-worker] falha ao enviar WhatsApp para ${config.telefone}: ${result.error}`,
        );
        // Não relança — continua para próximos números. Falha de 1 não cancela os demais.
      } else {
        console.log(
          `[alert-worker] WhatsApp enviado para ${config.telefone} (msgId: ${result.messageId})`,
        );
      }
    }
  }
}

/**
 * BullMQ Worker para processamento de alertas.
 * concurrency: 1 garante ordem de entrega (ALERTS-03).
 * Não instanciado em ambiente de teste.
 */
export const alertWorker =
  process.env.NODE_ENV === 'test'
    ? null
    : new Worker(
        'alert-jobs',
        async (job) => {
          const { emitAlertaCrossSite } = await import('../realtime/server');
          const { createRedisConnection } = await import('../services/redis');
          const redis = createRedisConnection();

          try {
            await processAlertJob(job.data as AlertJobData, {
              emitCrossSite: emitAlertaCrossSite,
              redis,
            });
          } finally {
            await redis.quit();
          }
        },
        {
          connection: createRedisConnection(),
          concurrency: 1, // ALERTS-03: nunca paralelo
        },
      );
```

3. Criar apps/api/src/jobs/alert-worker.test.ts:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { checkAndSetDedup, formatWhatsAppMessage, processAlertJob } from './alert-worker';
import type { CrossSiteAlertPayload, WhatsAppAlertPayload } from './alert-worker';

// Mock do prisma
vi.mock('@cargo-sentinel/database', () => ({
  prisma: {
    configuracaoAlerta: {
      findMany: vi.fn(),
    },
  },
}));

// Mock do whatsapp service
vi.mock('../services/whatsapp', () => ({
  sendAlertaWhatsApp: vi.fn(),
}));

const mockEmitCrossSite = vi.fn();

const crossSitePayload: CrossSiteAlertPayload = {
  empresaId: 'emp-1',
  placaNumero: 'ABC1234',
  classificacao: 'SUSPEITO',
  obraDetectadaId: 'obra-b',
  obraDetectadaNome: 'Obra B',
  obraClassificacaoId: 'obra-a',
  obraClassificacaoNome: 'Obra A',
  eventoId: 'evt-1',
  timestamp: new Date('2026-06-21T10:00:00Z').toISOString(),
};

const whatsappPayload: WhatsAppAlertPayload = {
  empresaId: 'emp-1',
  obraId: 'obra-b',
  placaNumero: 'ABC1234',
  classificacao: 'SUSPEITO',
  obraDetectadaNome: 'Obra B',
  obraClassificacaoNome: 'Obra A',
  timestamp: new Date('2026-06-21T10:00:00Z').toISOString(),
};

describe('checkAndSetDedup', () => {
  it('returns true on first call (key not set)', async () => {
    const mockRedis = { set: vi.fn().mockResolvedValue('OK') } as any;
    const result = await checkAndSetDedup(mockRedis, 'emp-1', 'ABC1234', 'SUSPEITO');
    expect(result).toBe(true);
    expect(mockRedis.set).toHaveBeenCalledWith(
      'alert:dedup:emp-1:ABC1234',
      '1',
      'NX',
      'EX',
      300, // SUSPEITO TTL
    );
  });

  it('returns false when key already exists (duplicate within window)', async () => {
    const mockRedis = { set: vi.fn().mockResolvedValue(null) } as any;
    const result = await checkAndSetDedup(mockRedis, 'emp-1', 'ABC1234', 'SUSPEITO');
    expect(result).toBe(false);
  });

  it('uses 900s TTL for CRITICO', async () => {
    const mockRedis = { set: vi.fn().mockResolvedValue('OK') } as any;
    await checkAndSetDedup(mockRedis, 'emp-1', 'ABC1234', 'CRITICO');
    expect(mockRedis.set).toHaveBeenCalledWith(
      'alert:dedup:emp-1:ABC1234',
      '1',
      'NX',
      'EX',
      900, // CRITICO TTL
    );
  });
});

describe('formatWhatsAppMessage', () => {
  it('includes plate, classification, and both obra names', () => {
    const msg = formatWhatsAppMessage(whatsappPayload);
    expect(msg).toContain('ABC1234');
    expect(msg).toContain('SUSPEITO');
    expect(msg).toContain('Obra B');
    expect(msg).toContain('Obra A');
  });

  it('uses CRITICO label for level 5', () => {
    const msg = formatWhatsAppMessage({ ...whatsappPayload, classificacao: 'CRITICO' });
    expect(msg).toContain('CRITICO');
  });
});

describe('processAlertJob — alert:cross-site', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('calls emitCrossSite with correct payload', async () => {
    const mockRedis = { set: vi.fn() } as any;
    await processAlertJob(
      { type: 'alert:cross-site', payload: crossSitePayload },
      { emitCrossSite: mockEmitCrossSite, redis: mockRedis },
    );
    expect(mockEmitCrossSite).toHaveBeenCalledWith('emp-1', crossSitePayload);
  });

  it('does not throw when emitCrossSite throws (socket not initialized)', async () => {
    const mockRedis = { set: vi.fn() } as any;
    const throwingEmit = vi.fn().mockImplementation(() => { throw new Error('not initialized'); });
    await expect(
      processAlertJob(
        { type: 'alert:cross-site', payload: crossSitePayload },
        { emitCrossSite: throwingEmit, redis: mockRedis },
      ),
    ).resolves.not.toThrow();
  });
});

describe('processAlertJob — alert:whatsapp', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('skips sending when dedup key exists', async () => {
    const { sendAlertaWhatsApp } = await import('../services/whatsapp');
    const mockRedis = { set: vi.fn().mockResolvedValue(null) } as any; // null = key exists

    await processAlertJob(
      { type: 'alert:whatsapp', payload: whatsappPayload },
      { emitCrossSite: mockEmitCrossSite, redis: mockRedis },
    );

    expect(sendAlertaWhatsApp).not.toHaveBeenCalled();
  });

  it('sends to all active configured numbers when dedup passes', async () => {
    const { prisma } = await import('@cargo-sentinel/database');
    const { sendAlertaWhatsApp } = await import('../services/whatsapp');

    const mockRedis = { set: vi.fn().mockResolvedValue('OK') } as any;
    vi.mocked(prisma.configuracaoAlerta.findMany).mockResolvedValue([
      { telefone: '+5511111111111' } as any,
      { telefone: '+5522222222222' } as any,
    ]);
    vi.mocked(sendAlertaWhatsApp).mockResolvedValue({ success: true, messageId: 'msg-1' });

    await processAlertJob(
      { type: 'alert:whatsapp', payload: whatsappPayload },
      { emitCrossSite: mockEmitCrossSite, redis: mockRedis },
    );

    expect(sendAlertaWhatsApp).toHaveBeenCalledTimes(2);
    expect(sendAlertaWhatsApp).toHaveBeenCalledWith('+5511111111111', expect.stringContaining('ABC1234'));
  });

  it('continues sending to remaining numbers when one fails', async () => {
    const { prisma } = await import('@cargo-sentinel/database');
    const { sendAlertaWhatsApp } = await import('../services/whatsapp');

    const mockRedis = { set: vi.fn().mockResolvedValue('OK') } as any;
    vi.mocked(prisma.configuracaoAlerta.findMany).mockResolvedValue([
      { telefone: '+5511111111111' } as any,
      { telefone: '+5522222222222' } as any,
    ]);
    vi.mocked(sendAlertaWhatsApp)
      .mockResolvedValueOnce({ success: false, error: 'timeout' })
      .mockResolvedValueOnce({ success: true, messageId: 'msg-2' });

    await expect(
      processAlertJob(
        { type: 'alert:whatsapp', payload: whatsappPayload },
        { emitCrossSite: mockEmitCrossSite, redis: mockRedis },
      ),
    ).resolves.not.toThrow();

    expect(sendAlertaWhatsApp).toHaveBeenCalledTimes(2);
  });
});
```
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/jobs/alert-worker.test.ts</automated>
  </verify>
  <done>Todos os testes de alert-worker passando. alertQueue exportada de queue.ts. alert-worker.ts exporta processAlertJob, checkAndSetDedup, formatWhatsAppMessage. alertWorker null em NODE_ENV=test.</done>
</task>

<task type="auto">
  <name>Task 2: Modificar worker.ts — lógica cross-site + enfileiramento</name>
  <files>apps/api/src/jobs/worker.ts, apps/api/src/jobs/worker.test.ts</files>
  <behavior>
    - APÓS o upsert da Placa e ANTES do upsert do Evento: verificar se classificação é SUSPEITO ou CRITICO
    - Se sim: comparar placa.obraClassificacaoId com camera.obraId
    - Se diferentes (cross-site detectado): enfileirar alert:cross-site E alert:whatsapp
    - Se o upsert criou NOVA placa (placa recém-criada = VISITANTE): sem alert (classificacao < 4)
    - Quando placa já existia com obraClassificacaoId === null e classificacao >= 4: não é cross-site ainda (não há "obra de origem"), apenas enfileira alert:whatsapp
    - INTEL-01: toda detecção LPR consulta classificação da placa no nível da empresa (o upsert já faz isso)
    - obraClassificacaoId é SET no worker quando worker.ts processa evento (não em placas.ts — o campo é definido no momento da classificação manual)
    - CORREÇÃO: obraClassificacaoId deve ser set em placas.ts (route de reclassificação) — adicionar ao update ao mudar para SUSPEITO/CRITICO. No worker.ts apenas DETECTAR (não setar).
  </behavior>
  <action>
1. Abrir apps/api/src/jobs/worker.ts

2. Adicionar import da alertQueue e dos tipos:
```typescript
import { alertQueue } from './queue';
import type { CrossSiteAlertPayload, WhatsAppAlertPayload } from './alert-worker';
```

3. Na função processLprJob, após o bloco `const placa = await prisma.placa.upsert(...)` e ANTES do upsert do Evento, adicionar:

```typescript
  // ── INTEL-01: Cross-site intelligence check ───────────────────────────────
  // Classificações que disparam alerta: SUSPEITO (4) ou CRITICO (5)
  const isHighRisk =
    placa.classificacao === 'SUSPEITO' || placa.classificacao === 'CRITICO';

  // INTEL-02: cross-site = placa foi classificada em OUTRA obra
  // obraClassificacaoId === null: classificação veio de outra sessão e não foi registrada, ou
  // a placa nunca foi manualmente classificada em nenhuma obra — não é cross-site neste caso
  const isCrossSite =
    isHighRisk &&
    placa.obraClassificacaoId !== null &&
    placa.obraClassificacaoId !== camera.obraId;

  if (isCrossSite && placa.obraClassificacaoId) {
    // Buscar nome da obra de classificação original para o payload
    const obraClassificacao = await prisma.obra.findUnique({
      where: { id: placa.obraClassificacaoId },
      select: { id: true, nome: true },
    });

    const obraClassificacaoNome = obraClassificacao?.nome ?? 'Obra desconhecida';

    const crossSitePayload: CrossSiteAlertPayload = {
      empresaId: camera.empresaId,
      placaNumero: PlateNumber,
      classificacao: placa.classificacao as 'SUSPEITO' | 'CRITICO',
      obraDetectadaId: camera.obraId,
      obraDetectadaNome: camera.obra.nome,
      obraClassificacaoId: placa.obraClassificacaoId,
      obraClassificacaoNome,
      eventoId: idempotencyKey, // placeholder — evento ainda não criado; atualizado em Plan 04-03
      timestamp: new Date(DateTime).toISOString(),
    };

    const whatsAppPayload: WhatsAppAlertPayload = {
      empresaId: camera.empresaId,
      obraId: camera.obraId, // obra que detectou — tem ConfiguracaoAlerta
      placaNumero: PlateNumber,
      classificacao: placa.classificacao as 'SUSPEITO' | 'CRITICO',
      obraDetectadaNome: camera.obra.nome,
      obraClassificacaoNome,
      timestamp: new Date(DateTime).toISOString(),
    };

    // Enfileira os dois jobs — alert-worker processa com concorrência 1 (ALERTS-03)
    await Promise.all([
      alertQueue.add('alert:cross-site', { type: 'alert:cross-site', payload: crossSitePayload }),
      alertQueue.add('alert:whatsapp', { type: 'alert:whatsapp', payload: whatsAppPayload }),
    ]);
  } else if (isHighRisk && !placa.obraClassificacaoId) {
    // Placa de alto risco sem obra de classificação registrada — envia WhatsApp mas sem overlay cross-site
    const whatsAppPayload: WhatsAppAlertPayload = {
      empresaId: camera.empresaId,
      obraId: camera.obraId,
      placaNumero: PlateNumber,
      classificacao: placa.classificacao as 'SUSPEITO' | 'CRITICO',
      obraDetectadaNome: camera.obra.nome,
      obraClassificacaoNome: 'Não especificada',
      timestamp: new Date(DateTime).toISOString(),
    };
    await alertQueue.add('alert:whatsapp', { type: 'alert:whatsapp', payload: whatsAppPayload });
  }
  // ─────────────────────────────────────────────────────────────────────────
```

4. Abrir apps/api/src/routes/placas.ts e adicionar atualização de obraClassificacaoId.
   Na função do PATCH '/:placaId/classificacao', após a linha `data: { classificacao: classificacaoFinal, ...`:

```typescript
// Adicionar ao objeto data do update:
...(
  (classificacaoFinal === 'SUSPEITO' || classificacaoFinal === 'CRITICO')
    ? { obraClassificacaoId: req.body.obraId ?? null }
    : {}
),
```

ATENÇÃO: O frontend deve enviar `obraId` no body do PATCH quando classifica. Como o body atual não tem obraId, buscar via query alternativa. Verificar se req.body.obraId está disponível, caso não, usar null (o cross-site detection é melhor esforço — quando o admin classificar a partir de um evento, o evento tem obraId).

ALTERNATIVA (mais robusta): Ao invés de depender do body, buscar o obraId do evento mais recente da placa ao fazer a classificação:

```typescript
// Após buscar placaAtual, buscar evento mais recente para inferir obra
const ultimoEvento = await req.tenantClient!.evento.findFirst({
  where: { placaId: placaId },
  orderBy: { timestamp: 'desc' },
  select: { obraId: true },
});

// No update:
data: {
  classificacao: classificacaoFinal,
  observacao: observacao?.trim() || null,
  ...(
    (classificacaoFinal === 'SUSPEITO' || classificacaoFinal === 'CRITICO') && ultimoEvento
      ? { obraClassificacaoId: ultimoEvento.obraId }
      : {}
  ),
},
```

5. Verificar se tests existentes de worker.test.ts ainda passam:
   `cd apps/api && npx vitest run src/jobs/worker.test.ts`

6. Registrar o alertWorker no index.ts para que seja inicializado junto com o lprWorker:
```typescript
// Em apps/api/src/index.ts, dentro do bloco if (process.env.NODE_ENV !== 'test'):
import('./jobs/worker');
import('./jobs/alert-worker'); // Adicionar esta linha
```
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/jobs/worker.test.ts src/routes/placas.test.ts</automated>
  </verify>
  <done>Tests existentes de worker e placas continuam passando. worker.ts tem lógica cross-site com enfileiramento condicional. placas.ts atualiza obraClassificacaoId para SUSPEITO/CRITICO. alert-worker importado em index.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BullMQ job data → processAlertJob | Payload deserializado do Redis — validar tipos antes de usar |
| Redis dedup key | Chave computada de valores do sistema (não do payload externo) — segura |
| prisma.configuracaoAlerta | Query já isolada por obraId e empresaId do evento (não do payload) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | CrossSiteAlertPayload empresaId injetado | mitigate | empresaId vem de camera.empresaId (DB source), nunca de payload externo — mesma proteção do worker existente |
| T-04-06 | Denial of Service | alertQueue sobrecarregada por câmera spammando | mitigate | Dedup Redis 5-15min por (empresaId, placa) — máximo 1 alert:whatsapp por janela |
| T-04-07 | Information Disclosure | Conteúdo da mensagem WhatsApp vaza contexto interno | accept | Mensagem contém apenas: placa, nível, nome das obras — sem IDs internos, IPs, ou dados sensíveis |
| T-04-08 | Repudiation | Sem log de qual job enviou qual WhatsApp | mitigate | console.log com telefone e messageId em cada envio — suficiente para v1 |
</threat_model>

<verification>
Após execução do plano:

1. `cd apps/api && npx vitest run src/jobs/alert-worker.test.ts` — todos os testes passando
2. `cd apps/api && npx vitest run src/jobs/worker.test.ts` — testes existentes ainda passando
3. `cd apps/api && npx vitest run src/routes/placas.test.ts` — testes existentes ainda passando
4. `grep "alert:cross-site" apps/api/src/jobs/worker.ts` — encontra enfileiramento
5. `grep "alertWorker" apps/api/src/index.ts` — confirma registro no bootstrap
</verification>

<success_criteria>
- alert-worker.ts com processAlertJob testável, checkAndSetDedup com TTL correto por nível
- worker.ts detecta cross-site e enfileira alert:cross-site + alert:whatsapp
- placas.ts atualiza obraClassificacaoId ao classificar SUSPEITO/CRITICO
- Todos os testes de worker, placas e alert-worker passando
- alertWorker registrado no bootstrap do app (index.ts)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-cross-site-intelligence-whatsapp-alerts/04-02-SUMMARY.md` com:
- Lógica exata de detecção cross-site implementada (condição isCrossSite)
- TTLs de dedup confirmados (300s SUSPEITO, 900s CRITICO)
- Como obraClassificacaoId é setado (via placas.ts usando último evento)
- Nomes dos jobs na fila ('alert:cross-site', 'alert:whatsapp')
</output>
