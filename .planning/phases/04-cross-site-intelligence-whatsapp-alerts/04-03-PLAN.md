---
id: "04-03"
name: "Socket.IO Cross-Site Alert Emission"
phase: "4"
plan: 3
type: execute
wave: 3
autonomous: true
objective: "Adicionar emitAlertaCrossSite ao servidor Socket.IO, estender o tipo de evento em emitToEmpresa, e adicionar o novo evento ao union de tipos do server.ts para INTEL-04"
requirements: [INTEL-03, INTEL-04, ALERTS-02]
files_modified:
  - apps/api/src/realtime/server.ts
  - apps/api/src/realtime/server.test.ts
  - apps/api/src/realtime/dto.ts
depends_on: ["04-02"]

must_haves:
  truths:
    - "emitAlertaCrossSite(empresaId, payload) emite evento 'feed:alerta-cross-site' na sala empresa:{empresaId}"
    - "Payload inclui: placaNumero, classificacao, obraDetectadaNome, obraClassificacaoNome (INTEL-03)"
    - "Nunca emite globalmente — sempre io.to('empresa:{empresaId}').emit(...) (INTEL-04, REALTIME-01)"
    - "Tipo CrossSiteAlertPayload exportado de dto.ts para uso no frontend (Plan 04-04)"
    - "Testes de server.ts existentes continuam passando após modificação"
  artifacts:
    - path: "apps/api/src/realtime/server.ts"
      provides: "emitAlertaCrossSite function"
      exports: ["emitAlertaCrossSite"]
    - path: "apps/api/src/realtime/dto.ts"
      provides: "CrossSiteAlertDTO type"
      exports: ["CrossSiteAlertDTO"]
  key_links:
    - from: "apps/api/src/jobs/alert-worker.ts"
      to: "apps/api/src/realtime/server.ts"
      via: "import { emitAlertaCrossSite } — importado dinamicamente no BullMQ worker"
    - from: "apps/web/src/components/dashboard-client.tsx (Plan 04-04)"
      to: "apps/api/src/realtime/server.ts"
      via: "socket.on('feed:alerta-cross-site', handler)"
---

<objective>
Este plano completa o lado servidor da emissão de alertas cross-site: registra o novo tipo de evento Socket.IO e garante que o payload correto chegue aos clientes conectados.

Purpose: O alert-worker.ts (Plan 04-02) chama emitAlertaCrossSite — esta função precisa existir antes do alert-worker funcionar. Além disso, o tipo CrossSiteAlertDTO exportado de dto.ts é consumido pelo frontend (Plan 04-04) para tipagem do handler Socket.IO. Este plano é pequeno (2 arquivos tocados) mas crítico para fechar o contrato entre backend e frontend.

Output: server.ts com emitAlertaCrossSite, dto.ts com CrossSiteAlertDTO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@apps/api/src/realtime/server.ts
@apps/api/src/realtime/dto.ts
@apps/api/src/realtime/server.test.ts

<interfaces>
<!-- Tipos e contratos que o executor precisa conhecer -->

De apps/api/src/realtime/server.ts (estado atual):
```typescript
// emitToEmpresa aceita apenas estes eventos:
export function emitToEmpresa(
  io: IoLike,
  empresaId: string,
  event: 'feed:evento-novo' | 'feed:placa-classificada' | 'feed:camera-status',
  payload: unknown,
): void

// Funções exportadas atualmente:
export function emitEventoNovo(empresaId: string, payload: unknown): void
export function emitPlacaClassificada(empresaId: string, payload: unknown): void
export function emitCameraStatus(empresaId: string, payload: unknown): void
```

Novo evento a adicionar ao union de emitToEmpresa:
```typescript
event: 'feed:evento-novo' | 'feed:placa-classificada' | 'feed:camera-status' | 'feed:alerta-cross-site'
```

Novo DTO a criar em dto.ts:
```typescript
export type CrossSiteAlertDTO = {
  empresaId: string;
  placaNumero: string;
  classificacao: 'SUSPEITO' | 'CRITICO';
  obraDetectadaId: string;
  obraDetectadaNome: string;
  obraClassificacaoId: string;
  obraClassificacaoNome: string;
  eventoId: string;
  timestamp: string;
};
```

De apps/api/src/jobs/alert-worker.ts (Plan 04-02):
```typescript
// O alert-worker faz import dinâmico:
const { emitAlertaCrossSite } = await import('../realtime/server');
// Assinatura esperada:
emitAlertaCrossSite(empresaId: string, payload: CrossSiteAlertPayload): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar CrossSiteAlertDTO ao dto.ts</name>
  <files>apps/api/src/realtime/dto.ts</files>
  <behavior>
    - CrossSiteAlertDTO exportado como type (não interface) — consistente com FeedItem e CameraStatusItem existentes
    - Campos obrigatórios: empresaId, placaNumero, classificacao ('SUSPEITO' | 'CRITICO'), obraDetectadaId, obraDetectadaNome, obraClassificacaoId, obraClassificacaoNome, eventoId, timestamp
    - Não criar função builder (diferente de eventoToFeedItem) — payload já vem formatado do worker
    - Testes existentes de dto.test.ts devem continuar passando (não modificar tipos existentes)
  </behavior>
  <action>
1. Abrir apps/api/src/realtime/dto.ts

2. Adicionar ao FINAL do arquivo (após calcCameraStatus):

```typescript
/**
 * DTO for cross-site alert events emitted via Socket.IO.
 * INTEL-03: payload must include plate, classification, detected obra, and original obra.
 * Consumed by frontend dashboard-client.tsx for overlay rendering (Plan 04-04).
 */
export type CrossSiteAlertDTO = {
  empresaId: string;
  placaNumero: string;
  classificacao: 'SUSPEITO' | 'CRITICO';
  obraDetectadaId: string;
  obraDetectadaNome: string;
  obraClassificacaoId: string;
  obraClassificacaoNome: string;
  eventoId: string;
  timestamp: string;
};
```

3. Verificar que `cd apps/api && npx vitest run src/realtime/dto.test.ts` ainda passa (sem alteração de comportamento)
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/realtime/dto.test.ts</automated>
  </verify>
  <done>CrossSiteAlertDTO exportado de dto.ts. Todos os testes existentes de dto.test.ts passando.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar emitAlertaCrossSite ao server.ts + testes</name>
  <files>apps/api/src/realtime/server.ts, apps/api/src/realtime/server.test.ts</files>
  <behavior>
    - Adicionar 'feed:alerta-cross-site' ao union de tipos de emitToEmpresa
    - Criar emitAlertaCrossSite(empresaId, payload: CrossSiteAlertDTO): void — análoga a emitEventoNovo
    - Nunca usar io.emit() global — apenas io.to(buildEmpresaRoom(empresaId)).emit(...)
    - Teste: emitAlertaCrossSite chama io.to com room correto e evento 'feed:alerta-cross-site'
    - Teste: emitAlertaCrossSite usa payload tipado CrossSiteAlertDTO
    - Testes existentes de server.test.ts NÃO devem quebrar
  </behavior>
  <action>
1. Abrir apps/api/src/realtime/server.ts

2. Adicionar import do DTO no topo (após imports existentes):
```typescript
import type { CrossSiteAlertDTO } from './dto';
```

3. Modificar o tipo de evento no emitToEmpresa para incluir o novo evento:
```typescript
export function emitToEmpresa(
  io: IoLike,
  empresaId: string,
  event: 'feed:evento-novo' | 'feed:placa-classificada' | 'feed:camera-status' | 'feed:alerta-cross-site',
  payload: unknown,
): void {
  io.to(buildEmpresaRoom(empresaId)).emit(event, payload);
}
```

4. Adicionar a nova função de emissão após emitCameraStatus:
```typescript
/**
 * Emits a cross-site alert to all operators of the empresa.
 * INTEL-04: always room-scoped, never io.emit() global.
 * INTEL-03: payload includes plate, classification, detected obra, and original obra.
 */
export function emitAlertaCrossSite(empresaId: string, payload: CrossSiteAlertDTO): void {
  emitToEmpresa(getRealtimeServer(), empresaId, 'feed:alerta-cross-site', payload);
}
```

5. Abrir apps/api/src/realtime/server.test.ts e adicionar testes para emitAlertaCrossSite.

Verificar padrão existente dos testes de server.test.ts e adicionar ao final da suite:

```typescript
describe('emitAlertaCrossSite', () => {
  it('emits feed:alerta-cross-site to empresa room', () => {
    const mockEmit = vi.fn();
    const mockTo = vi.fn().mockReturnValue({ emit: mockEmit });
    const mockIo = { to: mockTo } as any;

    // Inject mock io via the module's ioInstance (same pattern as other tests in server.test.ts)
    // If existing tests use a different pattern for mocking ioInstance, follow that pattern.

    const payload: CrossSiteAlertDTO = {
      empresaId: 'emp-1',
      placaNumero: 'ABC1234',
      classificacao: 'SUSPEITO',
      obraDetectadaId: 'obra-b',
      obraDetectadaNome: 'Obra B',
      obraClassificacaoId: 'obra-a',
      obraClassificacaoNome: 'Obra A',
      eventoId: 'evt-1',
      timestamp: new Date().toISOString(),
    };

    // Use emitToEmpresa directly to test routing (avoids needing ioInstance)
    emitToEmpresa(mockIo, 'emp-1', 'feed:alerta-cross-site', payload);

    expect(mockTo).toHaveBeenCalledWith('empresa:emp-1');
    expect(mockEmit).toHaveBeenCalledWith('feed:alerta-cross-site', payload);
  });
});
```

NOTA: Verificar o padrão exato usado nos testes existentes de server.test.ts antes de escrever o teste. Se usam spies ou mocks diferentes, adaptar o teste para seguir o mesmo padrão.

6. Executar todos os testes de realtime:
   `cd apps/api && npx vitest run src/realtime/`
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/realtime/</automated>
  </verify>
  <done>emitAlertaCrossSite exportada de server.ts. Todos os testes de realtime/ passando incluindo o novo teste de emitAlertaCrossSite. Tipo 'feed:alerta-cross-site' no union de emitToEmpresa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alert-worker → Socket.IO | Chamada interna — payload vem do job BullMQ (confiável, originado do worker LPR) |
| Socket.IO → client browser | Payload de alerta emitido para sala da empresa — client não pode injetar dados nesta direção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | CrossSiteAlertDTO inclui obraClassificacaoId | accept | IDs internos não são dados sensíveis; necessários para o frontend construir o link "Ver obra A" |
| T-04-10 | Tampering | Cliente manipula socket para emitir feed:alerta-cross-site | mitigate | Servidor nunca processa eventos emitidos pelo cliente para este canal — apenas emite. Socket.IO rooms são server-controlled. |
</threat_model>

<verification>
Após execução do plano:

1. `cd apps/api && npx vitest run src/realtime/` — todos os testes passando
2. `grep "emitAlertaCrossSite" apps/api/src/realtime/server.ts` — função existe
3. `grep "CrossSiteAlertDTO" apps/api/src/realtime/dto.ts` — type exportado
4. `grep "feed:alerta-cross-site" apps/api/src/realtime/server.ts` — evento no union de tipos
5. TypeScript build: `cd apps/api && npx tsc --noEmit` — zero erros de tipo
</verification>

<success_criteria>
- emitAlertaCrossSite exportada e tipada com CrossSiteAlertDTO
- 'feed:alerta-cross-site' no union de tipos de emitToEmpresa
- CrossSiteAlertDTO em dto.ts disponível para importação pelo frontend
- Todos os testes de realtime/ passando sem regressão
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-cross-site-intelligence-whatsapp-alerts/04-03-SUMMARY.md` com:
- Nome exato do evento Socket.IO: 'feed:alerta-cross-site'
- Tipo CrossSiteAlertDTO exportado de apps/api/src/realtime/dto.ts
- Campos do payload (para referência do Plan 04-04 ao implementar o handler frontend)
</output>
