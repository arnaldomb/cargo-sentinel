---
id: "04-01"
name: "Schema + Evolution API Service"
phase: "4"
plan: 1
type: execute
wave: 1
autonomous: true
objective: "Adicionar campo obraClassificacaoId ao modelo Placa, criar ConfiguracaoAlerta para números WhatsApp por obra, adicionar Evolution API v2.3.7 ao docker-compose e criar wrapper do serviço WhatsApp"
requirements: [INTEL-01, INTEL-02, ALERTS-01, ALERTS-06]
files_modified:
  - packages/database/prisma/schema.prisma
  - docker-compose.yml
  - apps/api/src/services/whatsapp.ts
  - apps/api/src/services/whatsapp.test.ts
depends_on: []

must_haves:
  truths:
    - "Prisma schema tem campo obraClassificacaoId em Placa — rastreia a obra onde a placa foi classificada pela primeira vez"
    - "ConfiguracaoAlerta model existe com telefone, obraId, empresaId e campo ativo"
    - "Evolution API container está no docker-compose.yml com tag 2.3.7 hard-pinned"
    - "whatsapp.ts exporta sendAlertaWhatsApp(telefone, mensagem) que chama Evolution API"
    - "whatsapp.ts é testável com variável de ambiente EVOLUTION_API_URL não definida (não lança em import)"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "ConfiguracaoAlerta model + campo obraClassificacaoId em Placa"
      contains: "model ConfiguracaoAlerta"
    - path: "docker-compose.yml"
      provides: "Evolution API v2.3.7 service"
      contains: "atendai/chatwoot-evolution-api:2.3.7"
    - path: "apps/api/src/services/whatsapp.ts"
      provides: "Wrapper do serviço WhatsApp"
      exports: ["sendAlertaWhatsApp"]
  key_links:
    - from: "apps/api/src/jobs/alert-worker.ts (Plan 04-02)"
      to: "apps/api/src/services/whatsapp.ts"
      via: "import { sendAlertaWhatsApp }"
    - from: "apps/api/src/jobs/worker.ts (Plan 04-02)"
      to: "packages/database/prisma/schema.prisma"
      via: "placa.obraClassificacaoId para detecção cross-site"
---

<objective>
Este plano cria a fundação de dados e serviços necessários para a inteligência cross-site e alertas WhatsApp.

Purpose: O campo `obraClassificacaoId` em `Placa` é o mecanismo central de detecção cross-site — sem ele não conseguimos comparar a obra de classificação com a obra de detecção atual. O modelo `ConfiguracaoAlerta` permite que admins configurem números WhatsApp por obra (ALERTS-06). O container Evolution API v2.3.7 é o canal de entrega de mensagens (ALERTS-01). O service wrapper encapsula a lógica de chamada HTTP para facilitar testes.

Output: Schema migrado, docker-compose.yml com Evolution API, apps/api/src/services/whatsapp.ts funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/database/prisma/schema.prisma
@docker-compose.yml
@apps/api/src/services/redis.ts

<interfaces>
<!-- Tipos existentes que o executor precisa conhecer -->

Do packages/database/prisma/schema.prisma (atual):
```prisma
model Placa {
  id            String        @id @default(cuid())
  numero        String
  empresaId     String
  classificacao Classificacao @default(VISITANTE)
  // ... outros campos
  @@unique([numero, empresaId])
  @@index([empresaId, classificacao])
}
```

Novo campo a adicionar em Placa:
```prisma
obraClassificacaoId String?  // obraId onde a classificação manual SUSPEITO/CRITICO foi registrada
obraClassificacao   Obra?    @relation("PlacaClassificacaoObra", fields: [obraClassificacaoId], references: [id])
```

Novo model a criar:
```prisma
model ConfiguracaoAlerta {
  id        String   @id @default(cuid())
  obraId    String
  empresaId String
  telefone  String   // E.164: +5511999999999
  ativo     Boolean  @default(true)
  createdAt DateTime @default(now())
  obra      Obra     @relation(fields: [obraId], references: [id])
  empresa   Empresa  @relation(fields: [empresaId], references: [id])

  @@unique([obraId, telefone])
  @@index([empresaId])
  @@index([obraId])
}
```

Interface do serviço WhatsApp a criar:
```typescript
// apps/api/src/services/whatsapp.ts
export interface WhatsAppSendResult {
  success: boolean;
  messageId?: string;
  error?: string;
}

export async function sendAlertaWhatsApp(
  telefone: string,
  mensagem: string,
): Promise<WhatsAppSendResult>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar schema Prisma — obraClassificacaoId + ConfiguracaoAlerta</name>
  <files>packages/database/prisma/schema.prisma</files>
  <behavior>
    - Placa.obraClassificacaoId é opcional (String?) — placas antigas não têm valor e continuam funcionando
    - ConfiguracaoAlerta tem @@unique([obraId, telefone]) — ALERTS-05: sem duplicata de número por obra
    - Obra model recebe relação inversa configuracoes e placasClassificadas para manter integridade referencial
    - Empresa model recebe relação inversa configuracoes
    - `prisma db push` aplicado ao final para validar schema contra postgres local
  </behavior>
  <action>
1. Abrir packages/database/prisma/schema.prisma

2. No model Placa, adicionar ANTES do fechamento do model (mas após o campo `updatedAt`):
```prisma
obraClassificacaoId String?
obraClassificacao   Obra?   @relation("PlacaClassificacaoObra", fields: [obraClassificacaoId], references: [id])
```

3. No model Obra, adicionar relações inversas:
```prisma
configuracoes       ConfiguracaoAlerta[]
placasClassificadas Placa[] @relation("PlacaClassificacaoObra")
```

4. No model Empresa, adicionar relação inversa:
```prisma
configuracoes ConfiguracaoAlerta[]
```

5. Adicionar novo model APÓS o model Evento (antes do fechamento do arquivo):
```prisma
model ConfiguracaoAlerta {
  id        String   @id @default(cuid())
  obraId    String
  empresaId String
  telefone  String
  ativo     Boolean  @default(true)
  createdAt DateTime @default(now())
  obra      Obra     @relation(fields: [obraId], references: [id])
  empresa   Empresa  @relation(fields: [empresaId], references: [id])

  @@unique([obraId, telefone])
  @@index([empresaId])
  @@index([obraId])
}
```

6. Executar: `cd packages/database && npx prisma db push` para validar schema
   - Se postgres não está rodando localmente: `npx prisma validate` valida sem conexão

7. Executar: `cd packages/database && npx prisma generate` para regenerar o client
  </action>
  <verify>
    <automated>cd packages/database && npx prisma validate</automated>
  </verify>
  <done>Schema válido, ConfiguracaoAlerta model existe, Placa tem obraClassificacaoId, `npx prisma validate` retorna sem erros</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evolution API v2.3.7 no docker-compose + variáveis de ambiente</name>
  <files>docker-compose.yml</files>
  <behavior>
    - Tag DEVE ser `2.3.7` — NUNCA `latest` (ALERTS-01, decisão hard-pinned)
    - Container não exposto via Traefik — apenas acessível internamente pela rede `sentinel`
    - api container precisa da env var EVOLUTION_API_URL=http://evolution-api:8080
    - Evolution API precisa de volume para persistência de sessão WhatsApp
    - Porta interna 8080 — não exposta na rede do host por padrão (segurança)
  </behavior>
  <action>
1. Abrir docker-compose.yml

2. Adicionar service `evolution-api` ANTES do service `traefik` (após o service `web`):
```yaml
  # ============================================================
  # evolution-api — WhatsApp messaging via Evolution API v2.3.7
  # HARD-PINNED: v2.3.7 — DO NOT upgrade to 2.4.0+ (requires
  # external license server activation — breaks self-hosted SLAs)
  # ALERTS-01: internal only, not exposed via Traefik
  # ============================================================
  evolution-api:
    image: atendai/evolution-api:2.3.7
    restart: unless-stopped
    environment:
      SERVER_URL: ${EVOLUTION_SERVER_URL:-http://evolution-api:8080}
      AUTHENTICATION_TYPE: apikey
      AUTHENTICATION_API_KEY: ${EVOLUTION_API_KEY}
      DATABASE_ENABLED: "false"
      REDIS_ENABLED: "true"
      REDIS_URI: redis://redis:6379
      REDIS_PREFIX_KEY: evolution
      WEBHOOK_GLOBAL_ENABLED: "false"
      STORE_MESSAGES: "false"
      STORE_MESSAGE_UP: "false"
      STORE_CONTACTS: "false"
      STORE_CHATS: "false"
      DEL_INSTANCE: "false"
    volumes:
      - evolution-instances:/evolution/instances
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - sentinel
```

3. No service `api`, adicionar env var para apontar para o container:
```yaml
      EVOLUTION_API_URL: http://evolution-api:8080
      EVOLUTION_API_KEY: ${EVOLUTION_API_KEY}
      EVOLUTION_INSTANCE_NAME: ${EVOLUTION_INSTANCE_NAME:-cargo-sentinel}
```

4. Na seção `volumes:` (no final do arquivo), adicionar:
```yaml
  evolution-instances:
```

NOTA IMPORTANTE: Imagem correta da Evolution API v2.3.7 é `atendai/evolution-api:2.3.7`. Se não existir, verificar em https://hub.docker.com/r/atendai/evolution-api/tags e usar a tag exata disponível. Nunca usar `latest`.
  </action>
  <verify>
    <automated>grep -c "evolution-api:2.3.7" docker-compose.yml</automated>
  </verify>
  <done>docker-compose.yml contém serviço `evolution-api` com imagem pinada em 2.3.7, volume `evolution-instances` declarado, api service tem EVOLUTION_API_URL e EVOLUTION_API_KEY nas env vars</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WhatsApp service wrapper com testes</name>
  <files>apps/api/src/services/whatsapp.ts, apps/api/src/services/whatsapp.test.ts</files>
  <behavior>
    - sendAlertaWhatsApp(telefone, mensagem) faz POST para Evolution API REST endpoint
    - Retorna WhatsAppSendResult — nunca lança exceção (caller não sabe tratar; falhas são logadas)
    - Se EVOLUTION_API_URL não está definida, retorna { success: false, error: "EVOLUTION_API_URL not configured" }
    - Telefone no formato E.164 sem o "+" para a API: "+5511999999999" → "5511999999999"
    - Testa: env var ausente retorna false, chamada bem-sucedida retorna true com messageId, erro HTTP retorna false com error string
    - Usa fetch nativo (Node 18+) — sem axios (não está no package.json da api)
  </behavior>
  <action>
Criar apps/api/src/services/whatsapp.ts:

```typescript
/**
 * WhatsApp service wrapper for Evolution API v2.3.7
 *
 * ALERTS-01: Evolution API v2.3.7 hard-pinned (Docker tag)
 * ALERTS-03: Never call directly from webhook — always via BullMQ alert-worker
 *
 * Does NOT throw — callers must handle WhatsAppSendResult.success === false
 */

export interface WhatsAppSendResult {
  success: boolean;
  messageId?: string;
  error?: string;
}

/**
 * Normalizes phone number to Evolution API format.
 * Strips leading "+" — Evolution API expects digits only with country code.
 * Examples: "+5511999999999" → "5511999999999", "5511999999999" → "5511999999999"
 */
export function normalizePhone(telefone: string): string {
  return telefone.replace(/^\+/, '');
}

/**
 * Sends a WhatsApp text message via Evolution API.
 *
 * @param telefone - Phone number in E.164 format ("+5511999999999")
 * @param mensagem - Plain text message body
 */
export async function sendAlertaWhatsApp(
  telefone: string,
  mensagem: string,
): Promise<WhatsAppSendResult> {
  const apiUrl = process.env.EVOLUTION_API_URL;
  const apiKey = process.env.EVOLUTION_API_KEY;
  const instanceName = process.env.EVOLUTION_INSTANCE_NAME ?? 'cargo-sentinel';

  if (!apiUrl) {
    return { success: false, error: 'EVOLUTION_API_URL not configured' };
  }

  if (!apiKey) {
    return { success: false, error: 'EVOLUTION_API_KEY not configured' };
  }

  const normalizedPhone = normalizePhone(telefone);
  const endpoint = `${apiUrl}/message/sendText/${instanceName}`;

  try {
    const response = await fetch(endpoint, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        apikey: apiKey,
      },
      body: JSON.stringify({
        number: normalizedPhone,
        text: mensagem,
      }),
    });

    if (!response.ok) {
      const body = await response.text().catch(() => '');
      return {
        success: false,
        error: `Evolution API returned ${response.status}: ${body.slice(0, 200)}`,
      };
    }

    const data = (await response.json()) as { key?: { id?: string } };
    return {
      success: true,
      messageId: data?.key?.id,
    };
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err);
    return { success: false, error: `Network error: ${message}` };
  }
}
```

Criar apps/api/src/services/whatsapp.test.ts:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { normalizePhone, sendAlertaWhatsApp } from './whatsapp';

describe('normalizePhone', () => {
  it('strips leading + from E.164 number', () => {
    expect(normalizePhone('+5511999999999')).toBe('5511999999999');
  });

  it('leaves number without + unchanged', () => {
    expect(normalizePhone('5511999999999')).toBe('5511999999999');
  });
});

describe('sendAlertaWhatsApp', () => {
  const originalEnv = process.env;

  beforeEach(() => {
    vi.resetAllMocks();
    process.env = { ...originalEnv };
  });

  afterEach(() => {
    process.env = originalEnv;
  });

  it('returns failure when EVOLUTION_API_URL is not set', async () => {
    delete process.env.EVOLUTION_API_URL;
    const result = await sendAlertaWhatsApp('+5511999999999', 'Alerta teste');
    expect(result.success).toBe(false);
    expect(result.error).toContain('EVOLUTION_API_URL not configured');
  });

  it('returns failure when EVOLUTION_API_KEY is not set', async () => {
    process.env.EVOLUTION_API_URL = 'http://localhost:8080';
    delete process.env.EVOLUTION_API_KEY;
    const result = await sendAlertaWhatsApp('+5511999999999', 'Alerta teste');
    expect(result.success).toBe(false);
    expect(result.error).toContain('EVOLUTION_API_KEY not configured');
  });

  it('returns success with messageId on 200 response', async () => {
    process.env.EVOLUTION_API_URL = 'http://localhost:8080';
    process.env.EVOLUTION_API_KEY = 'test-key';
    process.env.EVOLUTION_INSTANCE_NAME = 'test-instance';

    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue({
        ok: true,
        json: async () => ({ key: { id: 'msg-abc-123' } }),
      }),
    );

    const result = await sendAlertaWhatsApp('+5511999999999', 'Alerta: placa ABC1234');
    expect(result.success).toBe(true);
    expect(result.messageId).toBe('msg-abc-123');

    const fetchMock = vi.mocked(fetch);
    expect(fetchMock).toHaveBeenCalledWith(
      'http://localhost:8080/message/sendText/test-instance',
      expect.objectContaining({
        method: 'POST',
        body: JSON.stringify({ number: '5511999999999', text: 'Alerta: placa ABC1234' }),
      }),
    );
  });

  it('returns failure with error string on non-200 HTTP response', async () => {
    process.env.EVOLUTION_API_URL = 'http://localhost:8080';
    process.env.EVOLUTION_API_KEY = 'test-key';

    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue({
        ok: false,
        status: 404,
        text: async () => 'Instance not found',
      }),
    );

    const result = await sendAlertaWhatsApp('+5511999999999', 'Alerta teste');
    expect(result.success).toBe(false);
    expect(result.error).toContain('404');
  });

  it('returns failure on network error', async () => {
    process.env.EVOLUTION_API_URL = 'http://localhost:8080';
    process.env.EVOLUTION_API_KEY = 'test-key';

    vi.stubGlobal('fetch', vi.fn().mockRejectedValue(new Error('ECONNREFUSED')));

    const result = await sendAlertaWhatsApp('+5511999999999', 'Alerta teste');
    expect(result.success).toBe(false);
    expect(result.error).toContain('ECONNREFUSED');
  });
});
```

Executar testes: `cd apps/api && npx vitest run src/services/whatsapp.test.ts`
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/services/whatsapp.test.ts</automated>
  </verify>
  <done>Todos os 6 testes passando: normalizePhone (2), sendAlertaWhatsApp (4). whatsapp.ts exporta sendAlertaWhatsApp e normalizePhone. Nenhuma exceção lançada em cenários de erro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api → Evolution API | Chamada HTTP interna com apikey no header — chave em env var, não no código |
| Evolution API → WhatsApp | Gerenciado pela Evolution API; fora do escopo do nosso código |
| Admin → ConfiguracaoAlerta | Apenas ADMIN_EMPRESA pode configurar números (enforced em Plan 04-04 via requireRole) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | EVOLUTION_API_KEY em docker-compose.yml | mitigate | Usar variável de ambiente `${EVOLUTION_API_KEY}` — nunca hardcodar. Documentar em .env.example |
| T-04-02 | Tampering | obraClassificacaoId set incorretamente | mitigate | Campo só é atualizado no worker.ts ao reclassificar (Plan 04-02) — não aceito de payload externo |
| T-04-03 | Elevation of Privilege | ConfiguracaoAlerta acessível por OPERADOR | mitigate | requireRole('ADMIN_EMPRESA') no endpoint de configuração (Plan 04-04) |
| T-04-04 | Denial of Service | Evolution API container exposto publicamente | accept | Container sem labels Traefik — acessível apenas na rede Docker `sentinel` interna |
</threat_model>

<verification>
Após execução do plano:

1. `npx prisma validate` (em packages/database) retorna sem erros
2. `grep "ConfiguracaoAlerta" packages/database/prisma/schema.prisma` encontra o model
3. `grep "obraClassificacaoId" packages/database/prisma/schema.prisma` encontra o campo em Placa
4. `grep "2.3.7" docker-compose.yml` encontra a imagem da Evolution API pinada
5. `cd apps/api && npx vitest run src/services/whatsapp.test.ts` — todos os testes passando
</verification>

<success_criteria>
- Schema Prisma válido com ConfiguracaoAlerta model e obraClassificacaoId em Placa
- docker-compose.yml com Evolution API v2.3.7 (NUNCA latest) na rede sentinel interna
- whatsapp.ts com sendAlertaWhatsApp testável e que nunca lança exceção
- 6 testes de unidade passando para o serviço WhatsApp
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-cross-site-intelligence-whatsapp-alerts/04-01-SUMMARY.md` com:
- Campos adicionados ao schema (obraClassificacaoId, ConfiguracaoAlerta)
- Tag exata da imagem Evolution API confirmada
- Padrão de retorno WhatsAppSendResult documentado para Plans 04-02/04-03
</output>
