---
id: "04-04"
name: "Frontend Alert Overlay + Admin WhatsApp Config UI"
phase: "4"
plan: 4
type: execute
wave: 4
autonomous: true
objective: "Overlay full-screen no dashboard ao receber feed:alerta-cross-site via Socket.IO (INTEL-05), com auto-dismiss 30s e botão de dispensar; página de admin para configurar números WhatsApp por obra (ALERTS-06)"
requirements: [INTEL-03, INTEL-05, ALERTS-06]
files_modified:
  - apps/web/src/components/cross-site-alert-overlay.tsx
  - apps/web/src/components/cross-site-alert-overlay.test.tsx
  - apps/web/src/components/dashboard-client.tsx
  - apps/web/src/app/(admin)/configuracoes/alertas/page.tsx
  - apps/web/src/app/(admin)/configuracoes/alertas/alertas-client.tsx
depends_on: ["04-03"]

must_haves:
  truths:
    - "Ao receber evento Socket.IO 'feed:alerta-cross-site', overlay ocupa tela cheia com z-index máximo"
    - "Overlay exibe: placa, nível de classificação, obra detectada, obra de classificação original (INTEL-03)"
    - "Overlay tem botão 'Dispensar' e auto-dismiss após 30 segundos com countdown visível"
    - "Badge de classificação no overlay usa as mesmas cores da spec: orange para SUSPEITO, red para CRITICO"
    - "Admin Empresa pode adicionar/remover números WhatsApp por obra em /configuracoes/alertas (ALERTS-06)"
    - "Número salvo em formato E.164 — input com máscara ou validação de formato"
  artifacts:
    - path: "apps/web/src/components/cross-site-alert-overlay.tsx"
      provides: "Componente de overlay de alerta cross-site"
      exports: ["CrossSiteAlertOverlay"]
    - path: "apps/web/src/app/(admin)/configuracoes/alertas/page.tsx"
      provides: "Página de configuração de alertas"
    - path: "apps/web/src/app/(admin)/configuracoes/alertas/alertas-client.tsx"
      provides: "Client component com CRUD de números WhatsApp"
  key_links:
    - from: "apps/web/src/components/dashboard-client.tsx"
      to: "apps/web/src/components/cross-site-alert-overlay.tsx"
      via: "socket.on('feed:alerta-cross-site') → setState → <CrossSiteAlertOverlay />"
    - from: "apps/web/src/app/(admin)/configuracoes/alertas/alertas-client.tsx"
      to: "apps/api/src/routes/configuracoes-alerta.ts (a criar)"
      via: "fetch POST/DELETE /api/configuracoes-alerta"
---

<objective>
Este plano fecha o ciclo visível da feature: o operador vê o alerta cross-site em tempo real, e o admin pode gerenciar os números de contato para notificações WhatsApp.

Purpose: Sem o overlay (INTEL-05), os alertas cross-site existem no backend mas são invisíveis para o operador — a feature não entrega valor. Sem a UI de admin (ALERTS-06), não há como configurar para qual número o WhatsApp é enviado — o sistema não funciona end-to-end. Este plano fecha ambas as lacunas.

Output: CrossSiteAlertOverlay renderizado no dashboard, página /configuracoes/alertas com CRUD de números WhatsApp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@apps/web/src/components/dashboard-client.tsx
@apps/web/src/components/critical-confirm-dialog.tsx
@apps/web/src/components/classification-badge.tsx

<interfaces>
<!-- Tipos e contratos que o executor precisa conhecer -->

Evento Socket.IO a escutar (de Plan 04-03):
```typescript
// Evento: 'feed:alerta-cross-site'
// Payload type: CrossSiteAlertDTO
type CrossSiteAlertDTO = {
  empresaId: string;
  placaNumero: string;
  classificacao: 'SUSPEITO' | 'CRITICO';
  obraDetectadaId: string;
  obraDetectadaNome: string;
  obraClassificacaoId: string;
  obraClassificacaoNome: string;
  eventoId: string;
  timestamp: string;
};
```

Cores de badge por classificação (do CLAUDE.md e Phase 3):
```
SUSPEITO: #ea580c (orange-600 — Tailwind: text-orange-600 / bg-orange-100)
CRITICO:  #b91c1c (red-700   — Tailwind: text-red-700 / bg-red-100)
```

Padrão do CriticalConfirmDialog (componente de referência para estilo do overlay):
- Usa `fixed inset-0 z-50 flex items-center justify-center`
- Background: `bg-black/60 backdrop-blur-sm`
- Card: `bg-white rounded-2xl shadow-2xl p-8 max-w-md w-full mx-4`

Padrão de classificação existente de dashboard-client.tsx:
```typescript
// Estado para controle de overlay já existe como pattern:
const [confirmDialog, setConfirmDialog] = useState<{...} | null>(null);
// Novo estado a adicionar:
const [crossSiteAlert, setCrossSiteAlert] = useState<CrossSiteAlertDTO | null>(null);
```

Stack de autenticação: Auth.js v5 — session disponível via useSession() ou passada como prop.

API endpoints a criar no backend (Express) para ConfiguracaoAlerta:
```
GET    /api/configuracoes-alerta?obraId={id}   → lista configurações da obra
POST   /api/configuracoes-alerta               → { obraId, telefone }
DELETE /api/configuracoes-alerta/:id           → remove configuração
```
NOTA: O executor deve criar apps/api/src/routes/configuracoes-alerta.ts e registrar em index.ts.
      Este arquivo NÃO está em files_modified — adicionar se necessário. (O planner incluiu apenas
      os arquivos frontend que são o foco deste plan; o executor deve criar o backend route também.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CrossSiteAlertOverlay component + integração no dashboard</name>
  <files>apps/web/src/components/cross-site-alert-overlay.tsx, apps/web/src/components/cross-site-alert-overlay.test.tsx, apps/web/src/components/dashboard-client.tsx</files>
  <behavior>
    - Overlay renderizado via portal (fixed inset-0 z-[100]) acima de tudo
    - Countdown de 30s visível — número decrescendo a cada segundo via setInterval
    - Auto-dismiss quando countdown chega a 0 (clearInterval + setCrossSiteAlert(null))
    - Botão "Dispensar" fecha imediatamente (setCrossSiteAlert(null) + limpa intervalo)
    - Cores: SUSPEITO → bg-orange-500, CRITICO → bg-red-700 no header do card
    - Informações obrigatórias exibidas (INTEL-03): placa, classificação, obra detectada, obra original
    - Apenas 1 alerta por vez — novo alerta SUBSTITUI o anterior (não empilha)
    - Testes: renderiza com payload SUSPEITO, renderiza com CRITICO, botão dispensar chama onDismiss, auto-dismiss dispara após 30s (vi.useFakeTimers)
    - dashboard-client.tsx: adicionar socket.on('feed:alerta-cross-site') e state crossSiteAlert
  </behavior>
  <action>
1. Criar apps/web/src/components/cross-site-alert-overlay.tsx:

```tsx
'use client';

import { useEffect, useRef, useState } from 'react';

export type CrossSiteAlertDTO = {
  empresaId: string;
  placaNumero: string;
  classificacao: 'SUSPEITO' | 'CRITICO';
  obraDetectadaId: string;
  obraDetectadaNome: string;
  obraClassificacaoId: string;
  obraClassificacaoNome: string;
  eventoId: string;
  timestamp: string;
};

type CrossSiteAlertOverlayProps = {
  alert: CrossSiteAlertDTO;
  onDismiss: () => void;
  /** Countdown duration in seconds. Default: 30. */
  countdownSeconds?: number;
};

const HEADER_BG: Record<CrossSiteAlertDTO['classificacao'], string> = {
  SUSPEITO: 'bg-orange-500',
  CRITICO: 'bg-red-700',
};

const NIVEL_LABEL: Record<CrossSiteAlertDTO['classificacao'], string> = {
  SUSPEITO: 'SUSPEITO',
  CRITICO: 'CRÍTICO',
};

export function CrossSiteAlertOverlay({
  alert,
  onDismiss,
  countdownSeconds = 30,
}: CrossSiteAlertOverlayProps) {
  const [remaining, setRemaining] = useState(countdownSeconds);
  const intervalRef = useRef<ReturnType<typeof setInterval> | null>(null);

  useEffect(() => {
    setRemaining(countdownSeconds);
    intervalRef.current = setInterval(() => {
      setRemaining((prev) => {
        if (prev <= 1) {
          clearInterval(intervalRef.current!);
          onDismiss();
          return 0;
        }
        return prev - 1;
      });
    }, 1000);

    return () => {
      if (intervalRef.current) clearInterval(intervalRef.current);
    };
  }, [alert.eventoId, countdownSeconds, onDismiss]);

  const headerBg = HEADER_BG[alert.classificacao];
  const nivelLabel = NIVEL_LABEL[alert.classificacao];

  return (
    <div
      className="fixed inset-0 z-[100] flex items-center justify-center bg-black/70 backdrop-blur-sm"
      role="alertdialog"
      aria-modal="true"
      aria-label={`Alerta ${nivelLabel}: placa ${alert.placaNumero}`}
      data-testid="cross-site-alert-overlay"
    >
      <div className="mx-4 w-full max-w-lg overflow-hidden rounded-2xl bg-white shadow-2xl">
        {/* Header colorido por nível */}
        <div className={`${headerBg} px-6 py-4 text-white`}>
          <div className="flex items-center justify-between">
            <div className="flex items-center gap-3">
              <span className="text-2xl">⚠️</span>
              <div>
                <p className="text-sm font-medium uppercase tracking-widest opacity-90">
                  Alerta Cross-Site
                </p>
                <p className="text-xl font-bold">{nivelLabel}</p>
              </div>
            </div>
            <span
              className="text-sm font-mono opacity-80"
              data-testid="countdown"
            >
              {remaining}s
            </span>
          </div>
        </div>

        {/* Corpo */}
        <div className="px-6 py-5">
          <p className="mb-4 text-3xl font-bold tracking-widest text-slate-900">
            {alert.placaNumero}
          </p>

          <dl className="space-y-3 text-sm">
            <div>
              <dt className="font-medium text-slate-500">Detectada em</dt>
              <dd className="text-slate-900 font-semibold">{alert.obraDetectadaNome}</dd>
            </div>
            <div>
              <dt className="font-medium text-slate-500">Classificada originalmente em</dt>
              <dd className="text-slate-900 font-semibold">{alert.obraClassificacaoNome}</dd>
            </div>
            <div>
              <dt className="font-medium text-slate-500">Horário</dt>
              <dd className="text-slate-900">
                {new Date(alert.timestamp).toLocaleString('pt-BR')}
              </dd>
            </div>
          </dl>
        </div>

        {/* Rodapé */}
        <div className="border-t border-slate-100 px-6 py-4">
          <button
            onClick={onDismiss}
            className="w-full rounded-lg bg-slate-100 py-2.5 text-sm font-medium text-slate-700 transition-colors hover:bg-slate-200 focus:outline-none focus:ring-2 focus:ring-slate-400"
            data-testid="dismiss-btn"
          >
            Dispensar
          </button>
        </div>
      </div>
    </div>
  );
}
```

2. Criar apps/web/src/components/cross-site-alert-overlay.test.tsx:

```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render, screen, fireEvent, act } from '@testing-library/react';
import { CrossSiteAlertOverlay } from './cross-site-alert-overlay';
import type { CrossSiteAlertDTO } from './cross-site-alert-overlay';

const mockAlert: CrossSiteAlertDTO = {
  empresaId: 'emp-1',
  placaNumero: 'ABC1234',
  classificacao: 'SUSPEITO',
  obraDetectadaId: 'obra-b',
  obraDetectadaNome: 'Obra Beta',
  obraClassificacaoId: 'obra-a',
  obraClassificacaoNome: 'Obra Alpha',
  eventoId: 'evt-1',
  timestamp: new Date('2026-06-21T10:00:00Z').toISOString(),
};

describe('CrossSiteAlertOverlay', () => {
  beforeEach(() => {
    vi.useFakeTimers();
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  it('renders plate number and classification level', () => {
    const onDismiss = vi.fn();
    render(<CrossSiteAlertOverlay alert={mockAlert} onDismiss={onDismiss} />);
    expect(screen.getByText('ABC1234')).toBeDefined();
    expect(screen.getByText('SUSPEITO')).toBeDefined();
  });

  it('renders obra detected and obra classification names', () => {
    const onDismiss = vi.fn();
    render(<CrossSiteAlertOverlay alert={mockAlert} onDismiss={onDismiss} />);
    expect(screen.getByText('Obra Beta')).toBeDefined();
    expect(screen.getByText('Obra Alpha')).toBeDefined();
  });

  it('renders CRÍTICO label for critico classification', () => {
    const onDismiss = vi.fn();
    render(
      <CrossSiteAlertOverlay
        alert={{ ...mockAlert, classificacao: 'CRITICO' }}
        onDismiss={onDismiss}
      />,
    );
    expect(screen.getByText('CRÍTICO')).toBeDefined();
  });

  it('calls onDismiss when dismiss button is clicked', () => {
    const onDismiss = vi.fn();
    render(<CrossSiteAlertOverlay alert={mockAlert} onDismiss={onDismiss} />);
    fireEvent.click(screen.getByTestId('dismiss-btn'));
    expect(onDismiss).toHaveBeenCalledOnce();
  });

  it('auto-dismisses after 30 seconds', () => {
    const onDismiss = vi.fn();
    render(
      <CrossSiteAlertOverlay alert={mockAlert} onDismiss={onDismiss} countdownSeconds={30} />,
    );
    expect(onDismiss).not.toHaveBeenCalled();
    act(() => {
      vi.advanceTimersByTime(30_000);
    });
    expect(onDismiss).toHaveBeenCalledOnce();
  });

  it('shows countdown decrementing', () => {
    const onDismiss = vi.fn();
    render(
      <CrossSiteAlertOverlay alert={mockAlert} onDismiss={onDismiss} countdownSeconds={10} />,
    );
    expect(screen.getByTestId('countdown').textContent).toBe('10s');
    act(() => {
      vi.advanceTimersByTime(3_000);
    });
    expect(screen.getByTestId('countdown').textContent).toBe('7s');
  });
});
```

3. Abrir apps/web/src/components/dashboard-client.tsx e adicionar:

a) Import do componente e tipo:
```tsx
import { CrossSiteAlertOverlay, type CrossSiteAlertDTO } from './cross-site-alert-overlay';
```

b) Estado para o alerta cross-site (após o estado `confirmDialog`):
```tsx
const [crossSiteAlert, setCrossSiteAlert] = useState<CrossSiteAlertDTO | null>(null);
```

c) No useEffect do Socket.IO, após o handler `feed:camera-status`, adicionar:
```tsx
socket.on('feed:alerta-cross-site', (incoming: CrossSiteAlertDTO) => {
  // Novo alerta substitui o anterior (não empilha — INTEL-05)
  setCrossSiteAlert(incoming);
});
```

d) No JSX de retorno, adicionar o overlay APÓS o CriticalConfirmDialog:
```tsx
{/* Cross-site alert overlay — z-[100] acima de tudo (INTEL-05) */}
{crossSiteAlert && (
  <CrossSiteAlertOverlay
    alert={crossSiteAlert}
    onDismiss={() => setCrossSiteAlert(null)}
  />
)}
```

4. Executar testes do overlay: `cd apps/web && npx vitest run src/components/cross-site-alert-overlay.test.tsx`
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/cross-site-alert-overlay.test.tsx</automated>
  </verify>
  <done>Todos os 6 testes do overlay passando. CrossSiteAlertOverlay renderiza placa, classificação, obras. Auto-dismiss em 30s funcional. dashboard-client.tsx ouve 'feed:alerta-cross-site' e renderiza o overlay.</done>
</task>

<task type="auto">
  <name>Task 2: Backend CRUD + UI admin para configuração de alertas WhatsApp</name>
  <files>apps/api/src/routes/configuracoes-alerta.ts, apps/api/src/index.ts, apps/web/src/app/(admin)/configuracoes/alertas/page.tsx, apps/web/src/app/(admin)/configuracoes/alertas/alertas-client.tsx</files>
  <behavior>
    - Backend: GET /api/configuracoes-alerta?obraId=X retorna lista de telefones ativos da obra
    - Backend: POST /api/configuracoes-alerta { obraId, telefone } cria novo (validação E.164)
    - Backend: DELETE /api/configuracoes-alerta/:id remove configuração
    - Todos os endpoints requerem ADMIN_EMPRESA — OPERADOR não pode gerenciar alertas
    - Validação de telefone: regex /^\+\d{10,15}$/ — retorna 400 se inválido
    - Frontend: page.tsx é Server Component que valida role ADMIN_EMPRESA — redireciona OPERADOR
    - alertas-client.tsx: lista de números por obra, select de obra, botão adicionar, botão remover por linha
    - Select de obras: busca /api/obras para listar obras da empresa
    - Input de telefone: placeholder "+5511999999999", validação client-side antes de enviar
    - Feedback: toast/alert de sucesso/erro após operações (usando window.alert para v1 — mesmo padrão do classificacao)
  </behavior>
  <action>
1. Criar apps/api/src/routes/configuracoes-alerta.ts:

```typescript
import { Router } from 'express';
import { requireRole } from '../middleware/rbac';

const router = Router();

// Validação de número telefônico E.164
function isValidE164(phone: string): boolean {
  return /^\+\d{10,15}$/.test(phone);
}

// GET /api/configuracoes-alerta?obraId=X
router.get('/', requireRole('ADMIN_EMPRESA'), async (req, res) => {
  const { obraId } = req.query;

  if (!obraId || typeof obraId !== 'string') {
    return res.status(400).json({ error: 'obraId é obrigatório' });
  }

  try {
    const configs = await req.tenantClient!.configuracaoAlerta.findMany({
      where: { obraId, ativo: true },
      orderBy: { createdAt: 'asc' },
      select: { id: true, telefone: true, obraId: true, createdAt: true },
    });
    return res.json({ items: configs });
  } catch (err) {
    throw err;
  }
});

// POST /api/configuracoes-alerta
router.post('/', requireRole('ADMIN_EMPRESA'), async (req, res) => {
  const { obraId, telefone } = req.body as { obraId?: string; telefone?: string };

  if (!obraId || !telefone) {
    return res.status(400).json({ error: 'obraId e telefone são obrigatórios' });
  }

  if (!isValidE164(telefone)) {
    return res.status(400).json({
      error: 'Telefone deve estar no formato E.164 (+5511999999999)',
    });
  }

  try {
    // Verificar que a obra pertence à empresa do usuário (isolamento de tenant)
    await req.tenantClient!.obra.findFirstOrThrow({ where: { id: obraId } });

    const config = await req.tenantClient!.configuracaoAlerta.create({
      data: {
        obraId,
        empresaId: req.user!.empresaId!,
        telefone,
        ativo: true,
      },
      select: { id: true, telefone: true, obraId: true, createdAt: true },
    });
    return res.status(201).json(config);
  } catch (err: unknown) {
    const code = (err as { code?: string })?.code;
    // P2002 = unique constraint (@@unique([obraId, telefone]))
    if (code === 'P2002') {
      return res.status(409).json({ error: 'Este número já está configurado para esta obra' });
    }
    if (code === 'P2025') {
      return res.status(404).json({ error: 'Obra não encontrada' });
    }
    throw err;
  }
});

// DELETE /api/configuracoes-alerta/:id
router.delete('/:id', requireRole('ADMIN_EMPRESA'), async (req, res) => {
  const { id } = req.params;

  try {
    await req.tenantClient!.configuracaoAlerta.findFirstOrThrow({ where: { id } });
    await req.tenantClient!.configuracaoAlerta.delete({ where: { id } });
    return res.status(204).send();
  } catch (err: unknown) {
    const code = (err as { code?: string })?.code;
    if (code === 'P2025') {
      return res.status(404).json({ error: 'Configuração não encontrada' });
    }
    throw err;
  }
});

export default router;
```

2. Registrar em apps/api/src/index.ts:

Adicionar import:
```typescript
import configuracoesAlertaRouter from './routes/configuracoes-alerta';
```

Adicionar rota após as rotas existentes protegidas:
```typescript
app.use('/api/configuracoes-alerta', ...protectedPipeline, configuracoesAlertaRouter);
```

3. Criar diretório e arquivos do frontend.

Criar apps/web/src/app/(admin)/configuracoes/alertas/page.tsx:

```tsx
import { auth } from '@/lib/auth';
import { redirect } from 'next/navigation';
import { AlertasClient } from './alertas-client';

export default async function AlertasPage() {
  const session = await auth();

  // Apenas ADMIN_EMPRESA pode gerenciar alertas
  if (!session?.user || session.user.role !== 'ADMIN_EMPRESA') {
    redirect('/dashboard');
  }

  return (
    <div className="p-6">
      <div className="mb-6">
        <h1 className="font-heading text-2xl font-bold text-slate-900">
          Configuração de Alertas WhatsApp
        </h1>
        <p className="mt-1 text-sm text-slate-500">
          Configure os números que receberão alertas quando veículos Suspeitos ou Críticos
          forem detectados nas suas obras.
        </p>
      </div>
      <AlertasClient />
    </div>
  );
}
```

4. Criar apps/web/src/app/(admin)/configuracoes/alertas/alertas-client.tsx:

```tsx
'use client';

import { useEffect, useState } from 'react';
import { resolveApiBaseUrl } from '@/lib/dashboard';

type Obra = { id: string; nome: string };
type ConfiguracaoAlerta = {
  id: string;
  telefone: string;
  obraId: string;
  createdAt: string;
};

function isValidE164(phone: string): boolean {
  return /^\+\d{10,15}$/.test(phone);
}

export function AlertasClient() {
  const [obras, setObras] = useState<Obra[]>([]);
  const [obraId, setObraId] = useState<string>('');
  const [configs, setConfigs] = useState<ConfiguracaoAlerta[]>([]);
  const [telefone, setTelefone] = useState('');
  const [loading, setLoading] = useState(false);

  const apiBaseUrl =
    typeof window === 'undefined'
      ? 'http://localhost:4000'
      : resolveApiBaseUrl(window.location.hostname, window.location.protocol);

  // Carregar obras
  useEffect(() => {
    fetch(`${apiBaseUrl}/api/obras`, { credentials: 'include' })
      .then((r) => r.json())
      .then((data: { items?: Obra[] }) => {
        const items = data.items ?? [];
        setObras(items);
        if (items.length > 0 && !obraId) setObraId(items[0].id);
      })
      .catch(console.error);
  }, [apiBaseUrl]);

  // Carregar configs da obra selecionada
  useEffect(() => {
    if (!obraId) return;
    fetch(`${apiBaseUrl}/api/configuracoes-alerta?obraId=${obraId}`, {
      credentials: 'include',
    })
      .then((r) => r.json())
      .then((data: { items?: ConfiguracaoAlerta[] }) => setConfigs(data.items ?? []))
      .catch(console.error);
  }, [apiBaseUrl, obraId]);

  async function handleAdd() {
    if (!isValidE164(telefone)) {
      window.alert('Número deve estar no formato E.164: +5511999999999');
      return;
    }
    setLoading(true);
    try {
      const res = await fetch(`${apiBaseUrl}/api/configuracoes-alerta`, {
        method: 'POST',
        credentials: 'include',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ obraId, telefone }),
      });
      if (!res.ok) {
        const body = (await res.json()) as { error?: string };
        window.alert(body.error ?? 'Erro ao adicionar número');
        return;
      }
      const newConfig = (await res.json()) as ConfiguracaoAlerta;
      setConfigs((prev) => [...prev, newConfig]);
      setTelefone('');
    } catch {
      window.alert('Erro de rede ao adicionar número');
    } finally {
      setLoading(false);
    }
  }

  async function handleRemove(id: string) {
    if (!window.confirm('Remover este número de alerta?')) return;
    try {
      const res = await fetch(`${apiBaseUrl}/api/configuracoes-alerta/${id}`, {
        method: 'DELETE',
        credentials: 'include',
      });
      if (!res.ok) {
        window.alert('Erro ao remover número');
        return;
      }
      setConfigs((prev) => prev.filter((c) => c.id !== id));
    } catch {
      window.alert('Erro de rede ao remover número');
    }
  }

  return (
    <div className="max-w-2xl space-y-6">
      {/* Seletor de obra */}
      <div>
        <label className="mb-1 block text-sm font-medium text-slate-700">
          Obra
        </label>
        <select
          value={obraId}
          onChange={(e) => setObraId(e.target.value)}
          className="w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-ggtech-blue"
          data-testid="obra-select"
        >
          {obras.map((o) => (
            <option key={o.id} value={o.id}>
              {o.nome}
            </option>
          ))}
        </select>
      </div>

      {/* Lista de números configurados */}
      <div>
        <h2 className="mb-2 text-sm font-semibold text-slate-700">
          Números configurados ({configs.length})
        </h2>
        {configs.length === 0 ? (
          <p className="text-sm text-slate-500">
            Nenhum número configurado para esta obra.
          </p>
        ) : (
          <ul className="space-y-2" data-testid="configs-list">
            {configs.map((c) => (
              <li
                key={c.id}
                className="flex items-center justify-between rounded-lg border border-slate-200 bg-white px-4 py-3"
              >
                <span className="font-mono text-sm text-slate-900">{c.telefone}</span>
                <button
                  onClick={() => handleRemove(c.id)}
                  className="text-sm font-medium text-red-600 hover:text-red-800"
                  data-testid="remove-btn"
                >
                  Remover
                </button>
              </li>
            ))}
          </ul>
        )}
      </div>

      {/* Adicionar novo número */}
      <div>
        <h2 className="mb-2 text-sm font-semibold text-slate-700">
          Adicionar número
        </h2>
        <div className="flex gap-3">
          <input
            type="tel"
            value={telefone}
            onChange={(e) => setTelefone(e.target.value)}
            placeholder="+5511999999999"
            className="flex-1 rounded-lg border border-slate-300 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-ggtech-blue"
            data-testid="telefone-input"
          />
          <button
            onClick={handleAdd}
            disabled={loading || !telefone || !obraId}
            className="rounded-lg bg-ggtech-blue px-4 py-2 text-sm font-medium text-white hover:opacity-90 disabled:opacity-50"
            data-testid="add-btn"
          >
            {loading ? 'Adicionando...' : 'Adicionar'}
          </button>
        </div>
        <p className="mt-1 text-xs text-slate-500">
          Formato: +5511999999999 (código do país obrigatório)
        </p>
      </div>
    </div>
  );
}
```

5. Adicionar link de navegação para /configuracoes/alertas no sidebar (apps/web/src/components/sidebar.tsx).
   Verificar se sidebar tem seção de admin — adicionar link visível apenas para ADMIN_EMPRESA.
   Se sidebar recebe userRole como prop (já verificado no dashboard-client.tsx), condicionar exibição:
   ```tsx
   {userRole === 'ADMIN_EMPRESA' && (
     <a href="/configuracoes/alertas" className="...">
       Alertas WhatsApp
     </a>
   )}
   ```

6. Verificar TypeScript: `cd apps/web && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript compila sem erros. Backend CRUD de ConfiguracaoAlerta registrado em index.ts. Página /configuracoes/alertas acessível apenas para ADMIN_EMPRESA. AlertasClient permite adicionar e remover números com validação E.164.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/configuracoes-alerta | Request autenticado com cookie JWT — empresaId sempre do token, nunca do body |
| Socket.IO → Browser overlay | Payload recebido pelo cliente para display — não persiste dados, apenas renderiza |
| Admin input → telefone | Validação E.164 tanto no frontend quanto no backend antes de persistir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Elevation of Privilege | OPERADOR tentando acessar /api/configuracoes-alerta | mitigate | requireRole('ADMIN_EMPRESA') em todos os endpoints — OPERADOR recebe 403 |
| T-04-12 | Tampering | Body injection em POST configuracoes-alerta (obraId de outra empresa) | mitigate | findFirstOrThrow via tenantClient verifica que obraId pertence à empresa do token — isolamento garantido |
| T-04-13 | Information Disclosure | CrossSiteAlertDTO exibido na tela do operador | accept | Dados são operacionais (placa, obra) — o operador tem direito de ver essas informações |
| T-04-14 | Denial of Service | Overlay cross-site aparecendo sem parar | mitigate | Apenas 1 alerta ativo por vez (setCrossSiteAlert substitui), dedup no backend (Plan 04-02) |
| T-04-15 | Elevation of Privilege | Acesso direto a /configuracoes/alertas por OPERADOR | mitigate | page.tsx verifica role via auth() — redireciona para /dashboard se role != ADMIN_EMPRESA |
</threat_model>

<verification>
Após execução do plano:

1. `cd apps/web && npx vitest run src/components/cross-site-alert-overlay.test.tsx` — 6 testes passando
2. `cd apps/web && npx tsc --noEmit` — zero erros de tipo
3. `grep "feed:alerta-cross-site" apps/web/src/components/dashboard-client.tsx` — socket handler existe
4. `grep "configuracoes-alerta" apps/api/src/index.ts` — rota registrada
5. `grep "requireRole.*ADMIN_EMPRESA" apps/api/src/routes/configuracoes-alerta.ts` — proteção de role
6. Verificação humana: fazer login como ADMIN_EMPRESA, navegar para /configuracoes/alertas, adicionar um número no formato +5511999999999 e confirmar que aparece na lista
</verification>

<success_criteria>
- CrossSiteAlertOverlay: renderiza payload INTEL-03, auto-dismiss 30s, botão dispensar funcional
- dashboard-client.tsx ouve 'feed:alerta-cross-site' e exibe overlay
- /api/configuracoes-alerta (GET/POST/DELETE) protegido por ADMIN_EMPRESA
- Página /configuracoes/alertas acessível apenas para ADMIN_EMPRESA
- 6 testes do overlay passando
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-cross-site-intelligence-whatsapp-alerts/04-04-SUMMARY.md` com:
- Nome do evento Socket.IO escutado: 'feed:alerta-cross-site'
- Caminho da página de admin: /configuracoes/alertas
- Endpoints criados: GET/POST/DELETE /api/configuracoes-alerta
- Formato de validação de telefone: E.164 /^\+\d{10,15}$/
</output>
