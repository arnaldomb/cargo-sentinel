---
phase: 01-monorepo-lpr-ingestion-storage
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docker-compose.yml
  - .env.example
  - garage/garage.toml
  - traefik/acme.json
  - apps/api/Dockerfile
  - apps/web/Dockerfile
  - .dockerignore
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - STORAGE-01
user_setup: []

must_haves:
  truths:
    - "docker compose config validates with all 6 services: web, api, postgres, garage, redis, traefik"
    - "docker compose up starts all 6 services without crash-looping"
    - "Garage runs with a default bucket lpr-images and a GK-prefixed access key"
    - "Traefik routes /api/* to Express, /media/* to Garage, and /* to Next.js by priority"
    - "acme.json is mounted from a host volume for Let's Encrypt persistence"
  artifacts:
    - path: "docker-compose.yml"
      provides: "All 6 Phase 1 services"
      contains: "garage:"
    - path: "garage/garage.toml"
      provides: "Garage single-node config (replication_factor 1, s3_region garage)"
      contains: "replication_factor = 1"
    - path: ".env.example"
      provides: "Documented env vars incl GARAGE_ACCESS_KEY GK-prefix note"
      contains: "GARAGE_ACCESS_KEY"
    - path: "traefik/acme.json"
      provides: "Persistent Let's Encrypt cert store (chmod 600)"
  key_links:
    - from: "docker-compose.yml traefik service"
      to: "acme.json"
      via: "host-mounted volume"
      pattern: "acme.json"
    - from: "docker-compose.yml api service"
      to: "traefik router"
      via: "PathPrefix(/api) label"
      pattern: "PathPrefix\\(`/api`\\)"
---

<objective>
Define the Docker Compose stack with all 6 Phase 1 services (web, api, postgres, garage, redis, traefik), configure Garage v2 single-node S3 storage with the `lpr-images` bucket, set up Traefik v3 priority routing (`/api/*` → Express, `/media/*` → Garage, `/*` → Next.js), and persist Let's Encrypt certificates via a host-mounted `acme.json`.

Purpose: Phase 1 Success Criterion 2 requires `docker compose up` to start all 6 services with no errors. This stack is the runtime the LPR pipeline (Plan 04) executes inside, and the Garage bucket is where photos land.
Output: A validated docker-compose.yml, garage.toml, .env.example, Dockerfiles for api and web, and an acme.json volume file.

Satisfies: INFRA-03, INFRA-04, INFRA-05, STORAGE-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/01-monorepo-lpr-ingestion-storage/01-RESEARCH.md

<interfaces>
<!-- Garage / Traefik facts the executor must honor (from RESEARCH Pattern 4 and Pitfall 6): -->
<!-- - Garage image: dxflrs/garage:v2.3.0 -->
<!-- - Single-node mode: command `/garage server --single-node --default-bucket` with garage.toml replication_factor = 1; the flags auto-create the lpr-images bucket -->
<!-- - GARAGE_DEFAULT_ACCESS_KEY MUST start with `GK` (generate: GK$(openssl rand -hex 16)) -->
<!-- - GARAGE_DEFAULT_BUCKET = lpr-images -->
<!-- - s3_region = "garage" (must match the S3Client region in Plan 04) -->
<!-- - Traefik: traefik:v3.0; route priority high->low: /api (Express:4000), /media (Garage:3900), / (Next:3000) -->
<!-- - Postgres creds must match packages/database/.env: sentinel/sentinel/cargo_sentinel -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfiles + .env.example + garage.toml + acme.json</name>
  <files>apps/api/Dockerfile, apps/web/Dockerfile, .dockerignore, .env.example, garage/garage.toml, traefik/acme.json</files>
  <read_first>
    - .planning/phases/01-monorepo-lpr-ingestion-storage/01-RESEARCH.md (Pattern 4 garage.toml lines 369-387; Pitfall 6 access key format lines 537-541; Traefik example lines 592-610)
    - packages/database/.env (postgres credentials to mirror — created in Plan 02; if absent, use sentinel/sentinel/cargo_sentinel)
  </read_first>
  <action>
**1. `apps/api/Dockerfile`** — multi-stage build for the pnpm monorepo. Base `node:24-alpine`, enable corepack/pnpm, copy the workspace, `pnpm install --frozen-lockfile`, `pnpm --filter @cargo-sentinel/database exec prisma generate`, `pnpm --filter @cargo-sentinel/api build`, expose 4000, CMD `node apps/api/dist/index.js`. Because this is a workspace, copy the root `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, and all `apps/`+`packages/` package.json files first for layer caching, then the source.

**2. `apps/web/Dockerfile`** — same monorepo pattern, base `node:24-alpine`, build with `pnpm --filter @cargo-sentinel/web build`, expose 3000, CMD `pnpm --filter @cargo-sentinel/web start`.

**3. `.dockerignore`** — include: `node_modules`, `**/node_modules`, `.next`, `dist`, `.turbo`, `.git`, `.env`, `*.log`, `garage/data`, `garage/meta`.

**4. `.env.example`** — document every env var the compose stack needs (real `.env` is gitignored). Include this exact block with the GK note (RESEARCH Pitfall 6):
```
# Postgres (must match packages/database/.env)
POSTGRES_USER=sentinel
POSTGRES_PASSWORD=sentinel
POSTGRES_DB=cargo_sentinel
DATABASE_URL=postgresql://sentinel:sentinel@postgres:5432/cargo_sentinel?schema=public

# Redis
REDIS_URL=redis://redis:6379

# Garage S3 — ACCESS KEY MUST START WITH "GK" (generate: GK$(openssl rand -hex 16))
GARAGE_ACCESS_KEY=GKchangeme0000000000000000000000
GARAGE_SECRET_KEY=changeme_generate_with_openssl_rand_hex_32
GARAGE_DEFAULT_BUCKET=lpr-images
GARAGE_INTERNAL_URL=http://garage:3900
GARAGE_SERVER_URL=https://sentinel.example.com/media

# AWS SDK v3 + Garage compatibility (REQUIRED — prevents checksum errors)
AWS_REQUEST_CHECKSUM_CALCULATION=when_required
AWS_RESPONSE_CHECKSUM_VALIDATION=when_required

# Traefik
ACME_EMAIL=admin@example.com
PUBLIC_DOMAIN=sentinel.example.com
```

**5. `garage/garage.toml`** — EXACT config from RESEARCH Pattern 4 (lines 369-387): `metadata_dir`, `data_dir`, `db_engine = "sqlite"`, `replication_factor = 1`, rpc bind/public addr, an `rpc_secret` (note in a comment: generate with `openssl rand -hex 32`), `[s3_api]` with `s3_region = "garage"` and `api_bind_addr = "[::]:3900"`, and `[admin]` with its own token.

**6. `traefik/acme.json`** — create an empty file. CRITICAL (INFRA-05, RESEARCH lines 484, 609): it must be `chmod 600` or Traefik refuses to use it. Run `chmod 600 traefik/acme.json` after creating it and note in the SUMMARY that on a fresh clone this file must be created with 600 perms.
  </action>
  <verify>
    <automated>node -e "const f=require('fs'); ['apps/api/Dockerfile','apps/web/Dockerfile','.env.example','garage/garage.toml','traefik/acme.json','.dockerignore'].forEach(p=>{if(!f.existsSync(p))throw new Error('missing '+p)}); const g=f.readFileSync('garage/garage.toml','utf8'); if(!g.includes('replication_factor = 1'))throw new Error('garage.toml missing replication_factor'); if(!g.includes('s3_region'))throw new Error('garage.toml missing s3_region'); console.log('infra files OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `garage/garage.toml` contains `replication_factor = 1` and `s3_region = "garage"`
    - `.env.example` contains `GARAGE_ACCESS_KEY` with a comment noting the `GK` prefix requirement
    - `.env.example` contains `AWS_REQUEST_CHECKSUM_CALCULATION=when_required`
    - `apps/api/Dockerfile` contains `prisma generate` and exposes port 4000
    - `traefik/acme.json` exists
    - `.dockerignore` contains `node_modules` and `.env`
  </acceptance_criteria>
  <done>Dockerfiles for api and web exist; garage.toml configured for single-node (replication_factor 1, region garage); .env.example documents all vars including the GK-prefix and AWS checksum flags; acme.json created.</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose.yml with all 6 services + validate + smoke up</name>
  <files>docker-compose.yml</files>
  <read_first>
    - .planning/phases/01-monorepo-lpr-ingestion-storage/01-RESEARCH.md (Pattern 4 garage service lines 344-367; Traefik command flags lines 592-610; Architecture services list lines 9-12 of phase context)
    - .env.example (env var names this compose file references — created in Task 1)
    - garage/garage.toml (mounted into the garage service)
  </read_first>
  <action>
Create `docker-compose.yml` with EXACTLY these 6 services. Define a shared `sentinel` network and named volumes (`pg-data`, `redis-data`, `garage-meta`, `garage-data`).

**postgres** — image `postgres:16-alpine`, env from `POSTGRES_USER/PASSWORD/DB`, volume `pg-data:/var/lib/postgresql/data`, healthcheck `pg_isready -U $POSTGRES_USER`, `restart: unless-stopped`. (No Traefik labels — internal only.)

**redis** — image `redis:7-alpine`, volume `redis-data:/data`, healthcheck `redis-cli ping`, `restart: unless-stopped`. (Internal only.)

**garage** — image `dxflrs/garage:v2.3.0`, command `/garage server --single-node --default-bucket` (the `--single-node --default-bucket` flags are MANDATORY — they auto-create the `lpr-images` bucket and the GK-prefixed access key from the env vars on first start, satisfying STORAGE-01; without them the bucket is NOT created and STORAGE-01 breaks, per RESEARCH Pattern 4), env `GARAGE_DEFAULT_ACCESS_KEY=${GARAGE_ACCESS_KEY}`, `GARAGE_DEFAULT_SECRET_KEY=${GARAGE_SECRET_KEY}`, `GARAGE_DEFAULT_BUCKET=lpr-images`. Mount `./garage/garage.toml:/etc/garage.toml`, `garage-meta:/var/lib/garage/meta`, `garage-data:/var/lib/garage/data`. Traefik labels: enable, router rule `Host(\`${PUBLIC_DOMAIN}\`) && PathPrefix(\`/media\`)`, `priority=20`, loadbalancer server port `3900`, tls certresolver letsencrypt. `restart: unless-stopped`.

**api** — `build: { context: ., dockerfile: apps/api/Dockerfile }`, `env_file: .env`, depends_on postgres+redis+garage (condition service_healthy where available), Traefik labels: router rule `Host(\`${PUBLIC_DOMAIN}\`) && PathPrefix(\`/api\`)`, `priority=30` (highest — matched before /media and /), loadbalancer server port `4000`, tls certresolver letsencrypt. `restart: unless-stopped`.

**web** — `build: { context: ., dockerfile: apps/web/Dockerfile }`, Traefik labels: router rule `Host(\`${PUBLIC_DOMAIN}\`)` (no path prefix — catch-all), `priority=10` (lowest), loadbalancer server port `3000`, tls certresolver letsencrypt. `restart: unless-stopped`.

**traefik** — image `traefik:v3.0`, command flags from RESEARCH lines 596-603 (`--providers.docker=true`, `--providers.docker.exposedbydefault=false`, web entrypoint `:80`, websecure entrypoint `:443`, `--certificatesresolvers.letsencrypt.acme.tlschallenge=true`, `--certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL}`, `--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json`). Ports `80:80`, `443:443`, `8080:8080`. Volumes `/var/run/docker.sock:/var/run/docker.sock:ro` and `./traefik/acme.json:/letsencrypt/acme.json`. `restart: unless-stopped`.

Priority ordering rationale (INFRA-04): `/api` (30) > `/media` (20) > `/` (10) ensures the most specific path wins.

After writing the file, run `docker compose config` to validate syntax (requires a `.env` — copy from `.env.example` first: `cp .env.example .env`). Then smoke-test that the infra-only services start: `docker compose up -d postgres redis garage` and confirm with `docker compose ps` they are running, then `docker compose down`. (Building api/web images is heavier and validated at phase gate; this task gates on `docker compose config` + the three stateful services coming up.)
  </action>
  <verify>
    <automated>cp -n .env.example .env 2>/dev/null; docker compose config >/dev/null && echo "compose config valid"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config` exits 0 (valid syntax)
    - `docker compose config` output lists all 6 services: web, api, postgres, garage, redis, traefik
    - The garage service uses image `dxflrs/garage:v2.3.0`, command `/garage server --single-node --default-bucket`, and sets `GARAGE_DEFAULT_BUCKET` to `lpr-images`
    - After `docker compose up -d garage`, the `lpr-images` bucket auto-creates — verify with `docker compose exec garage /garage bucket list` (lists `lpr-images`) (STORAGE-01)
    - The api service has a Traefik label containing `PathPrefix(\`/api\`)` with `priority=30`
    - The traefik service mounts `./traefik/acme.json:/letsencrypt/acme.json`
    - `docker compose up -d postgres redis garage` brings all three to running state (verified via `docker compose ps`)
  </acceptance_criteria>
  <done>docker-compose.yml validates with all 6 services; priority routing /api > /media > / configured; acme.json mounted; postgres, redis, and garage start cleanly; the `lpr-images` bucket auto-creates via `--single-node --default-bucket` (verified with `garage bucket list`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → Traefik → services | All external traffic terminates TLS at Traefik before reaching internal services |
| Traefik → Garage | Object storage exposed only through /media path, never directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Information Disclosure | Garage bucket public access | mitigate | Bucket is NOT public; only reachable via Traefik /media with presigned URLs (enforced in Plan 04); `exposedbydefault=false` keeps unlisted containers off the router |
| T-1-I2 | Information Disclosure | acme.json world-readable | mitigate | `chmod 600 traefik/acme.json` (Task 1) — Traefik refuses looser perms |
| T-1-I3 | Information Disclosure | Secrets in compose env | mitigate | Real secrets live in gitignored `.env`; only `.env.example` (placeholders) is committed |
| T-1-E1 | Elevation of Privilege | Docker socket exposure to Traefik | accept | Socket mounted read-only (`:ro`); standard Traefik provider requirement; single-tenant VPS |
</threat_model>

<verification>
- `docker compose config` exits 0 and lists all 6 services
- garage uses v2.3.0 image, command `/garage server --single-node --default-bucket`; `lpr-images` bucket auto-creates (verified via `garage bucket list`)
- api router has PathPrefix(/api) priority 30; media priority 20; web priority 10
- acme.json mounted and chmod 600
- postgres + redis + garage start and reach running/healthy state
</verification>

<success_criteria>
- docker compose defines and validates all 6 services (Phase 1 Success Criterion 2 foundation)
- Garage single-node with lpr-images bucket (STORAGE-01)
- Traefik v3 priority routing /api > /media > / (INFRA-04)
- acme.json persistent host-mounted volume (INFRA-05)
- INFRA-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-lpr-ingestion-storage/01-03-SUMMARY.md`
</output>
