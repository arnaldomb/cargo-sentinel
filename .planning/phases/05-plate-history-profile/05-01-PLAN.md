---
id: "05-01"
name: "Backend Plate Profile API"
phase: "05"
plan: 1
type: execute
wave: 1
autonomous: true
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - apps/api/src/routes/placas.ts
  - apps/api/src/routes/eventos.ts
  - apps/api/src/index.ts
requirements: [HISTORY-01, HISTORY-02, HISTORY-03, HISTORY-04]

must_haves:
  truths:
    - "GET /api/placas/:numero/historico retorna eventos paginados (cursor) da placa em toda a empresa"
    - "GET /api/placas/:numero/classificacoes retorna audit trail cronológico com usuário responsável"
    - "GET /api/eventos/buscar aceita filtros placa/dataInicio/dataFim/obraId/cameraId e pagina por cursor"
    - "Nenhuma das 3 queries faz full-table scan — índice composto garante keyset pagination eficiente"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "Índice composto @@index([empresaId, placaId, timestamp]) em Evento"
      contains: "empresaId, placaId, timestamp"
    - path: "apps/api/src/routes/placas.ts"
      provides: "Rotas GET /api/placas/:numero/historico e GET /api/placas/:numero/classificacoes"
      exports: ["router"]
    - path: "apps/api/src/routes/eventos.ts"
      provides: "Rota GET /api/eventos/buscar com filtros"
      exports: ["router"]
  key_links:
    - from: "apps/api/src/routes/placas.ts"
      to: "req.tenantClient.evento.findMany"
      via: "cursor-based pagination com where.placaId"
      pattern: "placaId.*cursor"
    - from: "apps/api/src/routes/eventos.ts"
      to: "req.tenantClient.evento.findMany"
      via: "filtros compostos + cursor"
      pattern: "buscar.*cursor"
---

<objective>
Criar as 3 rotas de API necessárias para o perfil de placa e busca de histórico, com índice
de banco de dados que garante que nenhuma query faça full-table scan mesmo com 10.000+ eventos.

Purpose: Fundação backend para as páginas de perfil (05-02) e busca (05-03). Sem esta API, as
páginas do frontend não têm dados para exibir.

Output:
- Índice composto em `Evento` para queries de histórico de placa (migration via `prisma db push`)
- `GET /api/placas/:numero/historico` — eventos paginados cursor-based com filtros opcionais
- `GET /api/placas/:numero/classificacoes` — audit trail de classificação com nome do usuário
- `GET /api/eventos/buscar` — busca cross-filter com cursor pagination
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<!-- Schema atual — Evento, Placa, ClassificacaoHistorico -->
@packages/database/prisma/schema.prisma

<!-- Padrão de cursor pagination existente (usar como referência) -->
@apps/api/src/routes/eventos.ts

<!-- Middleware de tenant — req.tenantClient injetado aqui -->
@apps/api/src/middleware/pipeline.ts

<!-- Rota de placas existente — adicionar GET aqui -->
@apps/api/src/routes/placas.ts

<!-- Registro de rotas — adicionar buscar ao eventosRouter -->
@apps/api/src/index.ts
</context>

<interfaces>
<!-- Padrão cursor pagination (extraído de apps/api/src/routes/eventos.ts) -->
```typescript
// Padrão estabelecido: take: limit + 1, cursor: { id: cursor }, skip: 1
// nextCursor = hasMore ? page[page.length - 1]?.id ?? null : null
// Resposta: { items: T[], nextCursor: string | null }
```

<!-- Schema Evento — índices atuais -->
```prisma
model Evento {
  id            String        @id @default(cuid())
  timestamp     DateTime      @default(now())
  placaNumero   String
  placaId       String?
  direcao       Direcao?
  fotoGarageKey String?
  idempotencyKey String       @unique
  cameraId      String
  obraId        String
  empresaId     String
  classificacao Classificacao @default(VISITANTE)
  rawPayload    Json?
  // ... relações ...

  @@index([empresaId, timestamp])
  @@index([empresaId, placaNumero])
  @@index([placaId])
  @@index([obraId, timestamp])
  @@index([cameraId])
  // FALTA: @@index([empresaId, placaId, timestamp(sort: Desc)]) — adicionar neste plano
}
```

<!-- ClassificacaoHistorico — índices existentes já adequados -->
```prisma
model ClassificacaoHistorico {
  id                String         @id @default(cuid())
  placaId           String
  empresaId         String
  classificacaoDe   Classificacao?
  classificacaoPara Classificacao
  observacao        String?
  usuarioId         String
  createdAt         DateTime       @default(now())
  // ...
  @@index([placaId, createdAt(sort: Desc)])  // ← já existe, bom para audit trail
}
```

<!-- req.tenantClient tem empresaId do JWT injetado automaticamente via $extends -->
<!-- Todas as queries via req.tenantClient já filtram por empresaId -->
```typescript
// Em qualquer rota protegida:
req.tenantClient!.evento.findMany({ where: { /* empresaId implícito */ } })
req.tenantClient!.placa.findFirst({ where: { numero: placaNumero } })
req.tenantClient!.classificacaoHistorico.findMany({ ... })
```

<!-- Garage presigned URL — já importada em eventos.ts -->
```typescript
import { getPresignedUrl } from '../services/garage';
// getPresignedUrl(fotoGarageKey: string): Promise<string>
```

<!-- requireRole middleware -->
```typescript
import { requireRole } from '../middleware/rbac';
// requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR')
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Índice composto em Evento + migrate</name>
  <files>packages/database/prisma/schema.prisma</files>
  <behavior>
    - Após adicionar o índice, `prisma db push` deve completar sem erros
    - EXPLAIN ANALYZE de `WHERE empresaId = $1 AND placaId = $2 ORDER BY timestamp DESC` deve usar o índice (não Seq Scan)
  </behavior>
  <action>
Adicionar o índice composto ao model `Evento` no schema.prisma:

```prisma
@@index([empresaId, placaId, timestamp(sort: Desc)])
```

Colocar após os índices existentes. Depois executar:

```bash
cd packages/database && npx prisma db push
```

Verificar que não há erros de migration. Este índice é essencial para que as queries de
histórico de placa (`WHERE empresaId = X AND placaId = Y ORDER BY timestamp DESC`) usem
keyset pagination sem full-table scan, mesmo com datasets de 10.000+ eventos (HISTORY-04).

NÃO remover os índices existentes — são usados por outras queries.
  </action>
  <verify>
    <automated>cd packages/database && npx prisma db push --accept-data-loss 2>&1 | grep -E "done|already|error" ; cd ../.. && npx prisma validate --schema packages/database/prisma/schema.prisma 2>&1 | head -5</automated>
  </verify>
  <done>schema.prisma contém `@@index([empresaId, placaId, timestamp(sort: Desc)])` em Evento e `prisma db push` não retorna erros</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/placas/:numero/historico + GET /api/placas/:numero/classificacoes</name>
  <files>apps/api/src/routes/placas.ts, apps/api/src/routes/placas.test.ts</files>
  <behavior>
    - GET /api/placas/ABC1234/historico retorna { items: EventoHistoricoItem[], nextCursor: string | null }
    - items inclui: id, timestamp, direcao, thumbnailUrl, classificacao, obra.nome, camera.codigoLpr
    - Filtros opcionais: ?dataInicio=ISO&dataFim=ISO&obraId=cuid&cameraId=cuid&cursor=id&limit=20
    - limit máximo é 100, padrão 20
    - GET /api/placas/ABC1234/classificacoes retorna { items: ClassificacaoItem[] } (sem paginação — audit trail completo)
    - ClassificacaoItem inclui: id, createdAt, classificacaoDe, classificacaoPara, observacao, usuario.nome
    - Placa não encontrada na empresa retorna 404
    - Número de placa normalizado para uppercase antes da query
  </behavior>
  <action>
Adicionar duas rotas GET ao arquivo `apps/api/src/routes/placas.ts` após a rota PATCH existente.

**Rota 1: GET /:numero/historico (HISTORY-01, HISTORY-04)**

```typescript
router.get(
  '/:numero/historico',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR'),
  async (req, res) => {
    const numero = req.params.numero.toUpperCase().trim();
    const rawLimit = Number(req.query.limit ?? 20);
    const limit = Number.isFinite(rawLimit) ? Math.min(Math.max(rawLimit, 1), 100) : 20;
    const cursor = typeof req.query.cursor === 'string' ? req.query.cursor.trim() : undefined;
    const obraId = typeof req.query.obraId === 'string' ? req.query.obraId.trim() : undefined;
    const cameraId = typeof req.query.cameraId === 'string' ? req.query.cameraId.trim() : undefined;
    const dataInicio = typeof req.query.dataInicio === 'string' ? new Date(req.query.dataInicio) : undefined;
    const dataFim = typeof req.query.dataFim === 'string' ? new Date(req.query.dataFim) : undefined;

    // Primeiro resolve o placaId a partir do número (tenantClient já filtra empresaId)
    const placa = await req.tenantClient!.placa.findFirst({
      where: { numero },
      select: {
        id: true,
        numero: true,
        classificacao: true,
        empresaTransportadora: true,
        motorista: true,
        tipoVeiculo: true,
        observacao: true,
      },
    });

    if (!placa) return res.status(404).json({ error: 'Placa não encontrada' });

    const whereFilters: Record<string, unknown> = {
      placaId: placa.id,
      ...(obraId && { obraId }),
      ...(cameraId && { cameraId }),
      ...(dataInicio || dataFim
        ? {
            timestamp: {
              ...(dataInicio && { gte: dataInicio }),
              ...(dataFim && { lte: dataFim }),
            },
          }
        : {}),
    };

    const eventos = await req.tenantClient!.evento.findMany({
      where: whereFilters,
      take: limit + 1,
      ...(cursor ? { cursor: { id: cursor }, skip: 1 } : {}),
      orderBy: { timestamp: 'desc' },
      select: {
        id: true,
        timestamp: true,
        direcao: true,
        fotoGarageKey: true,
        classificacao: true,
        obra: { select: { id: true, nome: true } },
        camera: { select: { id: true, codigoLpr: true } },
      },
    });

    const hasMore = eventos.length > limit;
    const page = hasMore ? eventos.slice(0, limit) : eventos;

    const items = await Promise.all(
      page.map(async (e) => ({
        id: e.id,
        timestamp: e.timestamp.toISOString(),
        direcao: e.direcao,
        classificacao: e.classificacao,
        thumbnailUrl: e.fotoGarageKey ? await getPresignedUrl(e.fotoGarageKey) : null,
        obra: e.obra,
        camera: e.camera,
      })),
    );

    return res.json({
      placa,
      items,
      nextCursor: hasMore ? page[page.length - 1]?.id ?? null : null,
    });
  },
);
```

**Rota 2: GET /:numero/classificacoes (HISTORY-02)**

```typescript
router.get(
  '/:numero/classificacoes',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR'),
  async (req, res) => {
    const numero = req.params.numero.toUpperCase().trim();

    const placa = await req.tenantClient!.placa.findFirst({
      where: { numero },
      select: { id: true, numero: true, classificacao: true },
    });

    if (!placa) return res.status(404).json({ error: 'Placa não encontrada' });

    const historico = await req.tenantClient!.classificacaoHistorico.findMany({
      where: { placaId: placa.id },
      orderBy: { createdAt: 'desc' },
      select: {
        id: true,
        createdAt: true,
        classificacaoDe: true,
        classificacaoPara: true,
        observacao: true,
        usuario: { select: { id: true, nome: true } },
      },
    });

    return res.json({
      placa,
      items: historico.map((h) => ({
        ...h,
        createdAt: h.createdAt.toISOString(),
      })),
    });
  },
);
```

Adicionar `import { getPresignedUrl } from '../services/garage';` no topo do arquivo caso não exista.

Criar `apps/api/src/routes/placas.test.ts` com testes para:
- 200 com items e nextCursor null quando menos que limit
- 404 quando placa não encontrada na empresa
- Filtro obraId reduz resultados
- /classificacoes retorna usuario.nome em cada item
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- --testPathPattern=placas.test --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>GET /api/placas/:numero/historico e GET /api/placas/:numero/classificacoes retornam 200 com estrutura correta; 404 para placa inexistente; testes passam</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /api/eventos/buscar com filtros compostos</name>
  <files>apps/api/src/routes/eventos.ts, apps/api/src/routes/eventos.test.ts</files>
  <behavior>
    - GET /api/eventos/buscar?placa=ABC retorna eventos cujo placaNumero contém "ABC" (ILIKE, case-insensitive)
    - Filtros opcionais: placa (partial match), dataInicio, dataFim, obraId, cameraId, cursor, limit
    - limit máximo 100, padrão 20
    - Resposta: { items: BuscarItem[], nextCursor: string | null }
    - BuscarItem: id, timestamp, placaNumero, placaId, direcao, classificacao, thumbnailUrl, obra.nome, camera.codigoLpr
    - Sem filtros, retorna os 20 eventos mais recentes da empresa (mesmo que o feed, mas sem real-time)
    - Busca por placa parcial usa `contains` do Prisma com `mode: 'insensitive'`
  </behavior>
  <action>
Adicionar rota GET `/buscar` ao arquivo `apps/api/src/routes/eventos.ts`, ANTES do `export default router`.

```typescript
/**
 * GET /api/eventos/buscar
 *
 * Busca cross-filter de eventos com cursor pagination (HISTORY-03, HISTORY-04).
 * Filtros: placa (partial, case-insensitive), dataInicio, dataFim, obraId, cameraId.
 * Idêntico ao /feed mas com filtros adicionais e sem push em tempo real.
 */
router.get(
  '/buscar',
  requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR'),
  async (req, res) => {
    const rawLimit = Number(req.query.limit ?? 20);
    const limit = Number.isFinite(rawLimit) ? Math.min(Math.max(rawLimit, 1), 100) : 20;
    const cursor = typeof req.query.cursor === 'string' ? req.query.cursor.trim() : undefined;
    const placa = typeof req.query.placa === 'string' ? req.query.placa.trim() : undefined;
    const obraId = typeof req.query.obraId === 'string' ? req.query.obraId.trim() : undefined;
    const cameraId = typeof req.query.cameraId === 'string' ? req.query.cameraId.trim() : undefined;
    const dataInicio = typeof req.query.dataInicio === 'string' ? new Date(req.query.dataInicio) : undefined;
    const dataFim = typeof req.query.dataFim === 'string' ? new Date(req.query.dataFim) : undefined;

    const where: Record<string, unknown> = {
      ...(placa && { placaNumero: { contains: placa, mode: 'insensitive' } }),
      ...(obraId && { obraId }),
      ...(cameraId && { cameraId }),
      ...(dataInicio || dataFim
        ? {
            timestamp: {
              ...(dataInicio && { gte: dataInicio }),
              ...(dataFim && { lte: dataFim }),
            },
          }
        : {}),
    };

    const eventos = await req.tenantClient!.evento.findMany({
      where,
      take: limit + 1,
      ...(cursor ? { cursor: { id: cursor }, skip: 1 } : {}),
      orderBy: { timestamp: 'desc' },
      select: {
        id: true,
        timestamp: true,
        placaNumero: true,
        placaId: true,
        direcao: true,
        fotoGarageKey: true,
        classificacao: true,
        obra: { select: { id: true, nome: true } },
        camera: { select: { id: true, codigoLpr: true } },
      },
    });

    const hasMore = eventos.length > limit;
    const page = hasMore ? eventos.slice(0, limit) : eventos;

    const items = await Promise.all(
      page.map(async (e) => ({
        id: e.id,
        timestamp: e.timestamp.toISOString(),
        placaNumero: e.placaNumero,
        placaId: e.placaId,
        direcao: e.direcao,
        classificacao: e.classificacao,
        thumbnailUrl: e.fotoGarageKey ? await getPresignedUrl(e.fotoGarageKey) : null,
        obra: e.obra,
        camera: e.camera,
      })),
    );

    res.json({ items, nextCursor: hasMore ? page[page.length - 1]?.id ?? null : null });
  },
);
```

ATENÇÃO: a rota `/buscar` deve ser registrada ANTES de qualquer rota parametrizada como `/:id` para evitar que "buscar" seja interpretado como um ID. Posicionar no topo do router, antes da rota `/feed` existente, ou logo após ela — verificar que não há rota `/:id` no arquivo. Se houver, colocar `/buscar` antes.

Atualizar `apps/api/src/routes/eventos.test.ts` (ou criar) com testes:
- Sem filtros: retorna items e nextCursor
- Com placa="ABC": retorna apenas eventos com placaNumero contendo "ABC"
- Com obraId: filtra por obra
- nextCursor presente quando há mais itens
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- --testPathPattern=eventos.test --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>GET /api/eventos/buscar retorna eventos paginados com filtros funcionando; testes passam; rota registrada corretamente no router sem conflito com /feed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/placas/:numero/historico | Número de placa vem da URL — normalizado em uppercase, mas não causa SQL injection via Prisma ORM |
| client → GET /api/eventos/buscar?placa= | Parâmetro `placa` usado em `contains` — Prisma parametriza a query; sem risco de injection |
| JWT → tenantClient | empresaId extraído do JWT verificado, nunca do request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | GET /api/placas/:numero/historico | mitigate | tenantClient garante que somente eventos da empresa do token são retornados; número normalizado para uppercase evita bypass por capitalização |
| T-05-02 | Information Disclosure | GET /api/eventos/buscar | mitigate | Mesmo isolamento via tenantClient; placa partial search limitada ao tenant do JWT |
| T-05-03 | Denial of Service | buscar sem filtros + limit=100 | mitigate | limit máximo hardcoded em 100; cursor pagination impede full-table scan via índice composto |
| T-05-04 | Spoofing | empresaId no request | mitigate | empresaId sempre extraído do JWT verificado pelo authMiddleware; nunca aceito do cliente |
| T-05-05 | Elevation of Privilege | rota de historico | mitigate | requireRole('SUPER_ADMIN', 'ADMIN_EMPRESA', 'OPERADOR') em ambas as rotas; OPERADOR pode ver mas não modificar |
</threat_model>

<verification>
1. `pnpm -w build` não deve produzir erros TypeScript
2. `cd apps/api && pnpm test` deve passar todos os testes (incluindo os novos)
3. `curl -s -b "token=<jwt>" http://localhost:4000/api/placas/ABC1234/historico` retorna `{ placa, items, nextCursor }`
4. `curl -s -b "token=<jwt>" http://localhost:4000/api/placas/ABC1234/classificacoes` retorna `{ placa, items }` com `usuario.nome` em cada item
5. `curl -s -b "token=<jwt>" "http://localhost:4000/api/eventos/buscar?placa=ABC&limit=5"` retorna items filtrados
</verification>

<success_criteria>
- schema.prisma contém `@@index([empresaId, placaId, timestamp(sort: Desc)])` no model Evento
- `prisma db push` executado com sucesso
- `GET /api/placas/:numero/historico` funciona com e sem filtros, respeita cursor, retorna thumbnailUrl via presigned URL
- `GET /api/placas/:numero/classificacoes` retorna audit trail completo com `usuario.nome`
- `GET /api/eventos/buscar` aceita todos os filtros, usa cursor pagination, busca parcial case-insensitive em placaNumero
- Nenhuma das queries faz full-table scan (índices cobrem os `WHERE` e `ORDER BY`)
- Todos os testes passam com `pnpm test` no workspace `apps/api`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-plate-history-profile/05-01-SUMMARY.md` com:
- Rotas criadas (paths exatos, resposta shape)
- Índice adicionado ao schema
- Padrão de filtros para reutilização em 05-02 e 05-03
</output>
