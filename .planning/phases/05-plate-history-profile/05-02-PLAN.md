---
id: "05-02"
name: "Plate Profile Page"
phase: "05"
plan: 2
type: execute
wave: 2
autonomous: true
depends_on: ["05-01"]
files_modified:
  - apps/web/src/app/(dashboard)/placas/[numero]/page.tsx
  - apps/web/src/components/dashboard-client.tsx
requirements: [HISTORY-01, HISTORY-02]

must_haves:
  truths:
    - "Clicar em um número de placa no feed abre /placas/ABC1234 em nova aba ou navega para a página"
    - "Página de perfil exibe cabeçalho com número, classificação atual, empresa transportadora e motorista"
    - "Timeline de detecções mostra thumbnail, obra, câmera, direção e timestamp de cada evento"
    - "Timeline de classificações mostra nível anterior → nível atual, usuário responsável e timestamp exato"
    - "Paginação 'Carregar mais' funciona sem reload, appenda próxima página ao final da lista"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/placas/[numero]/page.tsx"
      provides: "Server Component + Client Component para perfil de placa"
      min_lines: 150
    - path: "apps/web/src/components/dashboard-client.tsx"
      provides: "Número de placa no feed como link clicável para /placas/:numero"
  key_links:
    - from: "apps/web/src/app/(dashboard)/placas/[numero]/page.tsx"
      to: "GET /api/placas/:numero/historico"
      via: "fetch no Server Component com credenciais de cookie"
    - from: "apps/web/src/app/(dashboard)/placas/[numero]/page.tsx"
      to: "GET /api/placas/:numero/classificacoes"
      via: "fetch no Server Component — carrega junto com historico"
    - from: "apps/web/src/components/dashboard-client.tsx"
      to: "apps/web/src/app/(dashboard)/placas/[numero]/page.tsx"
      via: "Link href='/placas/{item.placaNumero}'"
---

<objective>
Criar a página de perfil de placa que exibe o histórico completo de detecções e o audit trail
de classificações, e tornar os números de placa no feed clicáveis para navegar até esse perfil.

Purpose: Entrega direta dos requisitos HISTORY-01 e HISTORY-02 — o operador consegue ver em 1
clique o histórico completo de qualquer veículo detectado, incluindo quem classificou e quando.

Output:
- `apps/web/src/app/(dashboard)/placas/[numero]/page.tsx` — Server Component que carrega dados
  e Client Component que gerencia paginação de eventos
- Modificação de `dashboard-client.tsx` para tornar número de placa um link
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/05-plate-history-profile/05-01-SUMMARY.md

<!-- Componente do feed — modificar placa number para ser link -->
@apps/web/src/components/dashboard-client.tsx

<!-- Tipos e helpers existentes -->
@apps/web/src/lib/dashboard.ts

<!-- Referência de estilo — classificação badges -->
@apps/web/src/components/classification-badge.tsx

<!-- Layout do dashboard (route group) -->
@apps/web/src/app/layout.tsx
</context>

<interfaces>
<!-- API shape retornada por 05-01 (de 05-01-SUMMARY.md) -->
```typescript
// GET /api/placas/:numero/historico
type HistoricoResponse = {
  placa: {
    id: string;
    numero: string;
    classificacao: 'LIBERADO' | 'VISITANTE' | 'ATENCAO' | 'SUSPEITO' | 'CRITICO';
    empresaTransportadora: string | null;
    motorista: string | null;
    tipoVeiculo: string | null;
    observacao: string | null;
  };
  items: EventoHistoricoItem[];
  nextCursor: string | null;
};

type EventoHistoricoItem = {
  id: string;
  timestamp: string;       // ISO string
  direcao: 'ENTRADA' | 'SAIDA' | null;
  classificacao: 'LIBERADO' | 'VISITANTE' | 'ATENCAO' | 'SUSPEITO' | 'CRITICO';
  thumbnailUrl: string | null;
  obra: { id: string; nome: string };
  camera: { id: string; codigoLpr: string };
};

// GET /api/placas/:numero/classificacoes
type ClassificacoesResponse = {
  placa: { id: string; numero: string; classificacao: string };
  items: ClassificacaoAuditItem[];
};

type ClassificacaoAuditItem = {
  id: string;
  createdAt: string;       // ISO string
  classificacaoDe: string | null;
  classificacaoPara: string;
  observacao: string | null;
  usuario: { id: string; nome: string };
};
```

<!-- Tipos existentes em dashboard.ts -->
```typescript
export type FeedItem = {
  id: string;
  timestamp: string;
  placaId: string | null;
  placaNumero: string;
  classificacao: 'LIBERADO' | 'VISITANTE' | 'ATENCAO' | 'SUSPEITO' | 'CRITICO';
  direcao: 'ENTRADA' | 'SAIDA' | null;
  thumbnailUrl: string | null;
  obra: { id: string; nome: string };
  camera: { id: string; codigoLpr: string };
};
```

<!-- Cores de classificação existentes (UI-04) -->
```typescript
// getClassificationColor(c) → hex string
// getClassificationLabel(c) → 'Liberado' | 'Visitante' etc
// Ambas exportadas de @/lib/dashboard
```

<!-- ClassificationBadge existente -->
```typescript
// import { ClassificationBadge } from '@/components/classification-badge'
// <ClassificationBadge classificacao="CRITICO" />
```

<!-- resolveApiBaseUrl para fetch no client -->
```typescript
// import { resolveApiBaseUrl } from '@/lib/dashboard'
// Para Server Components, usar process.env.INTERNAL_API_URL ?? 'http://localhost:4000'
// ou fetch com cookies() do next/headers
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tornar número de placa clicável no feed</name>
  <files>apps/web/src/components/dashboard-client.tsx</files>
  <action>
Em `apps/web/src/components/dashboard-client.tsx`, dentro do map do feed (na tag `<article>`),
localizar o elemento que exibe `{item.placaNumero}` (linha ~252):

```tsx
<strong className="text-xl font-bold tracking-wider">{item.placaNumero}</strong>
```

Substituir por um Link do Next.js que navega para a página de perfil:

```tsx
import Link from 'next/link';
// ...
<Link
  href={`/placas/${item.placaNumero}`}
  className="text-xl font-bold tracking-wider text-slate-900 underline-offset-2 hover:underline hover:text-ggtech-blue"
  data-testid="placa-link"
>
  {item.placaNumero}
</Link>
```

Adicionar `import Link from 'next/link';` no topo do arquivo (verificar se já não existe).

O link deve abrir na mesma aba (comportamento padrão do Next.js Link). NÃO usar `target="_blank"`
— o operador deve usar o botão de voltar para retornar ao feed.

Não alterar nenhum outro comportamento do dashboard-client.tsx — apenas troca o `<strong>` pelo `<Link>`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>`dashboard-client.tsx` compila sem erros TypeScript; placa exibida como link `<a href="/placas/ABC1234">` no DOM</done>
</task>

<task type="auto">
  <name>Task 2: Página de perfil de placa /placas/[numero]</name>
  <files>apps/web/src/app/(dashboard)/placas/[numero]/page.tsx</files>
  <action>
Criar a estrutura de diretórios e o arquivo da página:
`apps/web/src/app/(dashboard)/placas/[numero]/page.tsx`

A página é um Server Component que busca dados e passa para um Client Component para
gerenciar a paginação "Carregar mais".

**Estrutura completa do arquivo:**

```tsx
import { cookies } from 'next/headers';
import { notFound } from 'next/navigation';
import { ClassificationBadge } from '@/components/classification-badge';
import { getClassificationColor, getClassificationLabel } from '@/lib/dashboard';
import PlacaHistoricoClient from './historico-client';

// Nota: params é async no Next.js 15
type Props = { params: Promise<{ numero: string }> };

const API_BASE = process.env.INTERNAL_API_URL ?? 'http://localhost:4000';

async function fetchPlacaHistorico(numero: string, cookieHeader: string) {
  const res = await fetch(
    `${API_BASE}/api/placas/${encodeURIComponent(numero)}/historico?limit=20`,
    { headers: { Cookie: cookieHeader }, cache: 'no-store' },
  );
  if (res.status === 404) return null;
  if (!res.ok) throw new Error('Erro ao carregar histórico');
  return res.json() as Promise<{
    placa: {
      numero: string;
      classificacao: 'LIBERADO' | 'VISITANTE' | 'ATENCAO' | 'SUSPEITO' | 'CRITICO';
      empresaTransportadora: string | null;
      motorista: string | null;
      tipoVeiculo: string | null;
      observacao: string | null;
    };
    items: Array<{
      id: string;
      timestamp: string;
      direcao: 'ENTRADA' | 'SAIDA' | null;
      classificacao: 'LIBERADO' | 'VISITANTE' | 'ATENCAO' | 'SUSPEITO' | 'CRITICO';
      thumbnailUrl: string | null;
      obra: { id: string; nome: string };
      camera: { id: string; codigoLpr: string };
    }>;
    nextCursor: string | null;
  }>();
}

async function fetchPlacaClassificacoes(numero: string, cookieHeader: string) {
  const res = await fetch(
    `${API_BASE}/api/placas/${encodeURIComponent(numero)}/classificacoes`,
    { headers: { Cookie: cookieHeader }, cache: 'no-store' },
  );
  if (!res.ok) return { items: [] };
  return res.json() as Promise<{
    items: Array<{
      id: string;
      createdAt: string;
      classificacaoDe: string | null;
      classificacaoPara: string;
      observacao: string | null;
      usuario: { id: string; nome: string };
    }>;
  }>();
}

export default async function PlacaProfilePage({ params }: Props) {
  const { numero } = await params;
  const cookieStore = await cookies();
  const cookieHeader = cookieStore.toString();

  const [historico, classificacoes] = await Promise.all([
    fetchPlacaHistorico(numero, cookieHeader),
    fetchPlacaClassificacoes(numero, cookieHeader),
  ]);

  if (!historico) notFound();

  const { placa, items, nextCursor } = historico;

  return (
    <div className="mx-auto max-w-5xl space-y-8 p-6">
      {/* Cabeçalho da placa */}
      <section className="flex flex-col gap-4 rounded-xl border border-slate-200 bg-white p-6 shadow-sm sm:flex-row sm:items-start sm:justify-between">
        <div>
          <div className="flex items-center gap-3">
            <h1 className="font-heading text-3xl font-bold tracking-widest text-slate-900">
              {placa.numero}
            </h1>
            <ClassificationBadge classificacao={placa.classificacao} />
          </div>
          <dl className="mt-3 grid grid-cols-1 gap-x-6 gap-y-1 text-sm text-slate-600 sm:grid-cols-2">
            {placa.empresaTransportadora && (
              <>
                <dt className="font-medium text-slate-500">Transportadora</dt>
                <dd>{placa.empresaTransportadora}</dd>
              </>
            )}
            {placa.motorista && (
              <>
                <dt className="font-medium text-slate-500">Motorista</dt>
                <dd>{placa.motorista}</dd>
              </>
            )}
            {placa.tipoVeiculo && (
              <>
                <dt className="font-medium text-slate-500">Tipo de veículo</dt>
                <dd>{placa.tipoVeiculo}</dd>
              </>
            )}
            {placa.observacao && (
              <>
                <dt className="font-medium text-slate-500">Observação</dt>
                <dd>{placa.observacao}</dd>
              </>
            )}
          </dl>
        </div>
      </section>

      {/* Timeline de detecções — Client Component para "Carregar mais" */}
      <section>
        <h2 className="font-heading mb-4 text-xl font-bold text-slate-900">
          Detecções
        </h2>
        <PlacaHistoricoClient
          placaNumero={placa.numero}
          initialItems={items}
          initialNextCursor={nextCursor}
        />
      </section>

      {/* Audit trail de classificações */}
      <section>
        <h2 className="font-heading mb-4 text-xl font-bold text-slate-900">
          Histórico de Classificações
        </h2>
        {classificacoes.items.length === 0 ? (
          <p className="text-sm text-slate-500">Nenhuma classificação registrada.</p>
        ) : (
          <ol className="relative border-l border-slate-200">
            {classificacoes.items.map((entry) => (
              <li key={entry.id} className="mb-6 ml-6">
                <span
                  className="absolute -left-1.5 mt-1.5 h-3 w-3 rounded-full border-2 border-white"
                  style={{
                    backgroundColor: getClassificationColor(
                      entry.classificacaoPara as 'LIBERADO' | 'VISITANTE' | 'ATENCAO' | 'SUSPEITO' | 'CRITICO',
                    ),
                  }}
                />
                <time className="mb-1 text-xs text-slate-400">
                  {new Date(entry.createdAt).toLocaleString('pt-BR')}
                </time>
                <p className="text-sm font-medium text-slate-800">
                  {entry.classificacaoDe
                    ? `${getClassificationLabel(entry.classificacaoDe as 'LIBERADO' | 'VISITANTE' | 'ATENCAO' | 'SUSPEITO' | 'CRITICO')} → `
                    : 'Classificação inicial: '}
                  {getClassificationLabel(entry.classificacaoPara as 'LIBERADO' | 'VISITANTE' | 'ATENCAO' | 'SUSPEITO' | 'CRITICO')}
                </p>
                <p className="text-xs text-slate-500">
                  por {entry.usuario.nome}
                  {entry.observacao ? ` — "${entry.observacao}"` : ''}
                </p>
              </li>
            ))}
          </ol>
        )}
      </section>
    </div>
  );
}
```

Em seguida, criar o Client Component de paginação no mesmo diretório:
`apps/web/src/app/(dashboard)/placas/[numero]/historico-client.tsx`

```tsx
'use client';

import { useState } from 'react';
import { getClassificationColor } from '@/lib/dashboard';

type EventoItem = {
  id: string;
  timestamp: string;
  direcao: 'ENTRADA' | 'SAIDA' | null;
  classificacao: 'LIBERADO' | 'VISITANTE' | 'ATENCAO' | 'SUSPEITO' | 'CRITICO';
  thumbnailUrl: string | null;
  obra: { id: string; nome: string };
  camera: { id: string; codigoLpr: string };
};

type Props = {
  placaNumero: string;
  initialItems: EventoItem[];
  initialNextCursor: string | null;
};

export default function PlacaHistoricoClient({ placaNumero, initialItems, initialNextCursor }: Props) {
  const [items, setItems] = useState<EventoItem[]>(initialItems);
  const [cursor, setCursor] = useState<string | null>(initialNextCursor);
  const [loading, setLoading] = useState(false);

  async function loadMore() {
    if (!cursor || loading) return;
    setLoading(true);
    try {
      const res = await fetch(
        `/api/placas-proxy/${encodeURIComponent(placaNumero)}/historico?cursor=${cursor}&limit=20`,
        { credentials: 'include' },
      );
      if (!res.ok) throw new Error('Erro ao carregar mais eventos');
      const data = await res.json() as { items: EventoItem[]; nextCursor: string | null };
      setItems((prev) => [...prev, ...data.items]);
      setCursor(data.nextCursor);
    } catch {
      // erro silencioso — botão permanece disponível
    } finally {
      setLoading(false);
    }
  }

  if (items.length === 0) {
    return <p className="text-sm text-slate-500">Nenhuma detecção registrada.</p>;
  }

  return (
    <div className="space-y-3">
      {items.map((evento) => (
        <article
          key={evento.id}
          className="grid grid-cols-[120px_1fr] items-center gap-3 rounded-xl border border-slate-200 bg-white p-3 shadow-sm"
          style={{
            borderLeftColor: getClassificationColor(evento.classificacao),
            borderLeftWidth: 6,
          }}
        >
          {/* Thumbnail */}
          <div className="flex h-[72px] w-[120px] items-center justify-center overflow-hidden rounded-lg bg-slate-100 text-xs text-slate-400">
            {evento.thumbnailUrl ? (
              <img
                src={evento.thumbnailUrl}
                alt={`Leitura em ${evento.obra.nome}`}
                className="h-full w-full object-cover"
              />
            ) : (
              'Sem foto'
            )}
          </div>

          {/* Info */}
          <div>
            <div className="flex flex-wrap items-center gap-2 text-sm font-medium text-slate-800">
              <span>{evento.obra.nome}</span>
              <span className="text-slate-400">·</span>
              <span className="text-slate-500">{evento.camera.codigoLpr}</span>
            </div>
            <div className="mt-1 flex flex-wrap gap-3 text-xs text-slate-500">
              <span>{new Date(evento.timestamp).toLocaleString('pt-BR')}</span>
              {evento.direcao && <span>{evento.direcao}</span>}
            </div>
          </div>
        </article>
      ))}

      {cursor && (
        <div className="pt-2 text-center">
          <button
            onClick={() => void loadMore()}
            disabled={loading}
            className="rounded-lg bg-ggtech-blue px-6 py-2 text-sm font-medium text-white hover:opacity-90 disabled:opacity-50"
          >
            {loading ? 'Carregando...' : 'Carregar mais'}
          </button>
        </div>
      )}
    </div>
  );
}
```

**ATENÇÃO — Proxy de API no Next.js:** O Client Component não pode chamar o Express diretamente
por CORS em produção. Criar uma rota proxy em Next.js:
`apps/web/src/app/api/placas-proxy/[numero]/historico/route.ts`

```typescript
import { type NextRequest } from 'next/server';
import { cookies } from 'next/headers';

const API_BASE = process.env.INTERNAL_API_URL ?? 'http://localhost:4000';

export async function GET(
  req: NextRequest,
  { params }: { params: Promise<{ numero: string }> },
) {
  const { numero } = await params;
  const cookieStore = await cookies();
  const search = req.nextUrl.searchParams.toString();

  const upstream = await fetch(
    `${API_BASE}/api/placas/${encodeURIComponent(numero)}/historico${search ? `?${search}` : ''}`,
    {
      headers: { Cookie: cookieStore.toString() },
      cache: 'no-store',
    },
  );

  const data = await upstream.json();
  return Response.json(data, { status: upstream.status });
}
```

Isso permite que o `PlacaHistoricoClient` chame `/api/placas-proxy/:numero/historico` (Next.js)
que por sua vez chama o Express internamente, mantendo o cookie de autenticação.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `pnpm tsc --noEmit` sem erros em apps/web
    - Arquivo `apps/web/src/app/(dashboard)/placas/[numero]/page.tsx` existe
    - Arquivo `apps/web/src/app/(dashboard)/placas/[numero]/historico-client.tsx` existe
    - Arquivo `apps/web/src/app/api/placas-proxy/[numero]/historico/route.ts` existe
    - `dashboard-client.tsx` exibe número de placa como link `href="/placas/{numero}"`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser → Next.js proxy route | Cookie de auth repassado pelo servidor Next.js para o Express interno |
| Next.js Server Component → Express API | Chamada server-to-server interna; cookieHeader extraído de cookies() |
| URL param [numero] | Número de placa na URL — encodeURIComponent antes de usar em fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | PlacaProfilePage (Server Component) | mitigate | Cookies do usuário repassados para o Express que valida o JWT; sem dados de outra empresa possíveis |
| T-05-07 | Spoofing | Cookie repasse no proxy route | mitigate | Cookie é lido pelo Next.js server via `cookies()` — nunca aceito do request body; cliente não pode forjar |
| T-05-08 | Information Disclosure | thumbnailUrl presigned | accept | URLs presignadas têm TTL 5 min (comportamento padrão do Garage); risco baixo para ambiente B2B |
| T-05-09 | Denial of Service | "Carregar mais" ilimitado | mitigate | limit hardcoded em 20 por página no client; máximo 100 no backend |
</threat_model>

<verification>
1. Navegar até o dashboard — números de placa são links clicáveis
2. Clicar em uma placa → abre `/placas/ABC1234` com cabeçalho (número + badge de classificação)
3. Timeline de detecções exibe thumbnails, obra, câmera, direção e horário
4. "Carregar mais" append novos eventos sem reload de página (visível com 20+ eventos)
5. Histórico de classificações mostra quem classificou e quando em ordem cronológica inversa
6. Placa inexistente → Next.js `notFound()` retorna 404
</verification>

<success_criteria>
- Número de placa no feed é um link `<a href="/placas/{numero}">`
- Página `/placas/[numero]` carrega cabeçalho com classificação, transportadora, motorista
- Detecções paginam com "Carregar mais" (cursor-based, sem reload)
- Audit trail de classificações mostra `usuario.nome` e timestamp em cada entrada
- TypeScript compila sem erros (`pnpm tsc --noEmit`)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-plate-history-profile/05-02-SUMMARY.md` com:
- Rotas Next.js criadas
- Componentes criados (page.tsx, historico-client.tsx)
- Proxy route path para referência em 05-03 se necessário
</output>
