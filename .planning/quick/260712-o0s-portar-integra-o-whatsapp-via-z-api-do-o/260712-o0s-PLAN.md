---
type: quick
slug: 260712-o0s-portar-integra-o-whatsapp-via-z-api-do-o
autonomous: true
files_modified:
  - packages/database/prisma/schema.prisma
  - packages/database/prisma/migrations/**
  - apps/api/src/jobs/alert-worker.ts
  - apps/api/src/jobs/alert-worker.test.ts
  - apps/api/src/services/whatsapp.ts
  - apps/api/src/services/whatsapp.test.ts
  - apps/api/src/routes/configuracoes-alerta.ts
  - apps/api/src/routes/configuracoes-whatsapp.ts
  - apps/api/src/routes/admin.ts
  - apps/api/src/index.ts
  - apps/api/src/infra/zapi/zapi.service.ts
  - apps/web/src/app/(admin)/configuracoes/whatsapp/whatsapp-client.tsx
  - apps/web/src/app/api/configuracoes-whatsapp-proxy/**
  - apps/web/src/app/(admin)/configuracoes/alertas/**
  - apps/web/src/app/(superadmin)/admin/**
  - apps/web/src/components/sidebar.tsx
  - .env.example
  - docker-compose.yml
  - docker-compose.vps.yml
  - CLAUDE.md
---

<objective>
Portar a integração WhatsApp via Z-API do projeto de referência opencheck para o cargo-sentinel, substituindo COMPLETAMENTE a integração Evolution API existente. O fluxo de provisionamento segue o modelo do opencheck: o SUPERADMIN cadastra/valida a instância Z-API (instanceId + token + clientToken) por empresa; o tenant (ADMIN_EMPRESA) só vê status/QR/grupos/config de envio/testar — nunca as credenciais brutas.

Decisões já confirmadas (NÃO revisitar):
1. Substituir Evolution API por Z-API completamente (remover Evolution).
2. SUPERADMIN provisiona a instância Z-API por empresa; tenant não vê token puro.

Purpose: unificar o canal de WhatsApp em Z-API com provisionamento centralizado e seguro.
Output: backend (schema+migração+rotas+worker), superadmin UI, tenant UI ajustado, Evolution removido, config/docs atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Cargo-sentinel — estado atual (revisar, não assumir correto)
@apps/api/src/infra/zapi/zapi.service.ts
@apps/api/src/routes/configuracoes-whatsapp.ts
@apps/api/src/jobs/alert-worker.ts
@apps/api/src/routes/admin.ts
@apps/api/src/index.ts
@apps/api/src/services/whatsapp.ts
@packages/database/prisma/schema.prisma
@apps/web/src/app/(admin)/configuracoes/whatsapp/whatsapp-client.tsx
@apps/web/src/app/api/configuracoes-whatsapp-proxy/route.ts
@apps/web/src/app/(superadmin)/admin/page.tsx
@apps/web/src/components/sidebar.tsx
@.env.example

# Referência opencheck (LER os arquivos reais para copiar padrões — não editar)
# D:\GGTRONIC\Desenvolvimeto\ctrl-safe\opencheck\apps\api\src\modules\superadmin\superadmin.routes.ts  (WhatsApp ~L95-211)
# D:\GGTRONIC\Desenvolvimeto\ctrl-safe\opencheck\apps\api\src\modules\notificacoes\notificacoes.routes.ts
# D:\GGTRONIC\Desenvolvimeto\ctrl-safe\opencheck\apps\api\src\modules\configuracoes\configuracoes.routes.ts
# D:\GGTRONIC\Desenvolvimeto\ctrl-safe\opencheck\apps\web\app\(superadmin)\clientes\[id]\page.tsx  (aba WhatsApp ~L420-515)
# D:\GGTRONIC\Desenvolvimeto\ctrl-safe\opencheck\apps\web\app\(dashboard)\configuracoes\notificacoes\page.tsx

<interfaces>
Cliente Z-API já existe em apps/api/src/infra/zapi/zapi.service.ts (revisar, reusar):
  export interface ZapiConfig { instanceId: string; token: string; clientToken?: string | null }
  export interface ZapiGrupo { id: string; nome: string }
  export function zapiConfigFrom(cfg): ZapiConfig | null  // fallback clientToken -> process.env.ZAPI_CLIENT_TOKEN
  export async function getStatus(cfg): Promise<{ connected: boolean; smartphoneConnected?: boolean; error?: string }>
  export async function getQrCodeImage(cfg): Promise<string | null>  // data URI
  export async function disconnect(cfg): Promise<void>
  export async function listGroups(cfg): Promise<ZapiGrupo[]>
  export async function sendWhatsAppText(cfg, to, message): Promise<void>

Model Prisma ConfiguracaoWhatsApp (por empresa, @@unique([empresaId]) já presente):
  empresaId, ativo, zapiInstanceId?, zapiToken?, zapiClientToken?,
  whatsappInstStatus (enum DESCONECTADO|AGUARDANDO_QR|CONECTADO),
  whatsappDestino?, whatsappGrupoJid?, whatsappGrupoNome?,
  classificacoesAlerta Classificacao[]  // vazio = todas

Admin router (apps/api/src/routes/admin.ts) já é montado atrás de requireRole('SUPER_ADMIN') no index.ts em /api/admin.
Proxies BFF (Next) chamam a API interna via cookies() — ver apps/web/src/app/api/configuracoes-whatsapp-proxy/route.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema, migração e worker — remover Evolication, alertas via Z-API apenas</name>
  <files>packages/database/prisma/schema.prisma, packages/database/prisma/migrations/, apps/api/src/jobs/alert-worker.ts, apps/api/src/jobs/alert-worker.test.ts, apps/api/src/services/whatsapp.ts, apps/api/src/services/whatsapp.test.ts, apps/api/src/routes/configuracoes-alerta.ts, apps/api/src/index.ts</files>
  <action>
Remover completamente o caminho Evolution API e o model per-obra `ConfiguracaoAlerta` (confirmado exclusivo do fluxo Evolution — usado só em configuracoes-alerta.ts, alertas-client.tsx, alert-worker.ts e testes).

1. schema.prisma:
   - Remover o model `ConfiguracaoAlerta` inteiro (linhas ~167-185) e a relação `configuracoes ConfiguracaoAlerta[]` em `Empresa` e em `Obra`.
   - Confirmar que `ConfiguracaoWhatsApp` mantém `@@unique([empresaId])` (já presente — necessário para upsert). NÃO alterar os demais campos.
2. Gerar migração Prisma: rodar `pnpm --filter @cargo-sentinel/database exec prisma migrate dev --name remove_evolution_zapi_provisioning` (requer DATABASE_URL apontando para um Postgres acessível). Se `migrate dev` não puder conectar no ambiente, gerar o SQL com `prisma migrate diff` e criar o diretório de migração manualmente com o DROP TABLE correspondente. Rodar `pnpm --filter @cargo-sentinel/database db:generate` ao final.
3. Excluir apps/api/src/services/whatsapp.ts e apps/api/src/services/whatsapp.test.ts (cliente Evolution).
4. Excluir apps/api/src/routes/configuracoes-alerta.ts.
5. index.ts: remover `import configuracoesAlertaRouter` e a linha `app.use('/api/configuracoes-alerta', ...)`.
6. Reescrever alert-worker.ts, bloco `alert:whatsapp`:
   - Remover import de `sendAlertaWhatsApp` e todo o bloco `configuracoesObra` / envio via Evolution.
   - Manter a dedup Redis existente.
   - Manter apenas o envio Z-API com o gate: buscar `configWhatsApp` por empresaId; enviar somente se `configWhatsApp.ativo && whatsappInstStatus === 'CONECTADO'` e `zapiConfigFrom(configWhatsApp)` retornar config; gate de classificação = `classificacoesAlerta.length === 0 || includes(payload.classificacao)`; enviar para `whatsappDestino` (se houver) e para `whatsappGrupoJid` (se houver), cada envio em try/catch independente com console.error na falha.
7. alert-worker.test.ts: remover mocks/casos que dependem de `ConfiguracaoAlerta`/Evolution; ajustar para o novo caminho Z-API-only (mockar `sendWhatsAppText` de infra/zapi e `prisma.configuracaoWhatsApp.findUnique`). Manter os testes de dedup e de format de mensagem.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/api build && pnpm --filter @cargo-sentinel/api test:unit</automated>
  </verify>
  <done>Model ConfiguracaoAlerta e serviço Evolution removidos; migração criada; alert-worker envia só via Z-API com gate correto; build e testes unitários da API passam.</done>
</task>

<task type="auto">
  <name>Task 2: Rotas tenant Z-API (ADMIN_EMPRESA) — sem credenciais brutas</name>
  <files>apps/api/src/routes/configuracoes-whatsapp.ts, apps/api/src/infra/zapi/zapi.service.ts</files>
  <action>
Refazer as rotas tenant seguindo o padrão de opencheck notificacoes.routes.ts + configuracoes.routes.ts. O tenant NUNCA lê nem grava zapiInstanceId/zapiToken/zapiClientToken. Todas as rotas com `requireRole('ADMIN_EMPRESA')` e usando `req.tenantClient!` + `req.user!.empresaId!`.

Rotas (base /api/configuracoes-whatsapp):
- GET `/` — retorna a config SEM credenciais: `{ ativo, whatsappInstStatus, whatsappDestino, whatsappGrupoJid, whatsappGrupoNome, classificacoesAlerta, instanciaVinculada }` onde `instanciaVinculada = !!(zapiInstanceId && zapiToken)`. Se não existir registro, criar default vazio (ativo:true, classificacoesAlerta:['SUSPEITO','CRITICO']) e retornar mascarado. Remover a exposição de zapi* do JSON.
- PUT `/` — config de ENVIO apenas: aceitar somente `{ ativo, whatsappDestino, whatsappGrupoJid, whatsappGrupoNome, classificacoesAlerta }`. Ignorar/rejeitar qualquer zapi* no body. Fazer update (não upsert de credenciais) do registro da empresa.
- GET `/status` (mudar de POST para GET) — `zapiConfigFrom(config)`; se null → `{ status: 'SEM_INSTANCIA' }`; senão `getStatus`, atualizar whatsappInstStatus no banco (CONECTADO se connected; senão preserva AGUARDANDO_QR ou DESCONECTADO), retornar `{ status, smartphoneConnected, erro }`.
- GET `/qrcode` — se sem instância → 400 "Instância não vinculada. Solicite ao administrador da plataforma."; se já conectado → `{ status:'CONECTADO', qrCode:null }`; senão `getQrCodeImage`, se veio QR marca AGUARDANDO_QR, retorna `{ status, qrCode }`.
- GET `/grupos` — `listGroups`; tratar 429 (retornar 429 com mensagem de rate limit) e falha genérica (502).
- POST `/desconectar` — `disconnect(zapi)` + marcar whatsappInstStatus DESCONECTADO (mantém credenciais e grupo/número).
- POST `/testar` — exige destino ou grupo e whatsappInstStatus CONECTADO; envia `sendWhatsAppText` para número e/ou grupo com texto "✅ Cargo Sentinel — Conexão WhatsApp testada com sucesso!"; 502 em falha.

Revisar zapi.service.ts: já cobre todos os endpoints necessários — reusar como está, sem alteração salvo pequenos ajustes se o build acusar tipos.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/api build</automated>
  </verify>
  <done>Rotas tenant não expõem nem gravam zapiInstanceId/token/clientToken; status/qr/grupos/desconectar/testar/config-envio implementados; build da API passa.</done>
</task>

<task type="auto">
  <name>Task 3: Rotas superadmin de provisionamento Z-API por empresa</name>
  <files>apps/api/src/routes/admin.ts</files>
  <action>
Adicionar ao adminRouter (já protegido por requireRole('SUPER_ADMIN')) o bloco WhatsApp por empresa, portando superadmin.routes.ts do opencheck (~L95-211) para Express + prisma direto (sem tenantClient — superadmin usa `prisma` raw).

- GET `/empresas/:id/whatsapp` — buscar `prisma.configuracaoWhatsApp.findUnique({ where:{ empresaId:id } })`. Se sem zapiInstanceId → `{ vinculada:false }`. Senão retornar `{ vinculada:true, instanceId, tokenMask: token ? token.slice(0,4)+'…'+token.slice(-4) : null, temClientToken: !!zapiClientToken, status: whatsappInstStatus, grupoNome }`. NUNCA retornar token puro.
- PUT `/empresas/:id/whatsapp` — body `{ instanceId, token, clientToken? }`. Validar obrigatórios (400 se faltar). Verificar empresa existe (404). Montar `zapiConfigFrom({ zapiInstanceId, zapiToken, zapiClientToken })`, chamar `getStatus` para VALIDAR credenciais antes de salvar — em erro retornar 400 "Credenciais inválidas ou instância inacessível na Z-API: ...". Fazer `prisma.configuracaoWhatsApp.upsert({ where:{ empresaId:id }, update/create })` gravando as credenciais + whatsappInstStatus (CONECTADO se connected, senão DESCONECTADO) + ativo:true quando conectado. Retornar `{ ok:true, status }`.
- POST `/empresas/:id/whatsapp/desconectar` — carregar credenciais, `disconnect(zapi)`, `updateMany` whatsappInstStatus DESCONECTADO (mantém vínculo).
- DELETE `/empresas/:id/whatsapp` — `disconnect` best-effort, depois zerar zapiInstanceId/zapiToken/zapiClientToken/whatsappGrupoJid/whatsappGrupoNome + whatsappInstStatus DESCONECTADO + ativo:false.

Importar getStatus/disconnect/zapiConfigFrom de ../infra/zapi/zapi.service.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/api build</automated>
  </verify>
  <done>Superadmin pode vincular (com validação getStatus), consultar mascarado, desconectar e remover vínculo Z-API por empresa; build da API passa.</done>
</task>

<task type="auto">
  <name>Task 4: UI superadmin de provisionamento + proxy BFF</name>
  <files>apps/web/src/app/(superadmin)/admin/empresas/[id]/whatsapp/page.tsx, apps/web/src/app/(superadmin)/admin/empresas/[id]/whatsapp/whatsapp-provision-client.tsx, apps/web/src/app/api/admin-whatsapp-proxy/[id]/route.ts, apps/web/src/app/(superadmin)/admin/page.tsx</files>
  <action>
Portar a aba WhatsApp do opencheck `(superadmin)/clientes/[id]/page.tsx` (~L420-515) para uma página dedicada de provisionamento por empresa.

1. Nova página server `(superadmin)/admin/empresas/[id]/whatsapp/page.tsx`: valida sessão SUPER_ADMIN (padrão das outras páginas superadmin), renderiza o client com `empresaId = params.id` (Next 15 async params — `const { id } = await params`).
2. `whatsapp-provision-client.tsx` ('use client'): dois estados —
   - NÃO vinculada: formulário com instanceId + token + clientToken (opcional) → PUT (valida via getStatus no backend); erros exibidos inline.
   - Vinculada: card com instanceId, token mascarado (tokenMask), badge temClientToken, badge status; botões "Desconectar sessão" (POST) e "Remover vínculo" (DELETE, com confirmação).
   Após cada ação, refetch do GET.
3. Proxy BFF `apps/web/src/app/api/admin-whatsapp-proxy/[id]/route.ts` com GET/PUT/POST/DELETE encaminhando para `${INTERNAL_API_URL}/api/admin/empresas/{id}/whatsapp` (POST → sufixo `/desconectar`) repassando `cookies()` — mesmo padrão do configuracoes-whatsapp-proxy existente. (Se preferir, criar sub-rota `[id]/desconectar/route.ts` para o POST em vez de sufixo — manter consistente.)
4. admin/page.tsx: na coluna Ações de cada empresa, adicionar um `<Link href={`/admin/empresas/${empresa.id}/whatsapp`}>` (ícone/label "WhatsApp") ao lado de Suspend/Impersonate.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/web build</automated>
  </verify>
  <done>Superadmin acessa /admin/empresas/[id]/whatsapp, vincula/valida instância, vê token mascarado, desconecta e remove vínculo; build do web passa.</done>
</task>

<task type="auto">
  <name>Task 5: UI tenant ajustada + sidebar + remover página de alertas Evolution</name>
  <files>apps/web/src/app/(admin)/configuracoes/whatsapp/whatsapp-client.tsx, apps/web/src/app/api/configuracoes-whatsapp-proxy/status/route.ts, apps/web/src/app/api/configuracoes-whatsapp-proxy/testar/route.ts, apps/web/src/app/api/configuracoes-whatsapp-proxy/desconectar/route.ts, apps/web/src/components/sidebar.tsx, apps/web/src/app/(admin)/configuracoes/alertas/</files>
  <action>
Ajustar o tenant para o modelo provisionado (sem credenciais) e remover a antiga UI Evolution.

1. whatsapp-client.tsx: REMOVER os campos e estados de zapiInstanceId/zapiToken/zapiClientToken e o card "Configurações Gerais" de credenciais. Manter/refazer:
   - Card Status: exibe whatsappInstStatus; se `instanciaVinculada===false`, mostrar aviso "Instância ainda não provisionada — solicite ao administrador da plataforma" e ocultar QR/testar.
   - Botão "Verificar Status" → GET /status (não mais POST). Botão "Obter QR Code" → GET /qrcode (exibe imagem). Botão "Desconectar" → POST /desconectar.
   - Card Destinatários: número individual + carregar/selecionar grupo (mantém padrão atual).
   - Card Classificações de alerta (mantém).
   - Botão "Testar envio" → POST /testar (alerta sucesso/erro).
   - Botão "Salvar" → PUT / com apenas { ativo, whatsappDestino, whatsappGrupoJid, whatsappGrupoNome, classificacoesAlerta }.
   Ajustar o tipo `ConfiguracaoWhatsApp` local para refletir a resposta sem zapi* e com `instanciaVinculada`.
2. Proxies: ajustar status/route.ts para GET (era POST); adicionar testar/route.ts (POST) e desconectar/route.ts (POST) encaminhando para os endpoints tenant correspondentes. Manter qrcode e grupos.
3. sidebar.tsx: remover o `<Link href="/configuracoes/alertas">` "Alertas (Evolution)"; manter um único item de configuração WhatsApp (renomear "Configurar Z-API" para "WhatsApp" se desejar). Remover import Bell se ficar sem uso.
4. Excluir a pasta `apps/web/src/app/(admin)/configuracoes/alertas/` (page.tsx + alertas-client.tsx) — dependia do fluxo Evolution/ConfiguracaoAlerta removido.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/web build</automated>
  </verify>
  <done>Tenant não vê campos de credenciais; usa status(GET)/qr/grupos/testar/desconectar/config-envio; página alertas Evolution removida; sidebar com item único; build do web passa.</done>
</task>

<task type="auto">
  <name>Task 6: Cleanup de config/infra e docs + verificação final do monorepo</name>
  <files>.env.example, docker-compose.yml, docker-compose.vps.yml, CLAUDE.md</files>
  <action>
Remover resquícios de Evolution e registrar a mudança de decisão.

1. .env.example: remover o bloco Evolution API (EVOLUTION_API_KEY, EVOLUTION_INSTANCE_NAME e comentário EVOLUTION_API_URL). Manter o bloco `ZAPI_CLIENT_TOKEN` (fallback global opcional).
2. docker-compose.vps.yml: remover o service `evolution-api` inteiro (linhas ~85-109), o volume `evolution-instances`, o `depends_on` para evolution e as envs `EVOLUTION_API_URL/EVOLUTION_API_KEY/EVOLUTION_INSTANCE_NAME` do service `api` (~L150-153).
3. docker-compose.yml: remover qualquer service/env Evolution equivalente (verificar com grep antes).
4. CLAUDE.md: na seção de stack/decisões, substituir a decisão "Evolution API v2.3.7" por "Z-API" — atualizar a tabela Recommended Stack (linha WhatsApp alerts), a seção Key Decisions #5, a tabela "What NOT to Use" (remover/ajustar a linha Evolution) e Version Notes. Registrar explicitamente: "Decisão revista (2026-07-12): Z-API substitui Evolution API — provisionamento centralizado por superadmin, sem servidor de licença externo."
5. Verificação final: garantir que não resta nenhuma referência a Evolution no código/infra (fora de .planning/ histórico).
  </action>
  <verify>
    <automated>! grep -rniE "evolution" --include=*.ts --include=*.tsx --include=*.yml --include=.env.example apps packages docker-compose.yml docker-compose.vps.yml .env.example && pnpm --filter @cargo-sentinel/api build && pnpm --filter @cargo-sentinel/web build</automated>
  </verify>
  <done>Nenhuma referência a Evolution em código/infra/env; CLAUDE.md reflete Z-API; builds de api e web passam.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cargo-sentinel/api build` e `test:unit` passam.
- `pnpm --filter @cargo-sentinel/web build` passa.
- `grep -rniE evolution` em apps/packages/infra retorna vazio.
- Migração Prisma criada removendo ConfiguracaoAlerta.
- Tenant nunca recebe/grava zapiInstanceId/zapiToken/zapiClientToken (auditar respostas GET).
- Superadmin GET retorna somente tokenMask + temClientToken.
</verification>

<success_criteria>
- Evolution API removido de código, infra, env e docs (decisão registrada no CLAUDE.md).
- Superadmin provisiona e valida (via getStatus) a instância Z-API por empresa; token exibido mascarado; desconectar e remover vínculo funcionam.
- Tenant vê apenas status/QR/grupos/config de envio/testar; sem acesso a credenciais.
- alert-worker dispara alertas somente via Z-API com o gate (ativo + CONECTADO + zapiConfig + classificação permitida + destino/grupo), cada envio em try/catch.
- Arquivos de debug soltos na raiz e o console.error em lpr.ts NÃO são tocados (fora de escopo).
</success_criteria>

<output>
Após completar, criar `.planning/quick/260712-o0s-portar-integra-o-whatsapp-via-z-api-do-o/260712-o0s-SUMMARY.md` com arquivos alterados, decisão de stack revista e resultado das verificações.
</output>