---
phase: quick-260712-t5s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - packages/database/prisma/migrations/
  - apps/api/src/routes/admin.ts
  - apps/api/src/routes/placas.ts
  - apps/api/src/routes/relatorios.ts
  - apps/web/src/app/api/admin-empresa-proxy/[id]/route.ts
  - apps/web/src/app/api/admin-usuarios-proxy/[id]/[usuarioId]/route.ts
  - apps/web/src/app/(superadmin)/admin/empresas/[id]/empresa-detail-shell.tsx
  - apps/web/src/app/(superadmin)/admin/empresas/[id]/usuarios-tab.tsx
  - apps/web/src/app/(superadmin)/admin/empresas/[id]/whatsapp/whatsapp-provision-client.tsx
autonomous: true
requirements: [SADMIN-EMPRESA-DELETE, SADMIN-USUARIOS-DELETE, SADMIN-WHATSAPP-EDIT]

must_haves:
  truths:
    - "Super admin pode editar nome/CNPJ de uma empresa na aba Geral"
    - "Super admin pode excluir uma empresa (cascata) confirmando o nome digitado"
    - "Super admin pode editar nome/email de um usuário e excluí-lo"
    - "Excluir um usuário com histórico de classificação NÃO falha por foreign key"
    - "Super admin pode reeditar credenciais Z-API de uma empresa já vinculada"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "ClassificacaoHistorico.usuarioId e Relatorio.criadoPor nullable com onDelete SetNull"
      contains: "onDelete: SetNull"
    - path: "apps/api/src/routes/admin.ts"
      provides: "DELETE /empresas/:id (cascata) e DELETE /empresas/:id/usuarios/:usuarioId"
      contains: "router.delete"
    - path: "apps/web/src/app/api/admin-empresa-proxy/[id]/route.ts"
      provides: "handler DELETE proxy empresa"
      contains: "export async function DELETE"
    - path: "apps/web/src/app/api/admin-usuarios-proxy/[id]/[usuarioId]/route.ts"
      provides: "handler DELETE proxy usuário"
      contains: "export async function DELETE"
  key_links:
    - from: "empresa-detail-shell.tsx"
      to: "/api/admin-empresa-proxy/:id"
      via: "PATCH (editar) e DELETE (excluir)"
      pattern: "admin-empresa-proxy"
    - from: "usuarios-tab.tsx"
      to: "/api/admin-usuarios-proxy/:id/:usuarioId"
      via: "PUT (nome/email) e DELETE"
      pattern: "admin-usuarios-proxy"
    - from: "admin.ts DELETE /empresas/:id"
      to: "prisma.$transaction deleteMany em ordem de dependência"
      via: "cascata manual"
      pattern: "\\$transaction"
---

<objective>
Completar o CRUD do painel super admin do Cargo Sentinel: adicionar edição e exclusão de Empresa, edição/exclusão de Usuários, e reedição de credenciais da conta WhatsApp (Z-API), em `apps/web/src/app/(superadmin)/admin/empresas/[id]/`.

Purpose: As telas hoje só listam/criam. Falta fechar o ciclo de gestão (editar/excluir) que o super admin precisa para operar as empresas.
Output: Schema nullable (SetNull) + migração aplicada, rotas DELETE/PUT estendidas no admin.ts, 2 handlers de proxy novos, e 3 frentes de UI (Geral, Usuários, WhatsApp) com editar/excluir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<decisions_locked>
1. Excluir EMPRESA: SEMPRE em cascata (apaga obras, câmeras, eventos, placas, histórico, usuários, config WhatsApp, relatórios), com confirmação digitando o nome exato da empresa.
2. Excluir USUÁRIO: SEMPRE permitido, mesmo com histórico. Requer tornar `ClassificacaoHistorico.usuarioId` e `Relatorio.criadoPor` nullable com `onDelete: SetNull` para desvincular em vez de bloquear.
</decisions_locked>

<interfaces>
Schema atual (packages/database/prisma/schema.prisma):
```prisma
model ClassificacaoHistorico {
  usuarioId  String
  usuario    User   @relation(fields: [usuarioId], references: [id])   // sem onDelete = Restrict
}
model Relatorio {
  criadoPor  String
  usuario    User   @relation("RelatoriosUsuario", fields: [criadoPor], references: [id])  // sem onDelete
}
```

Padrão dos proxies BFF (apps/web/src/app/api/admin-*-proxy):
```ts
const API_BASE = process.env.INTERNAL_API_URL ?? 'http://localhost:4000';
export async function DELETE(_req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const cookieStore = await cookies();
  const upstream = await fetch(`${API_BASE}/api/admin/empresas/${id}`, {
    method: 'DELETE',
    headers: { Cookie: cookieStore.toString() },
  });
  return Response.json(await upstream.json(), { status: upstream.status });
}
```

Rotas admin.ts existentes: PATCH /empresas/:id (nome/cnpj JÁ funciona), PUT /empresas/:id/usuarios/:usuarioId (nome/role/ativo, 403 se alvo SUPER_ADMIN), PUT /empresas/:id/whatsapp (upsert + revalida via getStatus na Z-API — exige token completo sempre).

Escritas de usuarioId/criadoPor sempre passam valor não-nulo (placas.ts:173 `usuarioId: req.user!.id`, relatorios.ts:187 `criadoPor`). Após nullable, os `select: { usuarioId: true }` (placas.ts:180) passam a tipar `string | null` — TS pode acusar no payload de `emitPlacaClassificada`; ajustar conforme o compilador apontar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema nullable + onDelete SetNull, migração aplicada, fix TS</name>
  <files>packages/database/prisma/schema.prisma, packages/database/prisma/migrations/, apps/api/src/routes/placas.ts, apps/api/src/routes/relatorios.ts</files>
  <action>
No schema.prisma:
- `ClassificacaoHistorico.usuarioId String` → `String?`; relação → `usuario User? @relation(fields: [usuarioId], references: [id], onDelete: SetNull)`.
- `Relatorio.criadoPor String` → `String?`; relação → `usuario User? @relation("RelatoriosUsuario", fields: [criadoPor], references: [id], onDelete: SetNull)`.
- Manter os `@@index([usuarioId, ...])` e `@@index([criadoPor, ...])` (índices em campo nullable são válidos).

Gerar e aplicar migração (banco dev via db push, sem tabela `_prisma_migrations` confiável):
- ANTES: conferir catch-up — comparar `packages/database/prisma/migrations/` (três migrações: add_camera_nome, remove_evolution_zapi_provisioning, add_user_ativo) com o estado real do banco via `docker exec cargo-sentinel-postgres-1 psql -U sentinel -d cargo_sentinel -c "\d \"User\""` (verificar coluna `ativo`). Se alguma migração commitada não estiver aplicada, aplicá-la primeiro.
- Gerar SQL: `cd packages/database && npx prisma migrate diff --from-config-datasource --to-schema-datamodel prisma/schema.prisma --script > /tmp/mig.sql` (usar a flag correta do Prisma 7; se `--from-config-datasource` não existir, usar `--from-url "$DATABASE_URL"`). O SQL deve conter `ALTER COLUMN ... DROP NOT NULL` e recriação das FKs com `ON DELETE SET NULL`.
- Criar pasta `packages/database/prisma/migrations/<timestamp>_nullable_usuario_relatorio_setnull/migration.sql` com esse SQL.
- Aplicar: `docker exec -i cargo-sentinel-postgres-1 psql -U sentinel -d cargo_sentinel < migration.sql`.
- `cd packages/database && npx prisma generate`.

Fix TypeScript onde o Prisma Client agora tipa `usuarioId`/`criadoPor` como `string | null` na leitura (ex.: `select` em placas.ts:180 alimentando `emitPlacaClassificada`, e report-worker/relatorios se acusar). Escritas não mudam (sempre passam valor não-nulo). Ajustar tipos/coalescências apenas onde `pnpm --filter @cargo-sentinel/api build` acusar erro — não relaxar tipos além do necessário.
  </action>
  <verify>
    <automated>docker exec cargo-sentinel-postgres-1 psql -U sentinel -d cargo_sentinel -c "SELECT is_nullable FROM information_schema.columns WHERE table_name='ClassificacaoHistorico' AND column_name='usuarioId'" | grep -q YES && pnpm --filter @cargo-sentinel/api build</automated>
  </verify>
  <done>Coluna usuarioId (e criadoPor) is_nullable=YES no Postgres, FKs com ON DELETE SET NULL, `prisma generate` rodado, build da API passa.</done>
</task>

<task type="auto">
  <name>Task 2: Rotas admin.ts — DELETE empresa (cascata), DELETE usuário, PUT usuário com email</name>
  <files>apps/api/src/routes/admin.ts</files>
  <action>
Adicionar/estender em admin.ts (todas já atrás de requireRole('SUPER_ADMIN') no index.ts):

1. `DELETE /empresas/:id/usuarios/:usuarioId`:
   - `findFirst({ where: { id: usuarioId, empresaId: id } })`; 404 se não achar.
   - 403 se `target.role === 'SUPER_ADMIN'`.
   - `await prisma.user.delete({ where: { id: usuarioId } })`. Retornar `{ success: true }`. (O onDelete SetNull da Task 1 garante que histórico/relatórios do usuário sejam desvinculados sem erro de FK.)

2. `DELETE /empresas/:id`:
   - `findUnique({ where: { id } })`; 404 se não achar.
   - `prisma.$transaction(async (tx) => { ... })` apagando em ordem de dependência ANTES da empresa, todos `where: { empresaId: id }`:
     `tx.classificacaoHistorico.deleteMany`, `tx.relatorio.deleteMany`, `tx.evento.deleteMany`, `tx.placa.deleteMany`, `tx.configuracaoWhatsApp.deleteMany`, `tx.camera.deleteMany`, `tx.obra.deleteMany`, `tx.user.deleteMany`, e por fim `tx.empresa.delete({ where: { id } })`.
   - Retornar 200 `{ success: true }`.

3. Estender o `PUT /empresas/:id/usuarios/:usuarioId` existente para aceitar `email?` além de `nome?`/`role?`/`ativo?`:
   - Se `email !== undefined`: validar com o mesmo regex de e-mail usado em POST; normalizar `email.toLowerCase()`; adicionar a `data.email`.
   - Envolver o `prisma.user.update` em try/catch; em erro Prisma `P2002` retornar `res.status(409).json({ error: 'E-mail já cadastrado' })`.
   - Manter o 403 para alvo SUPER_ADMIN já existente.

Seguir o estilo de validação e tratamento de erro P2002/P2025 já presente no arquivo.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/api build</automated>
  </verify>
  <done>admin.ts compila; existem `router.delete('/empresas/:id')`, `router.delete('/empresas/:id/usuarios/:usuarioId')`; o PUT de usuário aceita email com 409 em duplicado e mantém 403 para SUPER_ADMIN.</done>
</task>

<task type="auto">
  <name>Task 3: Proxies BFF — DELETE empresa e DELETE usuário</name>
  <files>apps/web/src/app/api/admin-empresa-proxy/[id]/route.ts, apps/web/src/app/api/admin-usuarios-proxy/[id]/[usuarioId]/route.ts</files>
  <action>
Adicionar handlers `DELETE` seguindo exatamente o padrão dos handlers existentes nesses arquivos (INTERNAL_API_URL, `cookies()`/`cookieStore.toString()`, `params` como `Promise`, sem body para DELETE, repassar `status` do upstream):

1. Em `admin-empresa-proxy/[id]/route.ts` (já tem GET/PATCH): adicionar `export async function DELETE(_req, { params }: { params: Promise<{ id: string }> })` → `fetch(`${API_BASE}/api/admin/empresas/${id}`, { method: 'DELETE', headers: { Cookie: cookieStore.toString() } })`.

2. Em `admin-usuarios-proxy/[id]/[usuarioId]/route.ts` (já tem PUT): adicionar `export async function DELETE(_req, { params }: { params: Promise<{ id: string; usuarioId: string }> })` → `fetch(`${API_BASE}/api/admin/empresas/${id}/usuarios/${usuarioId}`, { method: 'DELETE', headers: { Cookie: cookieStore.toString() } })`.
  </action>
  <verify>
    <automated>grep -q "export async function DELETE" "apps/web/src/app/api/admin-empresa-proxy/[id]/route.ts" && grep -q "export async function DELETE" "apps/web/src/app/api/admin-usuarios-proxy/[id]/[usuarioId]/route.ts"</automated>
  </verify>
  <done>Ambos os arquivos exportam um handler DELETE que repassa para o endpoint admin correspondente com cookies e status do upstream.</done>
</task>

<task type="auto">
  <name>Task 4: UI aba Geral — editar nome/CNPJ e excluir empresa</name>
  <files>apps/web/src/app/(superadmin)/admin/empresas/[id]/empresa-detail-shell.tsx</files>
  <action>
Tornar o componente interativo (já é `'use client'`). Adicionar estado local para o modo edição e modal de exclusão.

Editar:
- Botão "Editar" no bloco da aba Geral. Ao clicar, transforma os campos Nome e CNPJ em `<input>` controlados (inicializados com `empresa.nome`/`empresa.cnpj`) com botões "Salvar" e "Cancelar".
- Salvar: `PATCH /api/admin-empresa-proxy/${empresaId}` com `{ nome, cnpj }`. Em erro 400 (ex.: CNPJ duplicado) exibir a mensagem `body.error` inline (padrão de erro vermelho já usado no projeto). Ao sucesso, atualizar o estado exibido e sair do modo edição. Usar `router.refresh()` (import `useRouter` de `next/navigation`) para revalidar os dados do server component.

Excluir empresa (destrutivo, conforme decisão locked #1):
- Botão "Excluir empresa" em estilo destrutivo (vermelho), separado do fluxo de editar.
- Abre modal (mesmo padrão visual dos modais de usuarios-tab.tsx) pedindo digitar o NOME EXATO da empresa; o botão de confirmar fica `disabled` até `inputConfirmacao === empresa.nome`.
- Ao confirmar: `DELETE /api/admin-empresa-proxy/${empresaId}`; em sucesso `router.push('/admin')`; em erro exibir mensagem inline no modal.
Manter o `SuspendButton` existente no cabeçalho.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/web build</automated>
  </verify>
  <done>Aba Geral tem edição inline de nome/CNPJ (PATCH) com tratamento de erro, e botão Excluir empresa com modal de confirmação por nome digitado que faz DELETE e redireciona para /admin. Build web passa.</done>
</task>

<task type="auto">
  <name>Task 5: UI aba Usuários — editar nome/email e excluir usuário</name>
  <files>apps/web/src/app/(superadmin)/admin/empresas/[id]/usuarios-tab.tsx</files>
  <action>
Estender usuarios-tab.tsx (mantendo os fluxos existentes de criar/role/ativo/resetar-senha).

Editar nome/email:
- Botão de editar (ícone lápis / `Pencil` de lucide-react) por linha, exceto para SUPER_ADMIN.
- Abre modal no mesmo padrão dos modais existentes, campos Nome e Email pré-preenchidos.
- Salvar: `PUT /api/admin-usuarios-proxy/${empresaId}/${usuarioId}` com `{ nome, email }`. Tratar 409 (email duplicado) e 403 exibindo `body.error` inline no modal. Ao sucesso, fechar modal e `loadUsuarios()`.

Excluir usuário (decisão locked #2 — sempre permitido):
- Botão lixeira (`Trash2`) por linha, NÃO renderizado para linhas `role === 'SUPER_ADMIN'`.
- `window.confirm('Excluir este usuário? Esta ação não pode ser desfeita.')`; se confirmado, `DELETE /api/admin-usuarios-proxy/${empresaId}/${usuarioId}`; em erro `alert(body.error)`; em sucesso `loadUsuarios()`.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/web build</automated>
  </verify>
  <done>Cada linha de usuário (não-SUPER_ADMIN) tem editar nome/email (PUT, trata 409/403) e excluir (DELETE com confirmação). Build web passa.</done>
</task>

<task type="auto">
  <name>Task 6: UI aba WhatsApp — editar credenciais de instância vinculada</name>
  <files>apps/web/src/app/(superadmin)/admin/empresas/[id]/whatsapp/whatsapp-provision-client.tsx</files>
  <action>
Quando `wpp.vinculada === true`, adicionar botão "Editar credenciais" ao lado de Desconectar/Remover.

Ao clicar, reabrir o mesmo `<form>` de vincular (reaproveitar o form do ramo `else`), pré-preenchendo `form.instanceId` com `wpp.instanceId`. Como o `PUT /empresas/:id/whatsapp` revalida contra a Z-API via `getStatus` e exige `token` completo (confirmar: o backend retorna 400 se token vazio — sim, `if (!instanceId || !token)`), NÃO pré-preencher token/clientToken: pedir o token novamente por completo, com texto explicando que as credenciais serão revalidadas na Z-API ao salvar.
Adicionar estado `editando` (boolean). Botão "Cancelar" volta para a visualização do card (`setEditando(false)`), sem apagar nada. Ao salvar com sucesso (`salvarWhatsapp`), `setEditando(false)` e `carregar()`.
Reutilizar `salvarWhatsapp` existente (o PUT já faz upsert). Não duplicar lógica de rede.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/web build</automated>
  </verify>
  <done>Card de instância vinculada tem botão "Editar credenciais" que abre o formulário com instanceId pré-preenchido, token pedido de novo, texto de revalidação, e Cancelar que volta ao card. Build web passa.</done>
</task>

<task type="auto">
  <name>Task 7: Verificação final — builds + teste de exclusão com histórico</name>
  <files>apps/api/src/routes/admin.ts</files>
  <action>
Verificação end-to-end (sem novo código de produção, salvo pequenos fixes que os builds/testes acusarem):
- `pnpm --filter @cargo-sentinel/api build` e `pnpm --filter @cargo-sentinel/web build` devem passar limpos.
- Provar que a decisão #2 funciona: no banco dev, criar (ou localizar) um usuário com pelo menos um `ClassificacaoHistorico.usuarioId` apontando para ele e excluí-lo via `docker exec ... psql` (`DELETE FROM "User" WHERE id='...'`) OU chamando a rota; confirmar que NÃO há erro de FK e que o histórico correspondente ficou com `usuarioId` NULL (SetNull). Documentar o comando usado no SUMMARY.
Não introduzir dependências novas. Os testes pré-existentes quebrados (`eventos.test.ts`/`placas.test.ts`, mock garage incompleto) são conhecidos e fora de escopo — não corrigir aqui.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/api build && pnpm --filter @cargo-sentinel/web build</automated>
  </verify>
  <done>Ambos os builds passam; exclusão de usuário com histórico associado não falha por FK e deixa o histórico com usuarioId NULL.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cargo-sentinel/api build` passa.
- `pnpm --filter @cargo-sentinel/web build` passa.
- Coluna `ClassificacaoHistorico.usuarioId` e `Relatorio.criadoPor` são nullable no Postgres, FKs com ON DELETE SET NULL.
- Rotas: `DELETE /empresas/:id` (cascata), `DELETE /empresas/:id/usuarios/:usuarioId` (403 SUPER_ADMIN), `PUT` usuário aceita email (409 duplicado).
- Proxies DELETE existem para empresa e usuário.
- UI: Geral edita/exclui empresa (confirmação por nome), Usuários edita nome/email + exclui, WhatsApp edita credenciais.
</verification>

<success_criteria>
CRUD do painel super admin completo: super admin consegue editar e excluir empresa (cascata com confirmação por nome), editar nome/email e excluir usuários (mesmo com histórico, via SetNull), e reeditar credenciais Z-API de uma empresa já vinculada. Ambos os builds passam e a exclusão de usuário com histórico não falha por foreign key.
</success_criteria>

<output>
After completion, create `.planning/quick/260712-t5s-adicionar-editar-excluir-empresa-usu-rio/260712-t5s-SUMMARY.md`
</output>
