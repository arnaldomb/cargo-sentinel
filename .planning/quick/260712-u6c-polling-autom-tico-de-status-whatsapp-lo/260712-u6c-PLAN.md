---
type: quick
slug: 260712-u6c-polling-autom-tico-de-status-whatsapp-lo
autonomous: true
files_modified:
  - apps/api/src/jobs/alert-worker.ts
  - apps/web/src/app/(admin)/configuracoes/whatsapp/whatsapp-client.tsx
---

<objective>
Corrigir duas lacunas encontradas ao testar o fluxo real de WhatsApp: (1) a tela do tenant não atualizava o status da instância automaticamente após escanear o QR, exigindo clique manual em "Verificar Status"; (2) o alert-worker pulava o envio de alertas silenciosamente (sem log) quando a instância não estava conectada, config ausente, etc — tornando impossível diagnosticar por que um alerta de placa SUSPEITO não chegou ao grupo.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Logs de skip no alert-worker</name>
  <files>apps/api/src/jobs/alert-worker.ts</files>
  <action>
Reestruturar o bloco `if (data.type === 'alert:whatsapp')` de `processAlertJob` com early returns explícitos e logados para cada condição de skip: configWhatsApp ausente, ativo=false, whatsappInstStatus != CONECTADO, zapiConfigFrom retorna null, classificação fora do filtro, nenhum destino configurado. Comportamento de envio (sendWhatsAppText, try/catch) inalterado.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/api build && cd apps/api && npx vitest run alert-worker</automated>
  </verify>
  <done>Build passa; 14/14 testes de alert-worker.test.ts passam; cada skip agora loga o motivo.</done>
</task>

<task type="auto">
  <name>Task 2: Polling automático de status no whatsapp-client.tsx</name>
  <files>apps/web/src/app/(admin)/configuracoes/whatsapp/whatsapp-client.tsx</files>
  <action>
Adicionar useEffect com setInterval (4s) que faz polling de GET /api/configuracoes-whatsapp-proxy/status enquanto instanciaVinculada && whatsappInstStatus !== 'CONECTADO', atualizando o estado local silenciosamente (sem alert()). Cleanup via clearInterval. Botão "Verificar Status" mantido. Indicador visual leve "Aguardando conexão... verificando automaticamente" enquanto não conectado.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/web build</automated>
  </verify>
  <done>Build passa; status atualiza automaticamente sem precisar clicar no botão.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cargo-sentinel/api build` e testes de alert-worker passam.
- `pnpm --filter @cargo-sentinel/web build` passa.
</verification>

<success_criteria>
- Status da instância WhatsApp atualiza sozinho na tela do tenant após conectar, sem exigir clique manual.
- Alertas WhatsApp pulados por qualquer motivo (desconectado, sem config, fora do filtro, etc) deixam rastro no log do alert-worker.
</success_criteria>
