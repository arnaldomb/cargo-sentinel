---
type: quick
slug: 260712-rh1-corrigir-upsert-inv-lido-de-obra-em-seed
autonomous: true
files_modified:
  - packages/database/src/seed.ts
---

<objective>
Corrigir bug em packages/database/src/seed.ts: o upsert de Obra usava `where: { empresaId_nome: {...} } }`, mas o model Obra no schema.prisma não tem essa unique constraint composta (só `@@index([empresaId])`), quebrando `tsc` (TS2353) e por consequência o build Docker de api/web.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Substituir upsert por findFirst+create idempotente</name>
  <files>packages/database/src/seed.ts</files>
  <action>
Trocar o `prisma.obra.upsert({ where: { empresaId_nome: {...} } })` por `findFirst({ where: { empresaId, nome } }) ?? create({...})`, mantendo idempotência sem exigir nova unique constraint no schema.
  </action>
  <verify>
    <automated>pnpm --filter @cargo-sentinel/database build</automated>
  </verify>
  <done>tsc builda sem erros; comportamento idempotente preservado.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cargo-sentinel/database build` passa sem erros.
</verification>

<success_criteria>
- seed.ts não referencia mais `empresaId_nome`.
- Rodar o seed múltiplas vezes não duplica "Obra Centro".
</success_criteria>