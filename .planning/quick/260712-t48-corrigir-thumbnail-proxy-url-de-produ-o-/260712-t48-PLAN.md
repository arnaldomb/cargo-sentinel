---
type: quick
slug: 260712-t48-corrigir-thumbnail-proxy-url-de-produ-o-
autonomous: true
files_modified:
  - docker-compose.override.yml
---

<objective>
Corrigir imagens não carregando no Feed Operacional em dev local: `THUMBNAIL_PROXY_URL` herdava o domínio de produção (`https://portal.ggtronic.com.br`) do `.env`, gerando URLs de miniatura que o navegador (em `localhost:3000`) não consegue resolver/autenticar.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Override THUMBNAIL_PROXY_URL para localhost em dev</name>
  <files>docker-compose.override.yml</files>
  <action>
No serviço `api` de docker-compose.override.yml, adicionar `THUMBNAIL_PROXY_URL: "http://localhost:3000/api/image-proxy"`.
  </action>
  <verify>
    <manual>docker compose up -d api; GET http://localhost:3000/api/image-proxy?key=&lt;fotoGarageKey real&gt; com cookie de sessão válido retorna JPEG.</manual>
  </verify>
  <done>Thumbnails resolvem contra localhost:3000 em vez do domínio de produção.</done>
</task>

</tasks>

<verification>
- curl ao image-proxy com uma key real retorna imagem JPEG válida (confirmado: 78KB, JFIF válido).
</verification>

<success_criteria>
- Feed Operacional carrega miniaturas em dev local via docker-compose.
- docker-compose.yml (produção) não foi alterado.
</success_criteria>
